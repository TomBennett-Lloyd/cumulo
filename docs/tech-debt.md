# Tech-debt log

A **buffer, not an archive**. `/review-loop` appends SYSTEMIC findings here instead of iterating on them; `/triage` periodically clusters entries, files root-cause GitHub issues, and **deletes** what it captured. A long-lived entry here means triage is overdue.

Entry format:

```
## YYYY-MM-DD — short title
- Where: file/module references
- What: the pattern or problem (symptom AND suspected root cause if known)
- Source: PR/issue #
```

Pointers must survive unrelated edits: cite **files and symbol or section names** (function names, headings, config keys, script names) — never bare line numbers, and never a copied code literal unless the literal itself is the finding. An entry that pins its claim to `file.sh`:42 sends its reader to whatever happens to sit at line 42 months later. Applies to entries dated 2026-07-31 onward; the back catalogue is retrofitted opportunistically, whenever an entry is edited for any other reason.

---

## Captured-entry redirects

Source comments across the repo cite entries by title ("recorded in `docs/tech-debt.md` (…)"). Triage deletes the entry but cannot edit those comments without turning a docs-only PR into a source-code one, so this table is where a dangling pointer lands. It is a **redirect, not an archive** — the content lives on the issue.

Maintenance: a row dies with its issue. Each capturing issue's implementation edits the very files whose comments cite it, so the citation and the row go together; whoever closes the issue deletes the row.

**Resolving a conflict here after a pruning pass** (2026-08-11 pruned 36 entries, so every branch open across that day conflicts): take `main`'s pruned file as the base and re-apply only your branch's own additions — a union of **changes**, never a union of lines, because a line-union silently resurrects everything just deleted. A deletion on either side is authoritative and is never reinstated. Capture the heading and redirect-row sets of base, main and branch **before** rebasing and the resolution becomes arithmetic rather than judgement; `review-loop` step 5 states the full procedure.

| Captured entry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Now owned by                                                                                                                                               |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `check-supply-chain-policy.sh` reads YAML by line shape · Supply-chain gate asserts the manifest, but pnpm resolves policy from four places · `check-infra-mirrors.sh` reads Terraform by line shape                                                                                                                                                                                                                                                                                                                                                 | [#158](https://github.com/TomBennett-Lloyd/cumulo/issues/158)                                                                                              |
| Only `@cumulo/shared` can express a type-level test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#159](https://github.com/TomBennett-Lloyd/cumulo/issues/159)                                                                                              |
| Skill score compares two RMSEs computed over different sample sets · A metrics row cannot say which hours of its own period are missing                                                                                                                                                                                                                                                                                                                                                                                                              | [#160](https://github.com/TomBennett-Lloyd/cumulo/issues/160)                                                                                              |
| No error boundary above the dashboard's async work · The map's placeholder shells restate `MapView`'s structure · Loading announcements are per-component conventions                                                                                                                                                                                                                                                                                                                                                                                | [#161](https://github.com/TomBennett-Lloyd/cumulo/issues/161)                                                                                              |
| `invalid-response` now means two different failures · The shared data hook is still named after the surface that was deleted                                                                                                                                                                                                                                                                                                                                                                                                                         | [#162](https://github.com/TomBennett-Lloyd/cumulo/issues/162)                                                                                              |
| Cycle rotation advances one location per hour, not one window                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#163](https://github.com/TomBennett-Lloyd/cumulo/issues/163)                                                                                              |
| One ingestion alarm now stands for two unrelated operator questions · A forecast consumer pointed at the wrong environment is indistinguishable from an idle fleet · A user-site counter stuck above the real population                                                                                                                                                                                                                                                                                                                             | [#164](https://github.com/TomBennett-Lloyd/cumulo/issues/164)                                                                                              |
| A hook that cannot read its own events disables edit-time lint silently                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [#102](https://github.com/TomBennett-Lloyd/cumulo/issues/102) (comment)                                                                                    |
| The port-inversion invariant in `@cumulo/hindcast` is convention, not a gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [#112](https://github.com/TomBennett-Lloyd/cumulo/issues/112) (comment)                                                                                    |
| No gate covers a constant restated in prose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [#249](https://github.com/TomBennett-Lloyd/cumulo/issues/249)                                                                                              |
| CI never builds `apps/web`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#142](https://github.com/TomBennett-Lloyd/cumulo/issues/142) (comment)                                                                                    |
| `Retry-After` is unreadable cross-origin, so the client contract's backoff can never fire                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [#21](https://github.com/TomBennett-Lloyd/cumulo/issues/21) (comment)                                                                                      |
| The bundle gate's "stale build" verdict only detects multi-entry staleness                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#111](https://github.com/TomBennett-Lloyd/cumulo/issues/111) (comment)                                                                                    |
| The conflict-retry budget is derived from a closed cohort, so exhausting it is read as a fault it need not be                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#155](https://github.com/TomBennett-Lloyd/cumulo/issues/155) (comment)                                                                                    |
| actionlint's action-input check is dead over most of this repo's `uses:` lines                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [#108](https://github.com/TomBennett-Lloyd/cumulo/issues/108) (comment)                                                                                    |
| The two batch paths refuse an unusable policy in different shapes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [#100](https://github.com/TomBennett-Lloyd/cumulo/issues/100) (comment)                                                                                    |
| The split-capture migration silently narrowed the harness family's raw `$out` readers · reap's broken-git-link shape arm is shadowed by the identity arm                                                                                                                                                                                                                                                                                                                                                                                             | [#101](https://github.com/TomBennett-Lloyd/cumulo/issues/101) (comment)                                                                                    |
| The half-open UTC window is still declared five ways beside its named contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [#232](https://github.com/TomBennett-Lloyd/cumulo/issues/232)                                                                                              |
| Browser-side fetches have no timeout policy, and the de-dup map inherits the hang · The ingestion cycle deadline is conditional on no mid-body stall, and no finite slack covers it                                                                                                                                                                                                                                                                                                                                                                  | [#233](https://github.com/TomBennett-Lloyd/cumulo/issues/233)                                                                                              |
| The API deadline gates loops, not the straight-line prefix · `queryAllPages`'s pagination bound is optional, and four of five call sites walk unbounded                                                                                                                                                                                                                                                                                                                                                                                              | [#234](https://github.com/TomBennett-Lloyd/cumulo/issues/234)                                                                                              |
| The token preview hand-rolls a chart the chrome convention cannot reach · A dashboard test pins the chart's chrome wording by whole-table `toEqual` · Five of the twelve consolidated strings sit outside every phrase-class sweep · The spoken chart readout carries neither the unit nor the clock                                                                                                                                                                                                                                                 | [#235](https://github.com/TomBennett-Lloyd/cumulo/issues/235)                                                                                              |
| The hooks' "stdin was unreadable" report is dead code behind a hang                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#236](https://github.com/TomBennett-Lloyd/cumulo/issues/236)                                                                                              |
| Neither popover has focus-out dismissal, and the second one has now copied the first · The collapsed search bar dismisses on any focus-out · A site chosen through the collapsed search bar has no landing · Whether a keyboard reader can get from the landing back into the card is asserted by nobody                                                                                                                                                                                                                                             | [#349](https://github.com/TomBennett-Lloyd/cumulo/issues/349)                                                                                              |
| Stylesheet facts with no observer (four stacking values, the sheet's own import, the credits' veil, the credit-link treatment, the site card's anchoring) · Does every equally-stacked overlay owe an occlusion case                                                                                                                                                                                                                                                                                                                                 | [#350](https://github.com/TomBennett-Lloyd/cumulo/issues/350)                                                                                              |
| No a11y linting, on the diff that lands the app's first seven interaction patterns · The app's two native dialogs disagree about whether a modal names itself                                                                                                                                                                                                                                                                                                                                                                                        | [#351](https://github.com/TomBennett-Lloyd/cumulo/issues/351)                                                                                              |
| The chart's `Raw data` disclosure label is spelled in three places and owned by none of them · One chart is named three times, in three independent spellings                                                                                                                                                                                                                                                                                                                                                                                        | [#352](https://github.com/TomBennett-Lloyd/cumulo/issues/352)                                                                                              |
| The selection-focus rule is re-argued at nearly twenty sites instead of pointed at — #328 removed the D14 landing and the sprawl simply re-argues its absence                                                                                                                                                                                                                                                                                                                                                                                        | [#353](https://github.com/TomBennett-Lloyd/cumulo/issues/353)                                                                                              |
| A selection change is one commit ahead of the state that answers for it · Every range press re-spends the whole forecast read                                                                                                                                                                                                                                                                                                                                                                                                                        | [#354](https://github.com/TomBennett-Lloyd/cumulo/issues/354)                                                                                              |
| The header search answers "No matching sites" while the fleet is still loading · Re-selecting the site that is already selected is a complete no-op · The search shows a capped number of matches and never says there were more                                                                                                                                                                                                                                                                                                                     | [#355](https://github.com/TomBennett-Lloyd/cumulo/issues/355)                                                                                              |
| Three constants answer "how wide is this string at `--text-xs`", and the one no gate can see is 17% narrow                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#357](https://github.com/TomBennett-Lloyd/cumulo/issues/357)                                                                                              |
| The Node/pnpm setup block is copied into every workflow job that builds · No gate asserts a workflow's credential properties, in either direction · The Pages workflow's bundle-gate comment paraphrases the gate · The Pages teardown calls five hedges "one-line"                                                                                                                                                                                                                                                                                  | [#358](https://github.com/TomBennett-Lloyd/cumulo/issues/358)                                                                                              |
| review-loop's classification step fuses three owners into one paragraph · The adopted orchestration spec carries two internal count and cross-reference errors · A review-feedback entry carries a category the closed vocabulary does not declare                                                                                                                                                                                                                                                                                                   | [#359](https://github.com/TomBennett-Lloyd/cumulo/issues/359)                                                                                              |
| A standards bullet is amended by paraphrase, in a comment three files from the rule it reinterprets                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#360](https://github.com/TomBennett-Lloyd/cumulo/issues/360)                                                                                              |
| A second family of stated values is restated in prose with neither pointer nor ledger · Three owned values have restatement carriers and no rule-9 ledger · `ci.yml`'s hermetic-claim comment names the basemap provider outside the ledger · The contrast record's literals travel · Five stacks validate the environment alphabet nobody declared them against · The header breakpoint's restatement ledger was one site short · Three "there is no breakpoint" statements are scoped-true · The README's CI row is a census that stopped counting | [#249](https://github.com/TomBennett-Lloyd/cumulo/issues/249) (comment)                                                                                    |
| Every `cumulo-series` volume figure in the repo prices two models · The infra README still describes #136's forecast consumer as unbuilt                                                                                                                                                                                                                                                                                                                                                                                                             | [#289](https://github.com/TomBennett-Lloyd/cumulo/issues/289) (comment)                                                                                    |
| The per-IP limiter's route coverage is asserted one hand-written case at a time                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [#309](https://github.com/TomBennett-Lloyd/cumulo/issues/309) (comment)                                                                                    |
| Light-mode `--color-accent` is below AA for small text on its own surfaces                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#273](https://github.com/TomBennett-Lloyd/cumulo/issues/273) (comment)                                                                                    |
| The token gallery renders two colour tokens as chips with no colour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#297](https://github.com/TomBennett-Lloyd/cumulo/issues/297) (comment)                                                                                    |
| The app holds three incompatible verdicts on `calc(var(--space-1) / 4)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [#103](https://github.com/TomBennett-Lloyd/cumulo/issues/103) (comment)                                                                                    |
| Rule 9 exempts ADRs, so an ADR restating another ADR's figure has no discipline at all · Rule 9 exempts ADRs from the restatement ledger, and the Amendments convention made that exemption false                                                                                                                                                                                                                                                                                                                                                    | [#401](https://github.com/TomBennett-Lloyd/cumulo/issues/401)                                                                                              |
| The ADR amendment procedure and its own guardrail collide, and practice has silently picked a winner · The Amendments convention has no category for a value that was wrong when written · Amending an ADR turns every verbatim quotation of it into a misquotation · A value true-up moved the numerals and left behind the prose characterisations derived from them                                                                                                                                                                               | [#402](https://github.com/TomBennett-Lloyd/cumulo/issues/402)                                                                                              |
| The wall-clock admission invariant has three prose carriers and no rule-9 ledger beside its owner · The map treatment's Attribution section forbids restating the veil's ratios three lines after restating them · The `docs/review-feedback.md` path has fourteen carriers and no restatement ledger                                                                                                                                                                                                                                                | [#403](https://github.com/TomBennett-Lloyd/cumulo/issues/403)                                                                                              |
| A docblock that reasons in counterfactual worked numbers has no gate, and four review cycles each planted a new false claim in it                                                                                                                                                                                                                                                                                                                                                                                                                    | [#405](https://github.com/TomBennett-Lloyd/cumulo/issues/405)                                                                                              |
| Since D3 the forecast chart is a keyboard tab stop in every state · The live region's place in the figure is a written contract that nothing asserts                                                                                                                                                                                                                                                                                                                                                                                                 | [#410](https://github.com/TomBennett-Lloyd/cumulo/issues/410)                                                                                              |
| The tooltip panel's width tracks its content, against `design.md` rule 6's "never layout"                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [#411](https://github.com/TomBennett-Lloyd/cumulo/issues/411)                                                                                              |
| `rotateMap`'s vertical stroke is hard-coded upward and can leave the viewport                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#383](https://github.com/TomBennett-Lloyd/cumulo/issues/383) (comment)                                                                                    |
| The lane's `projects` docblock prices a second engine against a count nobody kept                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [#249](https://github.com/TomBennett-Lloyd/cumulo/issues/249) (comment)                                                                                    |
| One CSP rendering rule, implemented twice in two languages, with no gate that compares them                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [#158](https://github.com/TomBennett-Lloyd/cumulo/issues/158) (comment)                                                                                    |
| The zod parse path under test is not the one that ships                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [#182](https://github.com/TomBennett-Lloyd/cumulo/issues/182) (comment)                                                                                    |
| `review-loop` step 5's human classification contradicts `reviewedSourceRule`                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [#359](https://github.com/TomBennett-Lloyd/cumulo/issues/359) (comment)                                                                                    |
| The merge-ritual gate's logic is inline YAML, so only its passing arm is ever exercised                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [#358](https://github.com/TomBennett-Lloyd/cumulo/issues/358) (comment)                                                                                    |
| `chart-treatment.md`'s legend table names a series the app stopped shipping in #264                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#397](https://github.com/TomBennett-Lloyd/cumulo/issues/397) (comment)                                                                                    |
| A drawing-space case still says "measured width" six lines under the docblock explaining that nothing measures it · The readout suite carries the last instance of D6's retired "not modelled" framing                                                                                                                                                                                                                                                                                                                                               | [#353](https://github.com/TomBennett-Lloyd/cumulo/issues/353) (comment)                                                                                    |
| `browser-smoke.md` obliges the agent to emit findings, and its report block has nowhere to put them                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#413](https://github.com/TomBennett-Lloyd/cumulo/issues/413) — fixed, not captured: `browser-smoke.md` gained its `Findings:` slot in that issue's own PR |
| `git diff --check` is staging-sensitive, and the marker sweep pairs with it at the one moment the files are staged                                                                                                                                                                                                                                                                                                                                                                                                                                   | [#412](https://github.com/TomBennett-Lloyd/cumulo/issues/412) — captured into `docs/standards/evidence.md` by that issue's own PR                          |

---

## 2026-07-30 — `aws_budgets_budget.cost_types` left at AWS defaults

- Where: `infra/bootstrap/budget.tf` (`aws_budgets_budget.monthly_cost_ceiling`)
- What: no `cost_types` block, so the budget uses the AWS defaults, which subtract credits and refunds. On an account carrying promotional credits the meter can therefore run well past $100/month of gross usage while net cost stays under threshold and nothing alerts — the alarm reports what will be billed, not what is being consumed. That is a defensible reading of "cost ceiling" for a project whose ceiling is about the bank balance, and it is the current deliberate choice; it stops being defensible the moment credits land on the account, because the whole point of the ceiling is to catch runaway usage _before_ it is expensive. Revisit if credits appear (or before any AWS-credits programme is used for this project): either add `cost_types { include_credit = true, include_refund = false }`, or add a second usage-oriented budget beside the billed-cost one. Not a fix for this diff — it is a policy decision about what the number means, and it wants the account's credit state as an input.
- Source: #38 review cycle 1
- Triage note (2026-07-31): deliberately left in the buffer rather than filed. It is a parked decision with an external trigger (credits appearing on the account), not debt to clear — an open issue would be picked up by backlog burning and correctly do nothing. Convert it the moment the trigger fires.
- Triage note (2026-08-01): re-confirmed on the pass that emptied the other 29 entries. The trigger has not fired; this is the log's only survivor **by decision**, so its age is not evidence that triage is overdue.
- Triage note (2026-08-03): re-confirmed again on the pass that emptied the other 22 entries. Trigger still unfired.
- Triage note (2026-08-10): re-confirmed on the pass that emptied the other 41 entries. Trigger still unfired. It is no longer the log's only survivor by decision — this pass left seven others, each carrying its own dated note below saying why — so read the survivor set, not this entry alone, as the record of what triage considered and kept.
- Triage note (2026-08-11): re-confirmed on the pass that emptied the other 36 entries. Trigger still unfired. The pass's other survivors each carry their own dated note below; entries appended after it — this PR's own review loop added one — are new findings rather than survivors, and carry no triage note.

## 2026-08-02 — canon spawns an interpreter per path, and the sweep multiplies it

- Where: `canon` in `.claude/scripts/worktree-lib.sh`; its per-porcelain-line call sites in `reap-worktree.sh` and `sweep-worktrees.sh`
- What: the python→node migration kept canon's one-path-per-process shape, so reap is O(worktrees) node spawns and a sweep is O(N²); the lifecycle harness measured 27 s → 42 s on the same cases. No correctness impact and fine at this repo's worktree counts — recorded so the cost has a name. Fix direction if it ever bites: a `canon_many` reading a NUL-separated list in one spawn, or batching the porcelain parse's canon calls.
- Source: #102 review cycle 1
- Triage note (2026-08-03): left in the buffer, not filed. It is a measured cost with no correctness consequence and a named trigger ("if it ever bites" — worktree counts rising, or the lifecycle harness becoming slow enough to discourage running it); an open issue would be scheduled and would correctly find nothing worth doing. It clusters with nothing else in the log — every other script-harness entry this pass captured was about an assertion proving less than it claims, which this is not.
- Triage note (2026-08-10): considered and left again. The named trigger has not been met — worktree counts have not risen and the lifecycle harness is still run — so filing it would create an issue that correctly does nothing.
- Triage note (2026-08-11): the trigger is closer than it was — twelve worktrees were swept in one day — but the sweep still ran without anyone waiting on it. Left again; if worktree counts keep rising, re-measure the lifecycle harness rather than re-reading this note.

## 2026-08-03 — A provider payload repeating an hour is reported as ingestion's bug

- Where: `parseForecastResponse` in `apps/ingestion/src/open-meteo/response.ts` (its `hourly.time` loop and its `malformed` arm); the refusal it currently lands on is `requireUniqueKeys` in `packages/storage/src/write-preconditions.ts`, reached through `putForecastWeather` in `packages/storage/src/adapters/weather/weather-adapter.ts`
- What: `hourly.time` is mapped 1:1 into readings with no uniqueness check, so a payload that repeats an hour yields two readings for one location-hour and is caught a layer down, where the storage precondition refuses the write with "the caller must de-duplicate before writing". That message blames ingestion's code for the provider's payload and sends the reader to the wrong repository — the inverse of rule 1's blame tiebreak, which asks whose fault it is and wants the check owned by the `malformed` arm in `response.ts`, the arm that already names Open-Meteo as the party at fault and whose `ParsedForecast` failure value the cycle already knows how to report. Debt rather than blocker on two counts: the write is refused rather than corrupted, and the blast radius is correctly bounded to one location's cycle outcome. Only the blame is misrouted, and only on a payload we have never actually seen.
- Source: #100 review cycle 1
- Triage note (2026-08-10): considered and left. An unrelated singleton — nothing else in the log this pass was about blame routing on the ingestion parse path, so it clusters with no other entry and would file as an issue of one. Kept in the buffer until a second ingestion-blame entry gives it a root cause to share.
- Triage note (2026-08-11): unchanged. Nothing merged since touches the ingestion parse path, so the second entry that would give it a root cause has still not arrived.

## 2026-08-02 — `drainWriteRequests`' policy seam outlived the caller that justified it

- Where: `drainWriteRequests` and `putSeriesItems` in `packages/storage/src/adapters/series/series-adapter.ts`
- What: with `deleteSiteSeries` retired (ADR 0007), `drainWriteRequests` has exactly one caller and its `policy` parameter only ever receives `this.batchPolicy` — the doc sentence that justified the seam ("how hard to push" is where a cleanup and an ingestion write legitimately disagree) was deleted with the caller that made it true, but the seam itself remains. Whether the adapter's two one-caller private write layers still earn their split is a `packages/storage` shape question: collapsing them touches the ingestion write path, which the retiring diff deliberately left alone. Decide next time that path is open — collapse into `putSeriesItems`, or restate a reason the parameter exists.
- Source: #167 review cycle 1
- Triage note (2026-08-10): considered and left. A singleton below the clustering threshold, and the entry's own condition ("decide next time that path is open") makes it a decision waiting on a diff rather than debt to schedule.
- Triage note (2026-08-11): condition still unmet — the ingestion write path has not been opened since. Left.

## 2026-08-05 — `response.ts` calls itself the only source of responses while two 204s are hand-built beside it

- Where: the module docblock at `apps/api/src/http/response.ts:6-7` ("the two ways this API ever produces one — a validated success body, or an `apiErrorSchema` failure"); the counter-examples are `apps/api/src/sites/delete-site.ts:199` and, as of this branch, the preflight answer in `apps/api/src/http/router.ts`
- What: the claim was already false before #263 — `delete-site.ts` hand-builds `{ statusCode: 204, headers: {} }` rather than going through any helper here — and the preflight 204 makes it two. So the file that owns the response vocabulary describes a vocabulary the service does not keep to, and each new 204 is another object literal whose "no body, no content-type" reasoning is re-derived at the call site while the reasoning for it already lives in this file's own `ApiResponse` docblock (lines 18-19). Wanted: a named `noContentResponse()` exported here, the two call sites routed through it, and the docblock trued to three ways. Deliberately not forced into #263's diff: the two 204s are the same shape for genuinely different reasons (a deleted resource versus a CORS mechanism answer), so they may legitimately diverge — a shared helper is only right if it stays a single fact, and `structure.md` rule 7 says that judgement wants its own look rather than a drive-by extraction under a fix-now.
- Source: #263 review cycle 1
- Triage note (2026-08-10): considered and left. It reads as a member of the restatement family that #249 and #360 captured, but it is not one — the false claim is a docblock's census of its own module, with no owned value and no ledger to join — so folding it into either would have widened that issue past its root cause. On its own it is a singleton below the clustering threshold.
- Triage note (2026-08-11): re-examined against [#405](https://github.com/TomBennett-Lloyd/cumulo/issues/405), which this pass filed for docblocks whose claims nothing can pin. Still not a member: #405 is about prose reasoning from configurations the code does not have, where this is a census of code that exists and could simply be counted. Left as a singleton — and it is now the second entry in this log whose defect is a module docblock describing itself wrongly, so a third makes a cluster.

## 2026-08-10 — The close-affordance gate's reach is narrower than it looks, in two ways it does not say

- Where: `isGoverned` in `apps/web/src/close-affordance-contract.test.ts`, against the sibling idiom it is copied from — `isGoverned` in `apps/web/src/dashboard/state-copy-contract.test.ts`
- What: two gaps, neither a defect and neither written down until now. First, the copy narrows to `path.endsWith('.tsx')` where the original matches `/\.tsx?$/`, so a `.ts` module is outside the sweep here and inside it there; the narrowing is probably right (a close affordance is JSX) but it is undocumented, and a reader comparing the two idioms cannot tell a decision from a transcription slip. Second, and shared by both files: a text sweep sees the word only where the word is written, so `<button aria-label="…">{CLOSE_LABEL}</button>` with the string living in a `*-copy.ts` module evades both patterns of both gates — the exact-line one and the inline one. That is inherent to sweeping sources as text rather than rendering them, and the component tests named in the file's own docblock (`map/SitePopoverCard.test.tsx`, `header/AboutDialog.test.tsx`) are what actually see a rendered affordance. Recorded rather than fixed because the gate is worth what it costs as it stands; the point is that its reach should be legible from the file instead of living in a reviewer's head.
- Source: #327 design batch, review cycle 1
- Triage note (2026-08-11): considered and left. Nearest neighbour is [#403](https://github.com/TomBennett-Lloyd/cumulo/issues/403) (owned values whose carriers are unenumerated), and this is not that: the gate's reach is a property of a sweep's own pattern, not a value with copies. Singleton, with a natural trigger — the next time either `isGoverned` is edited, document the `.tsx`-only narrowing there rather than here.

## 2026-08-10 — The deterministic regression case inherits the lane's flakiest step

- Where: the file-level `test.beforeEach` in `apps/web/e2e/composition.spec.ts` (`routeBasemap` + `page.goto('/')`), against the `layoutBoxOf returns the box its poll observed rather than re-reading it` case at the foot of the same file
- What: the new regression case takes no `page` fixture — its whole value is that it drives `layoutBoxOf` from a hand-built `BoxSource` and is therefore deterministic — but Playwright runs the file-level hook for every test in the file regardless, so the one case that needs no browser state still pays for a basemap route and a full page boot before it runs. The fix is a `test.describe` around the composition cases that owns the hook, leaving the helper's case outside it; that is a whole-file re-indent for a small gain, which is why it is logged rather than taken. The failure mode is benign in the meantime: a boot failure reds every case in the file and attributes itself to the hook, so nobody is misled about what broke — the cost is a deterministic case that can be failed by something it does not depend on.
- Source: #367 review cycle 1
- Triage note (2026-08-11): considered and left. It sits beside [#404](https://github.com/TomBennett-Lloyd/cumulo/issues/404), which captures every other box-helper entry from the same review, and is deliberately not folded in: #404 is about where the lane's measuring instruments live, while this is about which cases a file-level hook should cover. The re-indent it asks for is cheap to take while #404's extraction is open, and pointless before.
- Update (2026-08-11, #404): this entry is **spent** — the cost it describes is no longer paid by anyone, and its own prediction is why. #404's extraction moved the helpers to `apps/web/e2e/layout-box.ts` and took the case with them, so the subject of this entry is not "at the foot of the same file" any more: `command grep -rn "layoutBoxOf returns the box its poll observed" apps/web/e2e` returns `apps/web/e2e/layout-box.spec.ts:84` and nothing in `composition.spec.ts`. The new home has no file-level hook to inherit — `command grep -cE "beforeEach" apps/web/e2e/layout-box.spec.ts` is `0`, against `1` for `composition.spec.ts` as the positive control — and no case in it takes a `page` fixture at all (`command grep -cE "\{ page \}|\(\{ *page" apps/web/e2e/layout-box.spec.ts` is `0`). The `test.describe` re-indent this entry asked for is therefore moot rather than outstanding, and it was never taken: the extraction separated the two sets of cases by moving one out, which is what the re-indent was a way of approximating. Nothing of the entry survives on the other side either — every one of `composition.spec.ts`'s nine remaining cases takes `page`, so its hook no longer covers a case that does not want it. What the moved case still pays is the lane's one-per-run `webServer` build, which `layout-box.spec.ts`'s own docblock records; that is a per-run cost of the whole lane and not the per-case hook this entry was about, so it does not keep the entry open

## 2026-08-11 — A docs-only fence split one claim's truing across two PRs

- Where: the Attribution section of `docs/design/map-treatment.md` (the composed-row paragraphs) and the `retro-P4-attribution-compact-form-reading` entry in `docs/review-feedback.md`, against the code-comment carriers named on [#416](https://github.com/TomBennett-Lloyd/cumulo/issues/416) — `apps/web/src/map/map.css`'s attribution-band docblock, `apps/web/src/map/MapAttributionStrip.tsx`'s module docblock, and the `packages/ui/src/attribution/` pair
- What: P4 confirmed that the compact-form condition reads against the row _as composed_, which makes that condition the constraint's own wording rather than this repo's reading of it. `architecture.md` rules 9 and 11 want a claim's carriers moved in the change that moves the claim, and here they could not be: the PR edits `CLAUDE.md`, a `humanAlways` path, so it was fenced to docs-only and the code comments were spun out. Between this merge and #416's, `main` states the claim both ways — the design doc saying the composed row is the sentence's own object, the code comments still calling it a reading — and each half cites the other as its owner, so a reader landing on either is told the disagreement is authoritative. The root cause is not this PR's fencing decision, which was right for the gate: it is that the gated/ungated split cuts across carrier sets, so **any** `humanAlways` change whose claim also lives in code inherits the same window, and the window's length is whatever the follow-up ticket's queue position happens to be. Wanted: either a stated convention for what a fenced PR owes its carriers (a named follow-up plus a dated in-place note on the stale side, so the disagreement is legible as temporary), or a rule that the gated file's own true-up waits for the whole carrier set. Not fixed here, because deciding it is a workflow-gate change and this PR is the one that would be judging its own fence
- Source: PR for #415, review cycle 1
- Update (2026-08-11, #416): the window this entry describes is **closed** — the code-comment carriers are trued in the same PR as this note, so `main` no longer states the claim both ways. Measured rather than asserted: `command grep -rnE "reading of the (amended )?sentence|reading rather than as CLAUDE|row cannot hold the full phrase" apps/web/src packages/ui/src` now returns nothing, against 5 hits before. What the entry is **for** is untouched and still open: the window lasted one merge only because #416 happened to be queued next, and no convention yet says what a fenced PR owes its carriers. The measurement of the cost — one docs PR, one code PR, and however long the queue is between them — is now on the record rather than hypothetical

## 2026-08-11 — The evidence page's ownership rule is scoped to agent and skill files, so runbooks keep accreting divergent mechanisms

- Where: the `## Adding a member` section of `docs/standards/evidence.md`; the `command grep` rationale sentence in `infra/README.md`'s phase-B CSP readback step ("B4"-region, the paragraph beginning "`command grep` rather than plain `grep`"); the `grep -c` count-derivation block in the same file's plan-count section ("That 9 is arithmetic, not memory"); the mode-tag surgery comment above the fixed-string `-e` block in `.claude/scripts/check-infra-mirrors.test.sh`; and member 5's own closing citation on the evidence page ("#206 is the incident this rule was written from")
- What: `## Adding a member` promises that adding a member is "one entry on this page and zero edits anywhere else", but it buys that promise by scoping its carriers to "agent and skill files" — the only files it asks to cite the page by path and never restate a rationale. Files outside that scope restate mechanisms freely, and in the state this PR ships, two still do. Sweeping the repo for member 5's mechanism, with this log excluded because its entries describe the problem rather than carry it:

  ```
  $ command grep -rnE 'ugrep|under-?match' --include='*.md' --include='*.sh' --include='*.ts' --include='*.json' --include='*.yml' . | command grep -vE 'node_modules|^\./docs/tech-debt\.md'
  ./infra/README.md:1436:… a shell whose `grep` is shimmed to another implementation can under-match an anchored or case-insensitive pattern …
  ./.claude/scripts/check-infra-mirrors.test.sh:507:# the shimmed `grep` on this machine is ugrep, which under-matches alternations
  ./docs/standards/evidence.md:29:5. **Bare `grep` is ugrep on this machine, and it searches a narrower set of files than you asked for.** …
  ```

  Three statements, one of them the owner's. This PR deleted the two wordings that used to sit in `.claude/agents/implementer.md` and `.claude/agents/planner.md` — the files the section's scope does reach — and left untouched the two it cannot reach, both of which assert the mechanism this PR's fixture falsified: the shim's divergence is a narrowed file set on recursive searches, not a pattern-engine defect. That is the drift the section exists to prevent, surviving through the door it left open. One further carrier sits outside the repo and outside any sweep of it: GitHub issue #206, whose title still reads "Shimmed grep (ugrep) silently under-matches ERE alternations — false-clean risk for grep-based acceptance evidence" (checked with `gh issue view 206 --json title`), and which member 5 names as the incident the rule was written from — so a reader who follows the owner's own citation is handed the false mechanism by the owner. `infra/README.md` separately departs from **sanctioned form 2** in a runbook the page's scope does not reach: its plan-count block derives three counts as bare `grep -c … # 5` lines with the expected number in a trailing comment, where the sanctioned shape is `n=$(command grep -cE …)` read as a number. (Not member 2, as this entry first said — member 2's defect is an `&&` chain halted by a legitimate zero, and those lines chain nothing. The label was wrong; the finding is not.) So the page's ownership claim is true only of the files it names, while the rule it owns is repo-wide — and its one outbound citation points at a title that contradicts it. Fix direction: either widen the declared carrier set to every file that states a member's mechanism (runbooks and scripts included), or accept runbooks as application sites and add a mechanical check that no file outside the page states a mechanism at all — the second is what makes the promise verifiable rather than aspirational. Either way the citation needs settling too, by retitling #206 or annotating the reference, since a closed issue's title is the one carrier no sweep of this repo will ever return.

- Source: PR for #412/#409/#408 (branch `412-workflow-debt-batch`), review cycle 1

## 2026-08-11 — Redirect rows for entries captured by their own PR are born dead

- Where: the `## Captured-entry redirects` table in `docs/tech-debt.md` — its Maintenance paragraph ("a row dies with its issue"), the row for `git diff --check` is staging-sensitive… (#412), and the `browser-smoke.md` obliges the agent to emit findings… row (#413) above it
- What: the table's stated purpose is to catch dangling pointers — source comments that cite a deleted entry by title and cannot be edited without turning a docs PR into a source one — and its maintenance rule is that whoever closes the capturing issue deletes the row. A row added by the same PR that closes its capturing issue satisfies neither half: the #412 row was born with its issue already closing, and no file in the repo cites that entry by title, so there is no pointer for it to catch and no later close to remove it. The #413 row shows the failure's other end — #413 is closed and its row is still here — so the table accretes rows that were never load-bearing and are removed by nobody. Fix direction: skip the redirect row entirely when an entry is captured by its own PR (the deletion and the capture are in one diff, so nothing can dangle), or state that exemption in the Maintenance paragraph so the next capture does not re-derive the question; either way, sweep for citations by title before adding a row, since a row with no citing site is the definition of one that cannot pay for itself.
- Source: PR for #412/#409/#408 (branch `412-workflow-debt-batch`), review cycle 1

## 2026-08-11 — Two files still say the prose half owns what the review-feedback entry must say, and the prose half no longer does

- Where: two files, four statements. In `.claude/workflow.json`, the `mergeRitualGate` key's closing clause ("which is why the prose half above still owns what the entry must say") and the `humanAlwaysRule` key's parenthetical ("mergeRitual owns what it must say"); in `.github/workflows/ci.yml`'s `merge-ritual-gate` comment block, its opening rationale ("What the entry has to _say_ is merge.mergeRitual in .claude/workflow.json, and is deliberately not restated here") and the later numbered item ending "merge.mergeRitual, and not this job, owns what the entry must say"; against the `mergeRitual` key all four point at, and `docs/review-feedback.md`'s `## Entry format` section
- What: #409 rewrote `mergeRitual` so that it explicitly restates neither half of the entry's content — its new text hands both the pre-label form and the merge-time Category and Verdict to `docs/review-feedback.md`'s `## Entry format` section and says "so this key restates neither". Every statement that cites `mergeRitual` as the owner of the entry's content was left saying what was true before that rewrite, so each now points a reader at a key that immediately redirects again, and the mechanical gate's own rationale for being "a floor, not a proof" rests on a claim its cited authority disclaims. This entry's own first draft named only two of those statements — one per file — and rested its reasoning on that count; the sweep that trued it found four. An entry about restatement carriers that misses half of them is the failure it describes, and the miss is recorded here rather than quietly corrected, because it is also the evidence for the fix direction: the carriers share no distinctive literal (one says "owns what it must say", another "deliberately not restated here"), so nothing short of reading both files finds them all. The count is what makes this systemic rather than a one-line fix: the statements are near-identical by design (the CI comment exists so the job is legible without opening `workflow.json`), so truing any subset desynchronises it from the rest, and `.github/workflows/ci.yml` was fenced from edits on this PR — a `.yml` change also reclassifies the diff's blast radius. Fix direction: in one change, point all four at `docs/review-feedback.md`'s `## Entry format` as the owner of the entry's content, leaving `mergeRitual` as the owner of the ritual's ordering, which is what it still genuinely holds; and treat this list as a floor, re-sweeping both files rather than trusting it.
- Source: PR for #412/#409/#408 (branch `412-workflow-debt-batch`), review cycle 2

## 2026-08-11 — The merge gate has three prose carriers and no ledger naming them

- Where: `merge.reviewedSourceRule` in `.claude/workflow.json` (the owner); the `### Where the phases sit relative to PR review` section of `infra/README.md`; the merge-policy bullet in `CLAUDE.md`'s Workflow section; against rule 9 of `docs/standards/architecture.md`
- What: #408 made `infra/README.md` state the merge policy once for the whole file, with every other stop-here step and phase-split paragraph pointing back at that section by anchor (ten such links) rather than restating it — a real improvement within the file, and the right shape for it. But the section does not only cite `merge.reviewedSourceRule`: it restates the rule's substance in its own words ("it merges on green CI plus a review-loop APPROVE, with no labelling step and no separate human sign-off"), which makes the file a third carrier alongside `CLAUDE.md`'s merge-policy bullet. Rule 9 asks that a rule with carriers list them beside the owner so a move is a lookup rather than a search; `.claude/workflow.json` has no such ledger for any `merge.*` key. The gate has already moved twice (`debtBurnRule` 2026-07-30, `reviewedSourceRule` 2026-08-01) and each move had to find its carriers by sweeping — and a sweep for this one is unusually weak, because the carriers share no distinctive literal: `infra/README.md` phrases it as "no separate human sign-off" and `CLAUDE.md` as "auto-merge on green CI plus a review-loop APPROVE", so nothing short of reading both finds them. Fix direction: a carrier list beside the rule in `workflow.json` (the shape `review-loop`'s own restatement ledger uses, declared a floor and naming its sweep), or reduce `infra/README.md`'s sentence to a pure pointer that names the key and states nothing about it.
- Source: PR for #412/#409/#408 (branch `412-workflow-debt-batch`), review cycle 2

## 2026-08-11 — The evidence page owns a class whose operative literals stay in its carrier files, and it names none of them

- Where: the `## Adding a member` section of `docs/standards/evidence.md` (the owner, and where the ledger belongs); its five carriers — `.claude/agents/implementer.md` (the acceptance-evidence rule spelling `command grep -E`, and the positive-control sentence beside it, twice over), `.claude/agents/planner.md` (the same pair in its acceptance-criteria guidance), `.claude/agents/task-orchestrator.md` (the report-hygiene item's `cmd > out 2>&1; rc=$?`), `.claude/skills/review-loop/SKILL.md` (step 5's ERE marker pattern, the same capture form, and the `git diff HEAD --check` pairing) and `.claude/skills/execute/SKILL.md` (the after-each-wave rule's captured exit code, "never a grep over the output"); a sixth found by this PR's confirmation pass after the five above were written, which is the floor doing its job — `.claude/agents/reviewer.md`'s sweep instruction restates sanctioned form 5 (the positive control) and cites the page nowhere at all, so it is a carrier that is also missing the citation `## Adding a member` asks for; against rule 9 of `docs/standards/architecture.md`, and the "Restatement ledger for this bound" paragraph in the same `review-loop` skill as the shape to copy
- What: the page's `## Adding a member` section promises that adding a member is "one entry on this page and zero edits anywhere else", and buys it by requiring agent and skill files to cite the page by path and never restate a member's _rationale_. The operative _forms_ are deliberately left in those files — an agent has to know what to type without the hop, which is the right call and is why this is a ledger gap rather than a duplication bug. But rule 9's second half is that a value with restatement carriers lists them beside the owner, so a change is a lookup rather than a search, and the page carries no such list. Changing a sanctioned form therefore still means sweeping the repo for its copies. The sweep is unusually weak for the same reason the merge-gate entry above is: the carriers share no single literal. `command grep -rlE 'command grep|rc=\$\?|n=\$\(command grep' .claude docs --include='*.md' --include='*.json'` returns four of the five and misses `.claude/skills/execute/SKILL.md`, whose restatement is the exit-code form (`pnpm verify; echo $?`) and matches none of those arms — so the list above is a floor established by reading the files, not by one pattern — and the confirmation pass promptly found a sixth, which is what a floor is for. The same sweep also returns `docs/design/task-orchestrator.md`'s report-hygiene item, which restates the capture form verbatim but is a **frozen** design record ("the body below is preserved exactly as it was reviewed"): it is a carrier that by convention must never be trued, and a ledger that does not say so invites a future editor to true it. This PR's cycle-1 entry covers the scope of the page's _ownership rule_ — which files may restate a rationale — and nothing covers the missing carrier list. Fix direction: add the carrier list to `## Adding a member`, declared a floor with the sweep that produced it and with the frozen record named as excluded, so the next change to a sanctioned form is a lookup.
- Source: PR for #412/#409/#408 (branch `412-workflow-debt-batch`), review cycle 3

## 2026-08-11 — The infra runbooks gate Phase B on the merge alone, and the account-verification gate is stated nowhere an operator reading them will see it

- Where: `### Where the phases sit relative to PR review` and the seven stop-here steps in `infra/README.md` that point at it; against the constraint's only two statements, the Live demo line in the root `README.md` and the header comment of `.github/workflows/deploy-pages.yml`
- What: #408's owner decision retired the policy gate on applies and explicitly kept the account gate — applies stay blocked on AWS account verification until it clears — but that constraint is written down in exactly two places, neither of which is the runbook. Sweeping the repo for it returns `README.md`'s Live demo line (#144's CloudFront distribution "waits on AWS account verification") and `deploy-pages.yml`'s header ("account is awaiting verification"); `infra/README.md` does not mention it at all. Before #408 the runbooks' "**PR review** — human, on the PR" step incidentally held every apply behind a person who knew, so the omission cost nothing; now Phase B's stated precondition is the merge and nothing else, and an operator who follows the runbook end to end is told to apply. The gate is pre-existing rather than introduced — the runbooks never stated it — and stating it was out of scope for a ticket whose subject was reconciling stale prose, which is why this is logged rather than taken. Fix direction: one sentence in the "Where the phases sit relative to PR review" section pointing at the account gate as a second, still-live precondition on Phase B, worded as a pointer to whichever of the two existing statements is made the owner, so this does not become a fourth carrier of the same constraint.
- Source: PR for #412/#409/#408 (branch `412-workflow-debt-batch`), review cycle 2

## 2026-08-11 — The workspace globs are restated by every package census and checked by none

- Where: the owner is the `packages:` block of `pnpm-workspace.yaml`. Carriers, a **floor** established by this sweep over tracked files on 2026-08-11 — `command grep -nE "'apps', *'packages'|\"apps\", *\"packages\"|apps/\*.*packages/\*|^ *- *'?apps/"` — and not by any single literal, since the carriers spell the pair differently (a JS array literal in the gates, backticked globs in the prose, a YAML sequence in the owner): the `GROUPS` constant in the census program of `.claude/scripts/check-node-types.sh` (plus the prose restating it in that file's own scope-limit list), the identically-shaped `GROUPS` in `.claude/scripts/check-aws-test-guard.sh`'s census program (plus the comment in `check-aws-test-guard.test.sh` describing that gate's "one restated value"), the Layout paragraph of `CLAUDE.md`, and rule 1 of `docs/standards/architecture.md`, whose statement of dependency direction treats the same groups as the whole workspace. The same sweep returns families that are **not** carriers, named so the next reader does not re-adjudicate them: the fixture manifests inside `check-supply-chain-policy.test.sh` (a fixture's workspace is its own world) and the `apps/<name>/**` path filters in the deploy workflows (per-package paths, not the group set). Against rules 8 and 9 of `docs/standards/architecture.md`
- What: the censuses named above enumerate workspace packages as "the root manifest plus every `apps/*` and `packages/*` directory holding a package.json", restating the workspace globs in a shell/JS array rather than reading them, on the stated ground that reading them would mean parsing YAML. Each header says it is a restatement, which is honest, and none of them is checked, which is the debt. Add a top-level group — a `services/` or a `tools/` — and each census silently narrows: every gate keeps passing, over less, while the prose carriers keep describing a workspace that no longer matches. No census's own death-guard closes this. `check-aws-test-guard.sh` has a partial backstop (its refusal on a `workspace:` edge naming a package the scan never found) that fires only when a scanned package happens to declare a cross-group workspace dependency; `check-node-types.sh` has none, and its census-death guard cannot fire while the manifests already inside `apps/` and `packages/` keep the declaration count non-zero — the failure is a census that shrank, not one that emptied, and only the second is detectable from inside. Rule 8 is the mechanism for exactly this class (a value mirrored across files, declared to a gate), but `check-infra-mirrors.sh` has no mode whose non-Terraform side is anything but a TypeScript `export const`, so the YAML-list-versus-array-literal pair cannot be declared to it today. Fix direction: one workspace-glob source the gate family shares — a small script or generated list every census reads — so a new group is one edit rather than a silent narrowing in each gate independently; that leaves the prose carriers, which no mechanism reaches, so the same change should put the carrier list beside the owner in `pnpm-workspace.yaml`. Note for whoever takes it: `CLAUDE.md` is a `humanAlways` path, so truing that carrier rides an owner-read PR rather than an auto-merged one.
- Source: PR for #407, review cycles 1-2

## 2026-08-11 — Each ADR's Amendments intro summarises the convention more narrowly than the convention now reads

- Where: the opening line of the `## Amendments` section in `docs/adr/0002-storage-split.md`, `docs/adr/0004-ingestion-transport.md`, `docs/adr/0005-fleet-api-hosting.md` and `docs/adr/0006-demo-abuse-protection.md`; against the `## Amendments` section of `docs/adr/README.md` as this branch rewrites it, and rule 9 of `docs/standards/architecture.md`. `docs/adr/0007-series-deletion-is-ttl-only.md` held an instance of the same shape, but it was authored by this batch and is fixed in this cycle — its intro now reads "follows the convention owned by `docs/adr/README.md` § Amendments; read it there rather than from a summary here", which is the shape the entry asks for and is why 0007 is named here as the exemplar rather than as a debt
- What: each of the four opens by summarising the convention as amendments truing up stated values that have legitimately moved, with the decision and rationale immutable (0002 phrases the citation as a bare "Per `README.md`", which resolves to the wrong document from a reader's point of view as well as summarising it). After this batch that sentence describes only the **carrier** case. The owner now turns on a carrier-versus-premise test: a premise is deliberately _not_ trued inline, because truing it would rewrite the document into a reasoning it never had, so the dated entry alone records the move and the reader is protected by an as-it-stood annotation instead — and where an entry has declared a family of body figures historical wholesale, that declaration _is_ the inline half. Rule 9 then adds a further obligation the intros do not hint at: an ADR-owned value's restatement ledger lives in that ADR's `## Amendments` section. So each intro tells a reader that inline truing is what amendment means, which is the one thing the owner says a premise must not get, and none of them carries a pointer that would make the divergence visible — a reader who trusts the summary never reaches `docs/adr/README.md` to discover it has moved underneath them. The root cause is structural rather than a wording slip: a per-document restatement of a document-level convention is rule 9's own subject, and the summaries drift every time the convention gains a case, which it has now done twice. Fix direction: give each intro 0007's form — a pointer at `docs/adr/README.md` § Amendments carrying no summary at all — so the convention has one statement and the ADRs have none. Note for whoever takes it: ADRs are a `humanAlways` path, so this rides an owner-read PR.
- Source: #401 batch, review cycle 1

## 2026-08-11 — The ledger-banking obligation landed in the rule and the agent contracts but not in the skills that execute them

- Where: the plan-validation bullet in `.claude/skills/plan-issue/SKILL.md` (the "a changed rule, value or procedure names every other site that states it" item) and step 2's FIX-NOW/SYSTEMIC partition in `.claude/skills/review-loop/SKILL.md`; against rule 9 of `docs/standards/architecture.md` and the sentences this batch added to `.claude/agents/planner.md` ("When the sweep finds a second carrier of a value that has no ledger, the plan writes the enumeration into the owner's restatement ledger … not only into the plan text") and `.claude/agents/reviewer.md` ("A completed sweep is banked into the owner's restatement ledger … in the fix round, not only into the finding — the ledger is where the next plan reads it")
- What: rule 9 now states when a ledger comes into existence — a value acquires one the first time a second site is found carrying it, and the finder writes it, so a review's claim sweep ends in the owner's ledger rather than only in the finding. That obligation reached the rule and both agent contracts in this batch. It did not reach the two skills that actually execute those procedures. `review-loop`'s step 2 still routes a cycle's output to exactly two destinations — FIX-NOW to an implementer dispatch, SYSTEMIC to this log — and neither branch mentions the owner's ledger, so a sweep that discovers a second carrier can be fully discharged by the skill's own instructions while the ledger the next plan will read stays unwritten. `plan-issue`'s bullet stops at naming the sites and at the evidence standard for the sweep claim, which is the half that catches a false sweep, not the half that banks a true one. The gap is easy to miss precisely because `review-loop`'s own file demonstrates the form it omits: the skill carries a model restatement ledger for its 3-cycle bound, declared a floor and naming its sweep, so the shape is on the page while the procedure never asks for it. Consequence: whether the obligation is honoured depends on which contract the executing agent happens to have loaded, and the failure is silent in the usual direction — nothing is red, the finding is closed, and the next sweep for that value starts from nothing again, which is the loop rule 9 cites #356 for. Logged rather than taken because the fix is a judgement about where procedure belongs: the skills could carry the banking step directly, or could cite the agents' contracts as owning it, and the same question governs every other duplicated instruction across the skill/agent boundary — so it wants deciding once, for the pair, rather than patched here.
- Source: #401 batch, review cycle 1

## 2026-08-11 — The Node major is restated in spellings no single sweep reaches, and gated in none of them

- Where: the owner is `.nvmrc`. Carriers, a **floor** — the first sweep for this entry was keyed to the deployed-runtime spelling and found only the Terraform arm; the widened sweep `command grep -inE 'nodejs[0-9]+\.x|node[0-9]{2}|node-version|"node": *"|target=node'` over tracked files on 2026-08-11 returned the rest, which is the evidence for treating the list as a floor rather than a census: the `runtime` attribute of the Lambda resource in each of `infra/forecast/lambda.tf`, `infra/ingestion/lambda.tf` and `infra/api/lambda.tf` (`nodejs22.x`, each with a comment above it citing `.nvmrc`); the `--target=node22` flag in the `build` script of `apps/api/package.json`, `apps/ingestion/package.json` and `apps/forecast/package.json`; and `engines.node` in the root `package.json` (`">=22"`, a bound rather than a pin, so it goes stale by permitting rather than by contradicting — the quietest of them). The same sweep returns the `node-version-file: .nvmrc` steps across the CI and deploy workflows, which are **not** carriers but the shape the carriers should have: they read the owner instead of copying it. The `cumulo-api-dev` function bullet under `## Decision` in `docs/adr/0005-fleet-api-hosting.md` names `nodejs22.x` too, and belongs in the floor above as a carrier rather than beside it as an exception: it asserts a value it says in the same breath is owned elsewhere ("owned by #14's Terraform"), which is the backwards case — an ADR quoting mutable code is listed in the code owner's ledger like any other carrier (rule 9; `docs/adr/README.md` § Amendments, where the reasoning lives and is not restated here). Immutability changes only what it is owed: not an inline true-up but an as-it-stood annotation on the quotation plus its own dated Amendments entry, dispatched from the ledger this entry asks for. Against `MIRRORS` in `.claude/scripts/check-infra-mirrors.sh` and rule 8 of `docs/standards/architecture.md`
- What: `.nvmrc` owns the Node major (this PR's `check-node-types.sh` makes that ownership mechanical for the `@types/node` family, and for that family only). Rule 8 is explicit that citing the other file in a comment is not enough, because comments do not fail builds — yet every carrier above is comment-only or silent, and none is in `MIRRORS`. The drift is silent in the dangerous direction: bump `.nvmrc` and CI tests on the new major while the deployed functions execute on the old one and esbuild keeps down-levelling output to the old one, with the comments still asserting they agree. Declaring the pairs is not currently possible, which is why this is logged rather than done: every `MIRRORS` mode pairs Terraform with a TypeScript `export const` (`eq`, `ts-lt`, `str-eq`, `regex-eq`) or with a second Terraform file (`tf-ge`), so a plain-text owner cannot be either side of a record — and the esbuild and `engines` carriers are not Terraform at all, so no amount of new mirror modes reaches them. That is what makes this a class rather than a per-function record: the spellings (`nodejs22.x`, `--target=node22`, `">=22"`, and the owner's own bare token) share no literal, so each carrier is invisible to a sweep written for any of the others — which is how the first version of this entry came to name only the Terraform arm. Fix direction: `.nvmrc` is already the single owner, so what is missing is the carrier ledger rule 9 asks for — and it cannot live beside the owner, because a bare token is that file's whole format and a comment added to it would break every reader it has, this PR's gate included. The ledger therefore needs a named home elsewhere that each carrier cites, listing them in every spelling they use, backed by whatever reaches each arm: a mirror mode whose non-Terraform side is a plain-text file read as a single token (the same one-token read `check-node-types.sh` already performs) for the Terraform arm, and a gate or a build-time derivation for the esbuild targets, which would otherwise be left exactly as stale as they are now.
- Source: PR for #407, review cycles 1-2

## 2026-08-11 — `contiguousRuns` splits on array adjacency, not on time adjacency

- Where: `contiguousRuns` in `apps/web/src/charts/chart-series.ts`. Its callers: the band, median and actuals run predicates in `ForecastChart.tsx` (`bandRuns`, `medianRuns`, `actualRuns`), which feed `bandElements`, `boundElements`, `actualsElements` and `medianElements` in `forecast-chart-marks.tsx`; `overlayElements` in the same file, which derives its own runs; and `nightElements` in `forecast-chart-context.tsx`, which since this branch is the one exemption. The mapping that gives the defect its width is `sampleXs` in `chart-geometry.ts`; the producer of the shape it mishandles is `joinFleetSeries` in `apps/web/src/dashboard/fleet-series.ts`, over `aggregateFleetForecast` in `packages/shared/src/aggregation.ts`
- What: the run predicate breaks a mark at an index it rejects, which covers an hour that is _present_ in the series carrying `null` — and cannot cover an hour that is **absent from the series entirely**. `joinFleetSeries` builds its x-domain as the union of forecast hours and actual hours, so an hour neither forecast nor measured has no row at all; its two neighbours stay adjacent in the array, no run is broken, and `curvedLinePath` draws one continuous curve through the hole. Measured rather than reasoned: a five-sample series over 00:00–05:00 with 03:00 absent renders **one** `.forecast-chart-median` path carrying **five** anchors, and one `.forecast-chart-actuals` path. This was harmless while the axis was index-spaced, because the elided hour had no width; #325 made placement time-proportional, so the hole now has its full width and the bridge is drawn across it in plain sight. That is `design.md` rule 5's "never bridge" failing on exactly the absence rule 5's own "full width on the axis" clause just made visible. Suspected fix: a run predicate that also breaks where the interval between consecutive samples exceeds the series' modal step — the derivation `modalStepMs` in `forecast-chart-context.tsx` already carries, which would move up to `chart-series.ts` and be applied once for every mark. Systemic rather than fix-now because every mark on the canvas keys off this one function and the change is a behaviour change to all of them at once; the night wash was fixed in place instead, because a wash spanning the hole asserts darkness at an hour whose classification is unknown, which is a false claim rather than a missing break. The prose that used to claim otherwise — in `chart-geometry.ts`'s `sampleXs`, `chart-series.ts`'s module docblock and `contiguousRuns`, `forecast-chart-marks.tsx`'s `medianElements`, and the "Settled, then reversed" section of `docs/design/chart-treatment.md` — now points here by this entry's title
- Source: #325, in the `323-geometry-batch` review cycle 1

## 2026-08-11 — `fleetCentroid` and `fleetCapacityKw` are pure-core exports with no production consumer

- Where: `fleetCapacityKw` and `fleetCentroid` in `packages/shared/src/aggregation.ts`, both re-exported from `packages/shared/src/index.ts`; their suites `aggregation.test.ts` (the `fleetCapacityKw` describe) and `fleet-centroid.test.ts`
- What: neither function is called anywhere outside `packages/shared`'s own tests. `fleetCapacityKw` lost its last one in this branch's #323 commit, which deleted `fleetStatsLine` from `apps/web/src/dashboard/fleet-panel-copy.ts`; `fleetCentroid` has never had one — `git log -S 'fleetCentroid(' -- apps/` is empty. Each is therefore kept alive by its own test and the barrel, which is the shape where a test proves an exported function still works and nothing proves anybody wants it. Not a deletion recommendation: [#419](https://github.com/TomBennett-Lloyd/cumulo/issues/419) is already open on `fleetCapacityKw`, asking the owner whether the fleet's installed capacity should have a home on the dashboard, and the answer decides whether the function is dead or merely early. The reason this is logged beside that issue rather than folded into it is that it is now a **two-instance pattern**: a second pure-core aggregate in the same module, with the same zero-consumer shape and no issue of its own, and `fleetCentroid`'s docblock spends a paragraph on a limitation (plane rather than spherical arithmetic) that no caller has ever been able to hit. Whatever #419 settles for the capacity sum should settle the centroid the same way, or say why not
- Source: #323, in the `323-geometry-batch` review cycle 1

## 2026-08-11 — The css-contract suites assert declaration text where they mean rendered geometry

- Where: `strokeWidthOf` and the `DASH` regex in `apps/web/src/charts/chart-css-contract.test.ts`; the same idiom, via its own `declarationsFor`, in `apps/web/src/map/map-css-contract.test.ts` (the `toContain('min-width: var(--space-6);')` family)
- What: these suites parse the stylesheet and assert on the **raw declaration string**, then draw conclusions about how the page renders. `strokeWidthOf` returns the trimmed text after `stroke-width:`, so `expect(dayBoundary).not.toBe(crosshair)` is satisfied by `1` against `1px` — two spellings SVG renders identically — and would report a contract that does not exist. The `DASH` regex has the mirror shape: it matches the presence of a `stroke-dasharray` declaration, so `stroke-dasharray: 0` would satisfy "marks the measurement seam with a dashed rule" while painting a solid line. The map suite's exact-string `toContain` is the same idiom pointed at lengths: a semantically identical respelling breaks it, and a different token with the same computed value passes it. **The contract is load-bearing and correct today** — `charts.css` declares `1` for the grid, horizon and day boundary and `2` for the crosshair, so the assertion bites, and the crosshair's non-grid ink is pinned separately — which is why this is an idiom to move rather than a bug to fix. Root cause: jsdom does not compute style for SVG presentation attributes, so a suite wanting rendered geometry has only the text available to it; the honest routing is that "these two marks look different" is a browser-lane criterion (`testing.md` rule 10) and what these suites can own is "this declaration exists and says this". Fix direction: normalise before comparing (parse the length, compare value and unit) so a respelling cannot pass for a difference, and say in each file which half of the claim it is actually making
- Source: #335, in the `323-geometry-batch` review cycle 1

## 2026-08-11 — The only producer of `ForecastChartPoint.night` collapses the very distinction its optionality is justified by

- Where: the `night` field's docblock on `ForecastChartPoint` in `apps/web/src/charts/chart-series.ts` (the claim); the empty-fleet arm of `fleetNightClassifier` in `apps/web/src/dashboard/fleet-night.ts` and `fleetChartAggregate` in `apps/web/src/dashboard/fleet-series.ts` (the producer); the `fleetChartAggregate` case "marks nothing at all for a fleet with no sites, whatever the hour" in `apps/web/src/dashboard/fleet-series.test.ts` (which pins the collapsed value); `nightElements`'s `=== true` predicate in `apps/web/src/charts/forecast-chart-context.tsx` (which makes the two indistinguishable to the renderer)
- What: the field is optional rather than a `boolean`, and the docblock justifies that on a distinction — "`false` is a caller that worked out this hour is daylight, and absence is a caller that did not answer the question at all". The one production producer collapses it in exactly the case the distinction exists for. A fleet with no sites takes `fleetNightClassifier`'s `() => false` arm, and `fleetChartAggregate` then spreads `night: isNight(point.validTimeIso)` over every point, so a caller that consulted nobody writes the "asked, and it is daylight" value onto the whole series; the test above pins `[false, false]` for that fleet. Nothing observable is wrong — `nightElements` tests `=== true`, so both spellings draw nothing — which is why this is a claim/behaviour mismatch rather than a bug: the type keeps a distinction alive that no producer honours, and a future third rendering behaviour would find the empty fleet already on the wrong side of it. Fix is one of two, and both are small: have the empty-fleet path omit the key (a second arm in `fleetChartAggregate`, since a classifier returning `boolean` cannot express absence), or retire the claim and make the field a plain `boolean`. It crosses `chart-series.ts`, `fleet-night.ts`, `fleet-series.ts` and their suites, and picking between the two is a design decision about what the field means — which is why it is logged rather than taken
- Source: #335, in the `323-geometry-batch` review cycle 3; **demoted at the review cap** (cycle 3 is the last, and this is the finding that was not taken)

## 2026-08-11 — The night layer's "cannot contradict the curve" guarantee holds for physics-derived series and the shipped demo is not one

- Where: the guarantee's carriers — the module docblock and the `isNightAt` docblock in `apps/web/src/dashboard/fleet-night.ts`, the layer rationale in `apps/web/src/charts/forecast-chart-context.tsx`, the Context layers subsection of `docs/design/chart-treatment.md`, and the #335 commit message's "can never contradict the series it explains"; against the two synthetic producers, `demoFleetDataSource`'s half-sine in `apps/web/src/data/demo-fleet-data-source.ts` (keyed to its own `SUNRISE_HOUR_UTC` / `DAYLIGHT_HOURS` constants) and the fixture generator in `apps/web/src/data/fixture-series.ts`
- What: #335 argues that the wash cannot shade a column whose power is non-zero, because night is the intersection of every site's darkness and each hour is judged by the sun that produced its power. That reasoning is sound for series the physics chain produced, and the demo the app actually ships is not one: the half-sine synthesises output from a fixed sunrise hour and daylight length at every longitude, by its own docblock "Not physics". Measured at midwinter, the classifier calls 00–07 and 18–23 night while the demo curve at the 07:00 sample is still about 26% of peak — so the wash's edge sits on a visibly non-zero column, which is exactly the contradiction the argument says is impossible. It is pre-existing rather than introduced by the hour-midpoint correction (that hour was classified night before it too), and nothing is wrong for real forecasts. The defect is that an unconditional guarantee is stated in five places while being conditional on the data's provenance. Convergent fix, and the reason this is one entry rather than five edits: scope the guarantee once at its owner — it holds for physics-derived series — and have the other carriers point there rather than restate it, so the next synthetic producer cannot silently falsify four copies. The carrier list above is a **floor**, from sweeping `zenith|civil twilight|under-claim|contradict` over `apps/web/src` and `docs`
- Source: #335, in the `323-geometry-batch` confirmation pass after the review cap

## 2026-08-11 — `HOUR_MIDPOINT_OFFSET_MS`'s new docblock enumerates its callers, where rule 9 wants a floor and a sweep

- Where: the `HOUR_MIDPOINT_OFFSET_MS` docblock in `packages/forecast/src/physics-forecast.ts`, which names the chain and "the web app's night classifier … the second caller"; against the third site that restates the value, the `HALF_HOUR_MS` literal in `packages/forecast/src/physics-forecast.test.ts`
- What: exporting the constant so `apps/web/src/dashboard/fleet-night.ts` could import it rather than respell 30 minutes was the right move and keeps one owner for the value. The docblock then went one step further and counted its importers, which is a completeness claim of the kind `docs/standards/architecture.md` rule 9 exists to make unnecessary — and it is already one short, because the owning package's own test restates the literal to assert the chain's evaluation instant. Nothing fails silently: that test reddens if the constant moves, which is why this is enumeration hygiene rather than a defect. The durable shape is a ledger that declares itself a floor and names the sweep that produced it — `command grep -nE '30 ?\* ?60 ?\* ?1000|1_800_000|1800000|HALF_HOUR|HOUR_MIDPOINT'` returns the owner, the test literal, and nothing else across `apps` and `packages` — rather than a count the next caller falsifies
- Source: #335, in the `323-geometry-batch` confirmation pass after the review cap

## 2026-08-11 — One of #335's three quoted night figures is carried by prose alone

- Where: the "band is non-empty year-round" paragraph of the #335 commit message on branch `323-geometry-batch`, against the premise cases in `apps/web/src/dashboard/fleet-night.test.ts`
- What: the message quotes 258 dark-everywhere minutes at midsummer, 858 at midwinter and a 118-minute June rolloff, and says the tests assert floors beneath those figures rather than the figures themselves. True for two of the three: 258 sits under a `toBeGreaterThan(120)` and 118 under a `> 60` at both solstices. The midwinter 858 has no floor at all — the only midwinter assertion bounds the everywhere-window from _above_, via the rolloff subtraction, so a change that shrank the midwinter band would not redden anything. The figure is correct today (reproduced by minute sweep over the real fleet in two independent passes) and the coverage the paragraph claims for its _purpose_ is real, so nothing here blocks. Same family as the existing entry on docblocks reasoning in counterfactual worked numbers (#405), and the same convergent answer: either drop the derived figure or name the floor each one sits on, since a figure with no gate is a claim that decays silently
- Source: #335, in the `323-geometry-batch` confirmation pass after the review cap

## 2026-08-11 — The favicon's dark arm answers a different question than the app's theme toggle

- Where: the `@media (prefers-color-scheme: dark)` block in `apps/web/public/favicon.svg`, against `resolveInitialTheme` in `apps/web/src/theme.ts` (and its wiring in `use-theme.ts`), whose resolved value is written as the `data-theme` attribute the dark block of `packages/ui/src/tokens/tokens.css` selects on. Against `architecture.md` rule 9 — two mechanisms answering "is this dark?" with no owner naming the divergence
- What: the app resolves its theme as _explicit persisted choice beats system hint beats light default_ — `theme.ts` says so, and the stored key is `cumulo-theme`. The favicon can only ever see the system hint: it is fetched as a standalone document with no access to `localStorage`, so `prefers-color-scheme` is the only signal available to it. The two agree exactly when the visitor has never touched the toggle. A visitor whose OS is dark but who explicitly chose light gets a light app with a dark-arm tab glyph, and the reverse for the opposite pair — the case the toggle exists to serve is precisely the case the two mechanisms disagree on. Not a defect with a fix available: no mechanism lets a standalone SVG document read the app's persisted choice, which is why this is logged rather than taken. What is genuinely missing is ownership — neither `theme.ts` nor the favicon's header states that a second, dumber theme signal exists, so the next person to touch either will find the divergence by noticing a wrong-coloured tab. Fix direction: state it beside `resolveInitialTheme`, which owns the resolution rule, and accept the divergence explicitly rather than leaving it to be rediscovered; if it ever has to actually agree, the only route is a script that rewrites the icon `link` at runtime from the resolved theme, which trades a static asset for shell JS and is a real cost to weigh rather than an obvious win
- Source: PR for #282 review cycle 1

## 2026-08-11 — No mechanical check reads `favicon.svg`, so its four token restatements are watched only by prose

- Where: `apps/web/public/favicon.svg`, against the restatement ledger beside `--color-accent` / `--color-text-muted` in `packages/ui/src/tokens/tokens.css`, the `lint:css` and `lint:js` scripts in the root `package.json`, and the head contract in `apps/web/e2e/branding-head.spec.ts`
- What: the file restates four token hex values because a standalone SVG document cannot resolve `var()` — legitimate under rule 9, and ledgered. What is missing is any gate over the carrier. `lint:css` runs stylelint over CSS files only, so the `style` element inside an `.svg` is invisible to the colour half of the frontend gate that would otherwise judge exactly these `fill` declarations; eslint does not read SVG; and the e2e spec asserts only that the icon URLs serve with the right content-type, never what colour they paint or that the values still match the tokens. So the ledger entry and the file's own header comment are the entire control, in both directions, and comments do not fail builds — the same objection `architecture.md` rule 8 raises against comment-only infrastructure mirrors, in a case rule 8's gate cannot cover because neither side is Terraform. Drift is silent and low-salience: a changed accent leaves a stale tab glyph, which nobody is looking at. Fix direction: the cheapest real check is a unit test in `@cumulo/ui` that parses the committed SVG and asserts its four `fill` values equal the four token values it reads from `tokens.css` — the same file both sides already depend on — which would convert this from prose into the gate rule 9 says prose cannot be. #273 (the open owner decision on the brand blue) is the likeliest trigger for the drift this would catch
- Source: PR for #282 review cycle 1

## 2026-08-11 — Whether the deployed icon links resolve is proven by a one-off grep, not by a standing check

- Where: the `webServer.command` in `apps/web/e2e/playwright.config.ts` (`vite build && vite preview`, no base flag) against the "Build web (production, Pages base path)" step of `.github/workflows/deploy-pages.yml` (`vite build --base=/cumulo/`), covering the icon `link` elements in `apps/web/index.html`; `check-web-bundle.sh` is the nearest standing gate and is base-independent by design
- What: the icon hrefs are root-absolute (`/favicon.svg`, `/brand-512.png`) and depend on Vite base-rewriting them to `/cumulo/…` for the interim Pages deploy. That rewriting is the single thing standing between the change and a live 404 on every icon, and nothing checks it on an ongoing basis: the browser lane builds and previews at the default base `/`, where the un-rewritten hrefs are correct by construction, so the lane is structurally incapable of catching a base-rewriting regression — it would stay green while the deployed site lost its favicon. The only evidence the rewriting works is a grep run once, by hand, as plan acceptance on this PR. The same gap covers the `og:image` tag from the other side: it must _not_ be rewritten, and is spared only because Vite excludes external absolute URLs from an asset table that does include `og:image` content — a mechanism verified once by building a deliberate mutation, and equally unwatched thereafter. Note the asymmetry that makes this worth logging rather than shrugging at: the failure appears only in the artefact the deploy workflow builds, which no test ever loads. Fix direction: either run the browser lane a second time against a `--base=/cumulo/` build (expensive for one assertion), or add a cheap assertion to `check-web-bundle.sh`, which already inspects the built entry document and already runs inside the Pages workflow against the exact artefact being shipped — it is the one existing gate positioned to see the base-rewritten form
- Source: PR for #282 review cycle 1

## 2026-08-11 — Restatement-ledger sweep recipes claim tracked-only scope while recording a command that searches everything

- Where: the recorded sweep in the `Where:` line of each of the two entries above dated 2026-08-11 — the one owned by the `packages:` block of `pnpm-workspace.yaml`, and the one owned by `.nvmrc` — plus the convention they set for the next ledger that copies their shape. Against catalogue member 5 of `docs/standards/evidence.md` (that doc numbers two lists, "Sanctioned forms" and "Member catalogue", so a bare "rule 5" there resolves ambiguously — the member is the ugrep-shim one, not the positive-control form) and rules 9 and 10 of `docs/standards/architecture.md`. The restatement ledger beside `--color-accent` in `packages/ui/src/tokens/tokens.css` was the third instance, and is the one already brought into line
- What: a restatement ledger's value is that re-running its recorded sweep re-establishes the carrier list. Both entries above describe theirs as running "over tracked files" while recording a command that does no such thing, and they fail in two different ways worth naming separately. As literally recorded, neither carries `-r` **or a path operand**, so neither would recurse at all — run as written they read stdin and hang or return nothing, which is the blunter defect and the one a reader hits first. Give either the `-r` and path it obviously intends and the subtler defect appears instead: `command grep -r` descends into whatever is on disk, putting `node_modules/`, `dist/` and `coverage/` in scope, so a re-run returns a carrier list that has to be hand-filtered before it means anything. Either way the scope words and the command disagree, and the command wins. Catalogue member 5 of `evidence.md` is the same fault line seen from the other side, and is why this is easy to fall into: bare `grep` on this machine is a ugrep shim that silently honours `.gitignore`, so `command grep` is mandated for _evidence_ precisely to stop a file set narrowing without warning. That mandate is right for an emptiness claim and wrong for a ledger sweep, which genuinely wants tracked-only scope — and the way to ask for it is `git grep`, whose narrowing is the thing being requested rather than an undocumented side effect of a shim. Nothing distinguishes the two uses today, so a ledger author reaching for the sanctioned evidence form gets the wrong scope while following the standard. A third trap sits inside the fix: `git grep` is cwd-relative, so a recipe recorded beside the file it sweeps returns only that file when re-run from there, silently dropping every carrier elsewhere — the `-- :/` pathspec is what makes it repository-wide from anywhere, and a ledger sweep needs it. Fix direction: state the split where both recipes are written — `git grep … -- :/` for a ledger or carrier sweep whose claim is about tracked files, `command grep -r` only where "anywhere on disk" is genuinely meant — and true the two recipes above, either by switching them to that form or by dropping the scope words to match what they do. Worth doing as one pass rather than per-ledger: the shape is copied from whichever ledger the next author happens to read. One unrelated defect noticed in passing and parked here rather than lost, for whoever takes this entry: the same third S3 sync is called the "second sync" by the comment in `.github/workflows/deploy-web.yml` and the "third call" by the deploy-path section of `infra/README.md`, so the two ordinals disagree about the same operation
- Source: PR for #282 review cycle 2

## 2026-08-11 — The e2e lane's viewport sizes have one owner for two of them and none for the rest

- Where: `apps/web/e2e/viewports.ts` against `WIDE_VIEWPORT` in `apps/web/e2e/header.spec.ts` and `apps/web/e2e/map-regressions.spec.ts`, `NARROW_VIEWPORT` in `apps/web/e2e/map-regressions.spec.ts` and `apps/web/e2e/chart-surfaces.spec.ts`, `COMPACT_VIEWPORT` in `apps/web/e2e/composition.spec.ts`, `D15_VIEWPORT` in `apps/web/e2e/chart-surfaces.spec.ts`, and the quoted inline form at `docs/design/design-principles.md:562`
- What: #404 gave the two phone widths an owner because their _relationship_ was the fact with nowhere to live — 390x844 shared on purpose, 360x740 different on purpose. The lane's other sizes were out of its scope and still are, and two of the names now collide across files at different values: `WIDE_VIEWPORT` is 1280x800 in `header.spec.ts` and 1280x900 in `map-regressions.spec.ts`, and `NARROW_VIEWPORT` is 900x700 in `map-regressions.spec.ts` and 500x800 in `chart-surfaces.spec.ts`. A reader who has learned one meaning of either name carries it into the other file and is wrong, which is a sharper version of the "legible only to someone who had opened all three" problem the new module was created to end. Two of these are correctly ledgered where their derivation lives (`map.css` names `COMPACT_VIEWPORT`, `chart-geometry.ts` names `D15_VIEWPORT`) and should stay spec-local; the colliding pair is the part worth deciding — either distinct names or one owner. Separately, `design-principles.md` still quotes the inline `test.use({ viewport: { width: 390, height: 844 } })` form as the lane's mobile idiom, so an author copying from the design doc re-inlines the literal this extraction just centralised; `viewports.ts`'s docblock names that one and declines to count the rest, pointing at the sweep instead — `git grep -n -E '390|844' -- :/`, which also returns unledgered prose carriers at `apps/web/src/charts/use-chart-width.test.tsx`, `apps/web/src/header/AppHeader.test.tsx`, and three more inside the lane at `composition.spec.ts` (which computes against it) and `header.spec.ts` twice. That list is a floor, not a census, which is the point of recording the sweep rather than the count. **Demoted at cap** — raised in review cycle 3, which is the cap cycle, and it is pure quality: nothing is wrong today, no assertion is weakened, and the fix is a naming decision across four specs that this branch's acceptance does not reach
- Source: PR for #404 (the #404 chart/web batch), review cycle 3 — demoted at cap

## 2026-08-11 — The lane's one composite box read stays inline, where the seam cannot reach it

- Where: `rectOf` and its hand-rolled ambiguity guard inside `searchGeometry`'s `page.evaluate` in `apps/web/e2e/header.spec.ts`, against `apps/web/e2e/layout-box.ts` and the `BoxSource` seam `apps/web/e2e/layout-box.spec.ts` drives
- What: #404 gave the lane's box reads one home, and this is the read it could not take with it. `searchGeometry` has to read three rects in a **single JS turn** — that atomicity is the whole fix for the non-atomic `Promise.all` it replaced — so it runs inside `page.evaluate`, where the shared module's helpers cannot be called and Playwright's own strict-mode ambiguity check does not apply. The guard that check used to supply for free is now four hand-written lines living in a spec, reachable from no seam: `BoxSource` narrows to `boundingBox`, and nothing crossing into an `evaluate` closure can be driven by a hand-built source, so the deterministic spec that owns every other reader's behaviour cannot own this one. Review cycle 1 proved the guard live by pointing the read at `.app-header *` and watching the centring case fail on 17 matches — a transient mutant, which is precisely the "a measurement printed is not a measurement asserted" gap `.claude/agents/browser-smoke.md` names. The next spec that needs a composite single-turn read copies the idiom rather than calling it, and copies the guard with it or silently drops it. Fix direction: an owned page-side reader — a string of JS the module exports and specs pass to `evaluate`, or a `page`-taking helper in `layout-box.ts` that wraps the `evaluate` itself and can be tested against a stub page — so the guard has one home and one case. Not taken here because it is a second extraction with a different seam question, and #404's acceptance is about `boundingBox` call sites, which this is not
- Source: PR for #404 (the #404 chart/web batch), review cycle 2

## 2026-08-11 — The arrival rule's one unstated exception is the actuals legend row

- Where: the **Drawn chrome is gated on arrival** clause and the ledger beneath it in the new `## Simulated-data disclosure` section of `docs/design/dashboard-composition.md`, against `forecastChartLegend` in `apps/web/src/charts/forecast-chart-legend.tsx` and `actualRuns` in `apps/web/src/charts/ForecastChart.tsx` — and, the site that argues the opposite way, the **Two of the forecast entries are unconditional; the band's is gated on the data** paragraph in `docs/design/chart-treatment.md`, echoed in miniature by `forecast-chart-legend.tsx`'s own docblock
- What: the rule as written says anything standing for a quantity in the plot exists only where the drawn points carry that quantity, and the ledger excuses the actuals row on the grounds that the actuals series is unconditional. The row is unconditional in the markup; the series is not unconditional in the data. `ForecastChart` derives `actualRuns` from `actualKw != null`, and an empty `FleetActualsResponse` is a documented, normal 200 — so a fleet with no readings yet draws no actuals line and still legends an actuals key, which is the honesty defect #295 fixed for the band. The capability flag cannot be what makes the row honest, and the doc's first draft said it could: `fleetActuals` gates copy arms (`FleetPanel.tsx` picks between two subtitles with it) while `FleetPanel` calls `dataSource.fleetActuals(range)` unconditionally, and **both** shipping sources declare it `true` — the demo one and the HTTP one alike. What actually keeps the row honest today is narrower: the demo source synthesizes a reading for every past hour, so it genuinely always has actuals to draw. The exposure is the HTTP source, where an empty actuals response is a documented normal 200 and a fleet with no stored history draws no line under a legend that names one. So this is a rule with an unstated exception rather than a defect in either source, and the next surface author inherits the exception without the reason. The rule is the right rule — the fix is either to gate the actuals row on arrival the way the band's row already is, or to state in the ledger which chrome is deliberately capability-gated and why the actuals row is the one case. Logged rather than taken because the diff that named the rule is the wrong place to change a drawn surface, and because #429 is about to move the legend into the (i) popover, which changes what "the legend row" is
- Source: PR for #406 (the #404 chart/web batch), review cycle 1

## 2026-08-11 — A ledger banks its sweep mid-batch, and later commits in the same batch falsify it before the batch merges

- Where: the `feedbackLogLedger` key in `.claude/workflow.json`, and the ADR-member paragraph of the header ledger in `infra/storage/tables.tf` — the one beginning "ADRs are carriers here like anything else", together with the sweep sentence that backs it ("The sweep behind the ADR members, run 2026-08-11 …") and the member and set-aside lists it classifies into. Both were written by this batch, in different commits: the ledger key by the #403 commit, the `tables.tf` paragraph by the #401 commit. Against rule 9 of `docs/standards/architecture.md`, whose floor-and-named-sweep form both are instances of
- What: each ledger states its carrier list as the output of a named sweep run on a stated date, and by the time the batch reached review, commits landing later in the same batch had put new matches inside those sweeps. `feedbackLogLedger` names `command grep -rnE 'review-feedback\.md'` over tracked files on 2026-08-11 and classifies every hit as carrier or not-carrier; re-run at the branch head it also returns `docs/adr/0002-storage-split.md`, whose 2026-08-11 (#401) Amendments entry names `docs/review-feedback.md` among the non-asserting hits of its own quoter sweep — a file that gained the path in the #401 commit, which lands after the #403 commit that banked the ledger — and `infra/web/security-headers.tf`, whose own ledger names `docs/review-feedback.md` as a past-tense record of what PR #393 did. Both sit outside the carrier list and the not-carrier list alike; the `security-headers.tf` hit is the sharper illustration of the shape rather than of the staleness, since it landed in the very commit that wrote the ledger and so was already unclassified at banking time. `tables.tf`'s ADR arm has the same shape pointed at `docs/adr/*.md`: its capacity arm `[0-9] ?(WCU|RCU)|25/25|provisioned`, joined-line, now returns `docs/adr/README.md`, on the `| provisioned | 14 | 21 |` quotation inside the worked premise-side case the #402 commit added to `## Amendments` — a paragraph that argues from that row ("that row is what the capacity-mode decision was sized against"), which is the ledger's own test for a member rather than a bare citation. The paragraph's set-aside list reaches `docs/adr/README.md` only for its read-arm hit, its description of ADR 0002's #264 entry, so the capacity hit is classified in neither direction. **No ledger here is false as worded**, because each declares itself a floor and names its sweep rather than claiming a census — that is the floor form doing exactly the job it was introduced for, and it is why this is logged rather than fixed. What it exposes is that a ledger written early in a batch is stale at merge in a predictable, mechanical way, and the batch has no step that re-runs a banked sweep: `/review-loop` reads the diff of the cycle in front of it, and nothing between the last fix round and the PR re-runs a pattern an earlier commit wrote down. Fix direction, either half of which closes it: the last commit of a batch re-runs the sweeps its earlier commits banked — the ledgers state their patterns verbatim, so this is mechanical rather than a judgement — or the sweep line records the commit it was run at rather than only the date, so a reader can tell how much has happened since, a date being the wrong resolution for a batch whose commits all carry the same one. Same root as this log's cycle-1 entry "The ledger-banking obligation landed in the rule and the agent contracts but not in the skills that execute them", different segment of the same lifecycle: that entry is about the obligation never being executed, this one about its output going stale inside a single change, and both are the ledger having no owner after the moment it is written
- Source: #401 batch, review cycle 2

## 2026-08-11 — The redirect table preserves retired claims in the present tense, and each sweep that meets them adjudicates them again from scratch

- Where: the `## Captured-entry redirects` section of this file — its intro paragraph ("Source comments across the repo cite entries by title … so this table is where a dangling pointer lands"), its Maintenance paragraph, and the rows themselves, of which the [#401](https://github.com/TomBennett-Lloyd/cumulo/issues/401) row is the live instance; against rule 9 of `docs/standards/architecture.md`, whose historical-claims clause reads "Historical claims survive in past tense, naming the decision they reason about"
- What: the rows reproduce a captured entry's title verbatim, and they have to — the intro says why. A source comment somewhere cites the entry by title, triage deletes the entry, and the row is the whole reason that citation still resolves; reword the title and the row stops doing the one job it exists for. But a title is written in the present tense of the day it was logged, and the row outlives the claim it was written about, so the table is a permanent present-tense repository of assertions that may since have been retired — history in the shape of live prose. The #401 row is the instance, and this batch is what made it one: its clauses "Rule 9 exempts ADRs, so an ADR restating another ADR's figure has no discipline at all" and "Rule 9 exempts ADRs from the restatement ledger, and the Amendments convention made that exemption false" state an exemption this batch's own narrowing of rule 9 retired. A joined-line sweep of tracked files for surviving statements of that exemption — `command grep -oE '[Rr]ule 9 exempts [^.·|]{0,80}|[Aa][Dd][Rr]s? (are|is) exempt[^.·|]{0,60}|exempt[a-z]* (from|covers)[^.·|]{0,60}[Aa][Dd][Rr][^.·|]{0,40}'`, run 2026-08-11 against a fixture carrying a written-out assertion of the exemption as its positive control — returns those clauses, plus one non-member its ADR arm catches across a line break: `apps/api/src/sites/create-site.ts`'s "structurally exempt from eviction (ADR 0002)", a different exemption entirely. That list is a floor, not a census. So every sweep of this claim family lands on the same row, reads it, works out that it is a preserved title rather than a live assertion, and leaves it. This cycle's sweep did exactly that; earlier cycles of the same batch had already done it, and neither wrote the conclusion anywhere the next one would find it. That is the systemic half: the cost is not this row, it is that **every** future claim retirement pays the same adjudication, once per sweep, forever, and the rows accumulate. Against the historical-claims clause the table is the awkward case — the rows are genuine history and satisfy the clause's intent exactly, while failing its stated form, and the clause gives them no way to say so. Fix direction, either half of which closes it: rule 9 admits a named exception for a table whose text is load-bearing for _resolution_ rather than for assertion, so the shape is legible from the rule; or the table carries one standing note above the rows that adjudicates all of them at once — these titles are preserved as of capture and assert nothing about the present — so the next sweep reads a sentence instead of re-deriving it. The note is cheaper and sits beside the thing it describes; the rule change is what stops the next reader deciding the note is wrong
- Source: #401 batch, review cycle 3

## 2026-08-12 — A sweep-defined ledger loses members to paraphrase and gains unlisted ones from the batch writing it

- Where: the `### Restatement ledger (architecture.md rule 9)` block in the "One chart, in every state, and nothing that swaps around it" section of `apps/web/src/dashboard/fleet-panel-body.tsx` — its member list, its `map/SitePopoverCard.tsx` bullet and the sweep sentence that backs them; against whatever that sweep returns when it is re-run, which this bullet deliberately does not copy out — the sweep is what establishes membership, and any list written down here would decay in exactly the way this entry is about. The What below names carriers as illustration and leaves membership to the sweep; and rule 9 of `docs/standards/architecture.md`, whose floor-and-named-sweep form the ledger is an instance of
- What: the ledger names its own membership rule — `command grep -rnE "in every state|One chart|one arrangement" apps/web/src docs`, run from the worktree root — and declares itself a floor rather than a census, which is the form working as intended and is why this is logged rather than fixed. What the batch that wrote it demonstrates is that the list decays in **both** directions the moment anyone edits prose, and neither direction is visible to any check. Losing a member: the ledger lists `map/SitePopoverCard.tsx` for "the note that the card plots nothing because the fleet chart is on screen in every state of the panel", and the same batch rewrote that note — the paraphrase survives and still reasons from the claim ("the fleet chart draws that site's line … one press away rather than on screen"), while the phrase the sweep matches is gone, so the file is now listed by a ledger whose own sweep no longer reaches it. Re-running the sweep to check the list would silently drop it. Gaining unlisted ones: the same sweep, re-run at the branch head from the worktree root, returns in-scope carriers the list does not name. **How many is deliberately not recorded here**, for the reason the ledger itself declares a floor rather than a census — a count is falsified by one more carrier, and an entry whose whole thesis is that this list decays in both directions is the worst possible place to carry one. Re-run the sweep; that is the only thing that answers it as of the moment it is asked. Named as the illustration rather than as a census: `docs/design/chart-treatment.md`'s Legend section, whose "**a legend is always one press away, in every state**" is the guarantee's own statement rather than a restatement of it, and which the ledger does not list. The direction keeps producing more inside this batch itself — `docs/design/dashboard-composition.md` is returned in sections outside the one the ledger's bullet for that file names, at least one of them written by a fix round of this very review cycle, which is the gaining direction happening inside the round that reported the losing one. The root cause is that membership is defined by a _string match_ while the claim is carried by _meaning_: a carrier rephrased into a paraphrase leaves the sweep without leaving the claim, and a new carrier written in the sweep's own words joins the claim without joining the list, so the ledger's floor is honest and its list is wrong in opposite directions at once. Same family as this log's "A ledger banks its sweep mid-batch, and later commits in the same batch falsify it before the batch merges", and one step past it: that entry is about a banked sweep going stale, this one about the sweep and the claim not having the same extension even when the sweep is re-run today. Fix direction: either the claim gets a citable owner that carriers reference by name rather than by phrase — a doc anchor or an exported symbol, so paraphrase is free and membership is a link — or the ledger's sweep is treated as a gate rather than a note, re-run mechanically at the end of a batch with its output diffed against the list, which catches the gaining direction but still cannot see a member that paraphrased its way out
- Source: #431 design-round batch (#428, #429, #430, #431), review cycle 1
