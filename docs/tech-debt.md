# Tech-debt log

A **buffer, not an archive**. `/review-loop` appends SYSTEMIC findings here instead of iterating on them; `/triage` periodically clusters entries, files root-cause GitHub issues, and **deletes** what it captured. A long-lived entry here means triage is overdue.

Entry format:

```
## YYYY-MM-DD — short title
- Where: file/module references
- What: the pattern or problem (symptom AND suspected root cause if known)
- Source: PR/issue #
```

Pointers must survive unrelated edits: cite **files and symbol or section names** (function names, headings, config keys, script names) — never bare line numbers, and never a copied code literal unless the literal itself is the finding. An entry that pins its claim to `file.sh`:42 sends its reader to whatever happens to sit at line 42 months later. Applies to entries dated 2026-07-31 onward; the back catalogue is retrofitted opportunistically, whenever an entry is edited for any other reason.

---

## Captured-entry redirects

Source comments across the repo cite entries by title ("recorded in `docs/tech-debt.md` (…)"). Triage deletes the entry but cannot edit those comments without turning a docs-only PR into a source-code one, so this table is where a dangling pointer lands. It is a **redirect, not an archive** — the content lives on the issue.

Maintenance: a row dies with its issue. Each capturing issue's implementation edits the very files whose comments cite it, so the citation and the row go together; whoever closes the issue deletes the row.

| Captured entry                                                                                                                                                                                                                                                                                       | Now owned by                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `check-supply-chain-policy.sh` reads YAML by line shape · Supply-chain gate asserts the manifest, but pnpm resolves policy from four places · `check-infra-mirrors.sh` reads Terraform by line shape                                                                                                 | [#158](https://github.com/TomBennett-Lloyd/cumulo/issues/158)           |
| Only `@cumulo/shared` can express a type-level test                                                                                                                                                                                                                                                  | [#159](https://github.com/TomBennett-Lloyd/cumulo/issues/159)           |
| Skill score compares two RMSEs computed over different sample sets · A metrics row cannot say which hours of its own period are missing                                                                                                                                                              | [#160](https://github.com/TomBennett-Lloyd/cumulo/issues/160)           |
| No error boundary above the dashboard's async work · The map's placeholder shells restate `MapView`'s structure · Loading announcements are per-component conventions                                                                                                                                | [#161](https://github.com/TomBennett-Lloyd/cumulo/issues/161)           |
| `invalid-response` now means two different failures · The shared data hook is still named after the surface that was deleted                                                                                                                                                                         | [#162](https://github.com/TomBennett-Lloyd/cumulo/issues/162)           |
| Cycle rotation advances one location per hour, not one window                                                                                                                                                                                                                                        | [#163](https://github.com/TomBennett-Lloyd/cumulo/issues/163)           |
| One ingestion alarm now stands for two unrelated operator questions · A forecast consumer pointed at the wrong environment is indistinguishable from an idle fleet · A user-site counter stuck above the real population                                                                             | [#164](https://github.com/TomBennett-Lloyd/cumulo/issues/164)           |
| A hook that cannot read its own events disables edit-time lint silently                                                                                                                                                                                                                              | [#102](https://github.com/TomBennett-Lloyd/cumulo/issues/102) (comment) |
| The port-inversion invariant in `@cumulo/hindcast` is convention, not a gate                                                                                                                                                                                                                         | [#112](https://github.com/TomBennett-Lloyd/cumulo/issues/112) (comment) |
| No gate covers a constant restated in prose                                                                                                                                                                                                                                                          | [#249](https://github.com/TomBennett-Lloyd/cumulo/issues/249)           |
| CI never builds `apps/web`                                                                                                                                                                                                                                                                           | [#142](https://github.com/TomBennett-Lloyd/cumulo/issues/142) (comment) |
| `Retry-After` is unreadable cross-origin, so the client contract's backoff can never fire                                                                                                                                                                                                            | [#21](https://github.com/TomBennett-Lloyd/cumulo/issues/21) (comment)   |
| The bundle gate's "stale build" verdict only detects multi-entry staleness                                                                                                                                                                                                                           | [#111](https://github.com/TomBennett-Lloyd/cumulo/issues/111) (comment) |
| The conflict-retry budget is derived from a closed cohort, so exhausting it is read as a fault it need not be                                                                                                                                                                                        | [#155](https://github.com/TomBennett-Lloyd/cumulo/issues/155) (comment) |
| actionlint's action-input check is dead over most of this repo's `uses:` lines                                                                                                                                                                                                                       | [#108](https://github.com/TomBennett-Lloyd/cumulo/issues/108) (comment) |
| The two batch paths refuse an unusable policy in different shapes                                                                                                                                                                                                                                    | [#100](https://github.com/TomBennett-Lloyd/cumulo/issues/100) (comment) |
| The split-capture migration silently narrowed the harness family's raw `$out` readers · reap's broken-git-link shape arm is shadowed by the identity arm                                                                                                                                             | [#101](https://github.com/TomBennett-Lloyd/cumulo/issues/101) (comment) |
| The half-open UTC window is still declared five ways beside its named contract                                                                                                                                                                                                                       | [#232](https://github.com/TomBennett-Lloyd/cumulo/issues/232)           |
| Browser-side fetches have no timeout policy, and the de-dup map inherits the hang · The ingestion cycle deadline is conditional on no mid-body stall, and no finite slack covers it                                                                                                                  | [#233](https://github.com/TomBennett-Lloyd/cumulo/issues/233)           |
| The API deadline gates loops, not the straight-line prefix · `queryAllPages`'s pagination bound is optional, and four of five call sites walk unbounded                                                                                                                                              | [#234](https://github.com/TomBennett-Lloyd/cumulo/issues/234)           |
| The token preview hand-rolls a chart the chrome convention cannot reach · A dashboard test pins the chart's chrome wording by whole-table `toEqual` · Five of the twelve consolidated strings sit outside every phrase-class sweep · The spoken chart readout carries neither the unit nor the clock | [#235](https://github.com/TomBennett-Lloyd/cumulo/issues/235)           |
| The hooks' "stdin was unreadable" report is dead code behind a hang                                                                                                                                                                                                                                  | [#236](https://github.com/TomBennett-Lloyd/cumulo/issues/236)           |

---

## 2026-07-30 — `aws_budgets_budget.cost_types` left at AWS defaults

- Where: `infra/bootstrap/budget.tf` (`aws_budgets_budget.monthly_cost_ceiling`)
- What: no `cost_types` block, so the budget uses the AWS defaults, which subtract credits and refunds. On an account carrying promotional credits the meter can therefore run well past $100/month of gross usage while net cost stays under threshold and nothing alerts — the alarm reports what will be billed, not what is being consumed. That is a defensible reading of "cost ceiling" for a project whose ceiling is about the bank balance, and it is the current deliberate choice; it stops being defensible the moment credits land on the account, because the whole point of the ceiling is to catch runaway usage _before_ it is expensive. Revisit if credits appear (or before any AWS-credits programme is used for this project): either add `cost_types { include_credit = true, include_refund = false }`, or add a second usage-oriented budget beside the billed-cost one. Not a fix for this diff — it is a policy decision about what the number means, and it wants the account's credit state as an input.
- Source: #38 review cycle 1
- Triage note (2026-07-31): deliberately left in the buffer rather than filed. It is a parked decision with an external trigger (credits appearing on the account), not debt to clear — an open issue would be picked up by backlog burning and correctly do nothing. Convert it the moment the trigger fires.
- Triage note (2026-08-01): re-confirmed on the pass that emptied the other 29 entries. The trigger has not fired; this is the log's only survivor **by decision**, so its age is not evidence that triage is overdue.
- Triage note (2026-08-03): re-confirmed again on the pass that emptied the other 22 entries. Trigger still unfired.

## 2026-08-02 — canon spawns an interpreter per path, and the sweep multiplies it

- Where: `canon` in `.claude/scripts/worktree-lib.sh`; its per-porcelain-line call sites in `reap-worktree.sh` and `sweep-worktrees.sh`
- What: the python→node migration kept canon's one-path-per-process shape, so reap is O(worktrees) node spawns and a sweep is O(N²); the lifecycle harness measured 27 s → 42 s on the same cases. No correctness impact and fine at this repo's worktree counts — recorded so the cost has a name. Fix direction if it ever bites: a `canon_many` reading a NUL-separated list in one spawn, or batching the porcelain parse's canon calls.
- Source: #102 review cycle 1
- Triage note (2026-08-03): left in the buffer, not filed. It is a measured cost with no correctness consequence and a named trigger ("if it ever bites" — worktree counts rising, or the lifecycle harness becoming slow enough to discourage running it); an open issue would be scheduled and would correctly find nothing worth doing. It clusters with nothing else in the log — every other script-harness entry this pass captured was about an assertion proving less than it claims, which this is not.

## 2026-08-03 — A second family of stated values is restated in prose with neither pointer nor ledger

- Where: event-source values (`maximum_concurrency`, `batch_size`, the Lambda `timeout`) restated at `apps/forecast/src/handler.ts` (two comment blocks), `apps/forecast/README.md` (the concurrency paragraph), `infra/README.md` (the burst-guard note and the forecast cost note) — owners `infra/forecast/event-source.tf` / `lambda.tf`, no ledger; ADR-0002-derived read costs ("~25 read units per load", "~0.5 read units") at `apps/web/src/dashboard/Dashboard.tsx`, `apps/web/src/data/fleet-data-source.ts`, `apps/web/src/data/use-first-forecast.ts` (+ its test), `infra/storage/tables.tf` (the series comment), `infra/README.md` (the `series` cost row's "a dashboard load is ~25 units")
- What: #231 installed the one-owner rule and ledgered the pool arithmetic, but rule 9 now binds repo-wide and these two families predate it — restatements with neither an owner pointer nor a ledger beside their owners. The gate half — whether a prose restatement can be checked mechanically at all — is #249; this entry is the compliance half. Fix direction: ledger comments beside `event-source.tf`/`lambda.tf` and the ADR-derived cost figures' one owner (or an explicit scope clause in rule 9 stating it binds new writes and opportunistic edits, not a retrofit).
- Source: #231 review cycle 2
- Note (2026-08-10, #296 review cycle 3): the family's number drifted, not just its wording — since #264 a dashboard load runs _two_ fleet-route fan-outs (~25 on `series` for forecasts + ~25 for actuals + ~2 on `sites` ≈ 52), so `infra/README.md`'s "~25 units" per load understates by ~2×. The cycle-3 sweep found exactly the ledgered sites plus that README row; correction routed to #296 C5's floor (correct the number, not just the noun).

## 2026-08-03 — A provider payload repeating an hour is reported as ingestion's bug

- Where: `parseForecastResponse` in `apps/ingestion/src/open-meteo/response.ts` (its `hourly.time` loop and its `malformed` arm); the refusal it currently lands on is `requireUniqueKeys` in `packages/storage/src/write-preconditions.ts`, reached through `putForecastWeather` in `packages/storage/src/adapters/weather/weather-adapter.ts`
- What: `hourly.time` is mapped 1:1 into readings with no uniqueness check, so a payload that repeats an hour yields two readings for one location-hour and is caught a layer down, where the storage precondition refuses the write with "the caller must de-duplicate before writing". That message blames ingestion's code for the provider's payload and sends the reader to the wrong repository — the inverse of rule 1's blame tiebreak, which asks whose fault it is and wants the check owned by the `malformed` arm in `response.ts`, the arm that already names Open-Meteo as the party at fault and whose `ParsedForecast` failure value the cycle already knows how to report. Debt rather than blocker on two counts: the write is refused rather than corrupted, and the blast radius is correctly bounded to one location's cycle outcome. Only the blame is misrouted, and only on a payload we have never actually seen.
- Source: #100 review cycle 1

## 2026-08-02 — `drainWriteRequests`' policy seam outlived the caller that justified it

- Where: `drainWriteRequests` and `putSeriesItems` in `packages/storage/src/adapters/series/series-adapter.ts`
- What: with `deleteSiteSeries` retired (ADR 0007), `drainWriteRequests` has exactly one caller and its `policy` parameter only ever receives `this.batchPolicy` — the doc sentence that justified the seam ("how hard to push" is where a cleanup and an ingestion write legitimately disagree) was deleted with the caller that made it true, but the seam itself remains. Whether the adapter's two one-caller private write layers still earn their split is a `packages/storage` shape question: collapsing them touches the ingestion write path, which the retiring diff deliberately left alone. Decide next time that path is open — collapse into `putSeriesItems`, or restate a reason the parameter exists.
- Source: #167 review cycle 1

## 2026-08-04 — Five stacks validate the environment alphabet nobody declared them against

- Where: the `condition = can(regex("^[a-z0-9-]+$", var.environment))` validation in `infra/{api,forecast,ingestion,alerting,web}/variables.tf`; the declared mirror is `infra/storage/variables.tf` against `ENVIRONMENT_PATTERN` in `packages/storage/src/table-name.ts` (the `regex-eq` record in `check-infra-mirrors.sh`)
- What: #133 declared exactly one of the six copies of the environment alphabet. The other five are live mirrors of the same constant, not coincidences — `apps/api/src/main.ts`, `apps/forecast/src/main.ts` and `apps/ingestion/src/main.ts` all route their env through `storageTableName`, so widen `infra/api/variables.tf` alone and the api stack accepts a suffix the code then throws on, with the gate green. The fix is a decision before it is five lines: either five more `regex-eq` records (one per stack, each `variables.tf` gaining the citation `infra/storage/variables.tf` carries), or an explicit statement that each stack owns its own alphabet and the storage record is the only load-bearing one. Deliberately not decided inside #133's diff, which was scoped to the gate's grammar rather than the mirror census.
- Source: #133 review cycle 1

## 2026-08-05 — Three owned values have restatement carriers and no rule-9 ledger

- Where: (a) the demo fleet's size — owner `generateFleet` in `packages/shared/src/fleet.ts`, where it is `fleetLocations` × `sitesPerLocation` (lines 39, 54); carriers `apps/ingestion/README.md:76,77,88,90,103`, `apps/ingestion/src/locations.test.ts:14,21,70,72`, `apps/web/src/add-site/creation-throttle.ts:71`, `apps/web/src/dashboard/Dashboard.tsx:466`, `apps/web/src/dashboard/FleetPanel.test.tsx:52,55`, `apps/web/src/map/clustering.test.ts:86`, `apps/web/src/map/clustering.ts:5`, plus this branch's `apps/web/e2e/composition.spec.ts` constant and assertion. (b) the demo's first-forecast delay — owner `DEFAULT_FIRST_FORECAST_DELAY_MS` in `apps/web/src/data/demo-fleet-data-source.ts:31`; carriers `apps/web/src/data/use-first-forecast.ts:15,40`, `apps/web/src/dashboard/Dashboard.test.tsx:35,116`, `apps/web/src/data/demo-fleet-data-source.test.ts:27,176`. (c) the opening zoom in `apps/web/src/map/framing.ts` — `INITIAL_CAMERA.zoom` since #265 review cycle 1, `INITIAL_ZOOM` when this entry was written — whose own doc comment restates its literal beside the declaration.
- What: rule 9 gives each of these one owner and asks every other mention to either name that owner or be listed in a restatement ledger beside it, so changing the owner finds every copy at plan time. None of the three has a ledger. Most of the carriers legitimately hold the literal — they compute with it or assert it — which is exactly the case the ledger exists for, so the fix is not literal removal at the carriers but a ledger comment beside each owner (with literal removal only where the carrier is prose). #107 trued the carriers inside its own diff (`apps/web/e2e/**`) and deliberately stopped there: ledgering three families across nine other files is a cross-cutting pass, not a browser-lane ticket, and doing it piecemeal per touching diff is how a census ends up half-written. Clusters with the 2026-08-03 entry above, which is the same rule-9 compliance gap on two further families. One more argument for doing it as a ledger rather than as a carrier list: the line numbers above drift with every diff that touches a carrier file — #265 alone moved the `Dashboard.tsx` fleet-size carrier from :466 to :516 and renamed (c)'s symbol out from under its pointer — so a census pinned to line numbers is stale before it is read, while the symbol and section anchors this log's own pointer convention already asks for (see the header) survive the edits that move them.
- Source: #107 review cycle 1; carrier-drift note added by #265 review cycle 1 (map controls)

## 2026-08-05 — The README's CI row is a census that stopped counting

- Where: `README.md:38` (the `.github/workflows/ci.yml` row of the "Three layers" table, `README.md:32-38`); the jobs it describes are `checks`, `web-build`, `web-e2e`, `gitleaks` and `terraform` in `.github/workflows/ci.yml`
- What: the row reads "the unskippable backstop: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm format:check` across the whole repo, plus a full-history gitleaks scan — authoritative", which is the `checks` and `gitleaks` jobs and nothing else. It already omitted `web-build` and `terraform`, and #107 added `web-e2e` — a required, merge-blocking lane that `pnpm test` does not reach, needs a browser binary, and has its own local command (`pnpm --filter @cumulo/web test:e2e`, named at `.github/workflows/ci.yml:144-145`). A reader of "Local checks" therefore cannot learn from the README that the lane exists or how to run it before pushing, which is exactly the audience the table is for. The row is phrased as a census (an enumerated list of commands) while behaving as a summary, and each new job silently widens the gap — three jobs missing is the signal that nobody will maintain an enumeration here. Fix direction is a decision first: either make it a summary that cannot go stale ("every gate in `ci.yml`, which owns its own list") and move the per-lane commands to where each lane is documented, or make it a real census and add the missing three plus a note that new jobs belong in it. Not folded into #107, whose diff owns the lane itself rather than the README's model of CI.
- Source: #107 review cycle 2

## 2026-08-05 — ci.yml's hermetic-claim comment names the basemap provider outside the ledger

- Where: `.github/workflows/ci.yml:154` — the `web-e2e` job's "(d) Hermetic" comment ("The specs stub the OpenFreeMap basemap style at the network boundary (apps/web/e2e/hermetic-basemap.ts)"); the owner is `OPENFREEMAP_STYLES` in `apps/web/src/map/basemap.ts`, whose restatement ledger (added this branch) does not list this comment
- What: the comment names the provider and points at the co-carrier `hermetic-basemap.ts` rather than at the owner. Rule 9 wants a mention to name the owner and carry no literal of its own; after a provider swap this comment would name a provider the lane no longer stubs, with nothing red. Descriptive prose, non-blocking (the file it cites is itself a ledger member) — fold into the ledger or repoint at the owner next time the census families above are swept; it is one more member of that same compliance gap.
- Source: #107 review cycle 3

## 2026-08-05 — `response.ts` calls itself the only source of responses while two 204s are hand-built beside it

- Where: the module docblock at `apps/api/src/http/response.ts:6-7` ("the two ways this API ever produces one — a validated success body, or an `apiErrorSchema` failure"); the counter-examples are `apps/api/src/sites/delete-site.ts:199` and, as of this branch, the preflight answer in `apps/api/src/http/router.ts`
- What: the claim was already false before #263 — `delete-site.ts` hand-builds `{ statusCode: 204, headers: {} }` rather than going through any helper here — and the preflight 204 makes it two. So the file that owns the response vocabulary describes a vocabulary the service does not keep to, and each new 204 is another object literal whose "no body, no content-type" reasoning is re-derived at the call site while the reasoning for it already lives in this file's own `ApiResponse` docblock (lines 18-19). Wanted: a named `noContentResponse()` exported here, the two call sites routed through it, and the docblock trued to three ways. Deliberately not forced into #263's diff: the two 204s are the same shape for genuinely different reasons (a deleted resource versus a CORS mechanism answer), so they may legitimately diverge — a shared helper is only right if it stays a single fact, and `structure.md` rule 7 says that judgement wants its own look rather than a drive-by extraction under a fix-now.
- Source: #263 review cycle 1

## 2026-08-05 — No a11y linting, on the diff that lands the app's first seven interaction patterns

- Where: `eslint.config.mjs` (no `eslint-plugin-jsx-a11y` in its plugin list); the patterns now relying on review attention alone are the disclosure in `HeaderMenu` (`apps/web/src/header/HeaderMenu.tsx` — its `aria-expanded` button and the `onKeyDown` on the non-interactive wrapper `div`), the native modal in `AboutDialog` (`apps/web/src/header/AboutDialog.tsx` — its `aria-labelledby` and `onCancel` wiring), the decorative `aria-hidden` mark in `Brand` (`apps/web/src/header/Brand.tsx`), and — the fourth and by some distance the most intricate — the combobox in `SiteSearch` (`apps/web/src/header/SiteSearch.tsx`: `role="combobox"` with `aria-expanded`/`aria-controls`/`aria-autocomplete` on the input, `aria-activedescendant` pointing into a `role="listbox"` of `role="option"` `<li>`s, one of them `aria-disabled` for the no-match message, and `aria-selected` tracking the highlight), and the fifth, the toggletip in `InfoTip` (`apps/web/src/info/InfoTip.tsx` — its `aria-label` naming the subject rather than the glyph, its `aria-expanded` button, and the `onKeyDown` on its non-interactive wrapper `span`; it is deliberately a disclosure rather than the canonical `role="status"` toggletip, which is the kind of considered deviation a rule set has to be chosen around rather than switched on over), and the two the site table brought with it — the native `<details>`/`<summary>` disclosure and the data table itself (`apps/web/src/dashboard/SiteTable.tsx`: `scope="col"`/`scope="row"` deciding which cells are headers of what, the table's `aria-label`, and `aria-current` on the row buttons), and — the eighth, which arrived after that diff rather than in it — the icon-only button in `HeaderMenu` (#284 D16 replaced its text child with a burger, so `aria-label="Menu"` is now the only place its accessible name exists, the `<svg>` under it being `aria-hidden`), and the ninth, from the same design pass — the collapsed search in `AppHeader` (`apps/web/src/header/AppHeader.tsx`, #284 D17: a second icon-only control, `aria-label="Search sites"` over an `aria-hidden` magnifier, whose `aria-expanded` disclosure opens a bar dismissed by a `keydown` and a `focusout` on the non-interactive wrapper `div` around it — and which is also the first place two renderings of the same named control exist in one tree, with a media query rather than anything a linter reads deciding that only one of them is ever in the accessibility tree)
- What: the repo enforces the frontend gate mechanically for _tokens_ and nothing else — `react.md` rule 5's "if a token is missing, the lint error _is_ the design signal" is scoped to colour and spacing, and no rule in either linter has an opinion about roles, labels, or keyboard reachability. That was defensible while `apps/web` was buttons and links; this diff adds the first `aria-expanded` disclosure, the first `<dialog>`, the first keydown handler on a non-interactive element, the first composite widget in the codebase, and — as the design pass finished — the toggletip that three surfaces now mount, the first native `<details>` disclosure, and the first data table, so the a11y properties of seven new patterns rest entirely on a reviewer noticing — eight as of #284 D16 and nine as of D17, that count and the heading's being this diff's rather than the standing list's, which has grown twice since. The failure mode is silent and cumulative: each future popover, dialog or custom control copies whatever the first one did, and a regression (a dropped `aria-expanded`, a label that stops matching, a handler that becomes unreachable) is green in every gate. The combobox is the sharpest illustration and arrived with its own worked example: it shipped in review cycle 1 with `aria-activedescendant` set whenever a match existed rather than whenever the popup was _open_, so on every page load the attribute named an element that was not in the document. `axe`'s `aria-valid-attr-value` reports exactly that, `jsx-a11y` does not (the value is computed), and neither is wired up — a reviewer reading the JSX is what caught it, which is precisely the dependency this entry is about. Fix direction is a decision before it is a config line — `jsx-a11y`'s recommended set would flag real things here but also has known false positives on deliberate patterns (the wrapper `onKeyDown` above is one, and it is correct as written), so adopting it means choosing a rule subset and accepting the first round of triage, not switching it on; and the combobox case argues that the static half is not the whole answer, so a rendered-DOM check (`axe-core` over the browser lane's already-assembled page, or `jest-axe` in the unit lane) belongs in the same decision. The table members are the second worked example, and a cheaper one to have missed: the row-header semantics its own docblock calls load-bearing shipped with nothing asserting them, so swapping `<th scope="row">` for a `<td>` left every test green and every gate quiet while a screen reader reading across a row stopped being told which site the numbers belonged to. A `getAllByRole('rowheader')` assertion closed _that_ hole in review, which is the point: review attention caught it, and review attention is not a gate. `jsx-a11y` has rules in exactly this area (`scope`, `no-redundant-roles`) and would have had an opinion about the markup, though not about the missing assertion — which is the other half of why the rendered-DOM check belongs in the same decision. `SiteSearch`'s own docblock points here, so the pointer is bidirectional and neither end can be edited in ignorance of the other. The icon-only button is the third worked example and the one with the least left to notice: the disclosure listed first above dropped its text child for a glyph in #284 D16, so its accessible name is now one attribute and nothing else. A dropped `aria-label` there is not a degraded name but no name at all — a control a screen reader announces as "button" and a voice-control user cannot say out loud — and it is the first pattern here where that is true: the (i) tips carry an `aria-label` over a visible `i`, so the same slip leaves one of those badly named rather than unnamed, and the mark in `Brand` has no name to lose. `jsx-a11y` has a rule shaped for exactly this (`control-has-associated-label`), which is one more thing the static half would in fact have caught; what stands in for it today is a case in `HeaderMenu.test.tsx` asserting the name and the absence of a text node together — a test somebody remembered to write, which is the distinction this entry keeps making, and which #284 D17 has now had to remember a second time (`AppHeader.test.tsx` makes the same pair of assertions about the search toggle). That second one is what turns this worked example from an anecdote into the entry's argument: the pattern is being copied, exactly as the failure mode above predicts, and the thing being copied is a convention held up by whoever writes the next test rather than by a rule.
- Source: #265 review cycle 1 (header shell); combobox member added by the header search's cycle 1; toggletip member added when the descriptions moved behind (i) tips (#265 C10); disclosure and table members added by the site table (#265 C9), folded in at that PR's review cycle 1; icon-only-button member added when the header menu became a burger (#284 D16); collapsed-search member added when the search folded behind an icon (#284 D17)

## 2026-08-05 — Neither popover has focus-out dismissal, and the second one has now copied the first

- Where: `HeaderMenu` in `apps/web/src/header/HeaderMenu.tsx` and `InfoTip` in `apps/web/src/info/InfoTip.tsx` — each with the same three dismissal routes (the button's own `onClick`, the Escape arm of `handleKeyDown`, and the `mousedown` listener in the outside-press effect), and neither with a fourth
- What: the popover closes on a second button press, on Escape, and on a press outside — but not on focus leaving it. Tabbing forward past "About Cumulo" moves focus into the dashboard behind while the popover stays open and painted, so a keyboard reader ends up with an overlay above the surface they are now interacting with, and a sighted mouse user watching the same screen sees a menu that no longer belongs to anything. Neither the unit lane nor the browser lane catches it: every dismissal test drives one of the three routes that do work. Pattern-level rather than a local bug — this is the app's first popover, so whatever it does about focus-out is what the next one will do, and adding the fourth route later means retrofitting every copy. Fix direction: a `focusout` handler on the container that closes when `relatedTarget` is outside it (and treats `null` — focus leaving the document entirely — as "stay open", or the popover dismisses itself when the window loses focus). Deliberately not folded into #265's diff, which owns the header's composition rather than a dismissal policy every future overlay inherits; decide it once, in one place, when a second popover exists to state the shared rule against.

  That second popover now exists. The toggletip (#265 C10) reproduced all three routes in the same shapes and inherited the missing fourth with them, which is this entry's prediction arriving rather than a new finding — and it is also what the fix now has to account for: the `focusout` handler is one rule wanted in two components, so applying it is the moment the shared hook `structure.md` rule 7 declined to extract from a single copy earns its keep. `InfoTip`'s own docblock points here, so the pointer is bidirectional and neither end can be edited in ignorance of the other.

- Source: #265 review cycle 1 (header shell); second-copy member added by the toggletips (#265 C10)

## 2026-08-05 — Stylesheet facts with no observer: two of the app's four stacking values, the sheet's own import, the credits' veil, the credit-link treatment, and the site card's anchoring

- Where: the app's four `z-index` declarations — `.header-menu-popover` and `.site-search-listbox` in `apps/web/src/header/header.css`, `.info-tip-panel` in `apps/web/src/info/info.css` since the descriptions moved behind (i) tips, and `.site-popover-anchor` in `apps/web/src/map/site-popover.css` since #284 C2; the first two are one member because they are the same declaration at the same value, clearing the same maplibre controls, and would fail in the same way; the third — `.info-tip-panel` — GAINED an observer in #265 PR 6 (`info-tips.spec.ts`'s `panelIsOnTop` hit-tests paint order at the panel's centre, and dropping that declaration turns the lane red), so it is listed here only as the worked example of what the other two still lack; the fourth — `.site-popover-anchor`, a `1` that lifts the selected site's card over the fleet's own markers rather than over the bar — never was a member, because it **arrived** with its observer (`map-regressions.spec.ts` polls `elementFromPoint` where card and marker overlap after a zoom-out reclusters, and deleting the rule hands that pixel to a cluster bubble), which is this entry's fix direction being applied at the moment the declaration was written rather than retrofitted, `apps/web/src/main.tsx` (its `import './header/header.css'` line, in the ordered block that file's header comment owns), `apps/web/src/map/map.css` (the `background: var(--color-surface-veil)` declaration on `.map-attribution`), the credit-link rules in `packages/ui/src/attribution/attribution.css` (`.cumulo-attribution a`) plus their twin `.map-attribution-tiles a` in `map.css`, and the anchoring block on `.site-popover` in `apps/web/src/map/site-popover.css` (its `position`/`bottom`/`left`/`transform` quartet, and the border-plus-opaque-surface it separates itself from the tiles with — a treatment now chosen rather than settled for, since #284 D6 put elevation ink in the scale as `--color-shadow` — though not on the chart tooltip's terms, because that shadow is a `filter: drop-shadow()` and `filter` is on no list in `stylelint.config.mjs`, so it was never judged, while a card `box-shadow` is judged sub-value by sub-value and confined to bare `var()`/`0` offsets; that file's own comment carries both the constraint and the argument for keeping the border)
- What: all five are load-bearing and all five are invisible to every gate. Taking the first four here and the fifth in its own paragraph below: delete either of the two unobserved `z-index` values and that popup paints under maplibre's positioned controls — the search's listbox hangs directly over the map, so it is the most visible to lose (the info tip's identical declaration is the one with an observer, above, as is the site card's anchor at its own smaller value — two of four watched, two not); delete the import and the entire header renders unstyled; swap the veil back to an opaque `--color-surface` and the credits stop being an overlay in the only way that matters visually; revert the credit links to `--color-accent` without the underline and they drop below AA on the veil while losing the only non-colour cue that marks them as links — and in each case `pnpm verify` and the full browser lane stay green, because nothing asserts these declarations — the unit tests that do read stylesheets (`map-css-contract.test.ts` over `map.css`, and `tokens.test.ts`'s block parser over `tokens.css` — the text-contract shape already has two instances) assert other rules, and the lane's one paint-order assertion (`panelIsOnTop`) watches the info tip alone. (The lane's `toBeVisible` checks pass on an unstyled or overlapped element; visibility is not stacking; the `composition.spec.ts` band-geometry case measures the band's _box_, which neither an opaque background nor a link colour moves; and its trial-click is deliberately ink-agnostic.) This is the same residual class `apps/web/src/map/map-css-contract.test.ts` documents in its header comment for `map.css` — declarations that carry a design obligation, provable as text in the unit lane or as computed style in a real browser, and asserted by neither today. The **credit-link treatment** is the sharpest of these four, because it is the whole substance of a documented decision: three files describe it (`attribution.css`'s header, `docs/design/map-treatment.md`'s Attribution section, and `tokens.css`'s validation record), so a silent revert would leave all three describing a treatment the app no longer has, with a WCAG 1.4.1 regression underneath. Fix direction: extend the text-contract idea to cover the sheets that have obligations (a shared helper rather than a second bespoke parser), or add browser-lane assertions on computed style — the popover's stacking against a map control (the shape `info-tips.spec.ts`'s `panelIsOnTop` now demonstrates for the tip — partly discharging this entry's own fix direction), the band's resolved `background-color` alpha, and one `getComputedStyle` read of the credit link's `color` and `text-decoration-line`, which is the cheapest of those reads. Worth deciding once for all five rather than growing another copy of the pattern.

  The **site card's anchoring** is the fifth member, the newest, and the one with the most riding on it: that quartet of declarations is the entire reason the card sits _above_ its marker rather than on top of it, and nothing asserts any of them. Swap `bottom` for `top` and the card covers the mark that says which site it is about; drop the `translate` and it hangs off to one side of its own coordinate; drop the border and it reads as ink floating on tiles. Every gate stays green through all three, and the browser lane's cases for this card assert focus, text, and — since #284 C2 — which of the card and a marker paints on top, none of which is where the card sits. It also has a measured consequence already recorded separately (the card clipped at the map's top edge), which is what makes the geometry worth an observer rather than a comment.

- Source: #265 review cycle 1 (header shell; veil member added cycle 1, link-treatment member cycle 2 — both layout; site-card anchoring member added by the selection rework's cycle 1; the search listbox's stacking folded into the first member by the header search's cycle 1; the info tip's stacking folded into the same member by #265 C10, then split back out as observed by PR 6's hit-test case; census trued to four values, and the elevation-token claim retired, by #284 review cycle 1)

## 2026-08-05 — The credits band occludes markers across the map's full width

- Where: `.map-attribution` in `apps/web/src/map/map.css` (its `position: absolute` / `inset` rule) against the marker buttons `SiteMarkers` mounts; the evidence is the covered-point predicate in `basemapPoint` in `apps/web/e2e/map-regressions.spec.ts`, which now has to treat `.map-attribution` as a covering overlay alongside `.maplibregl-marker`
- What: moving the credits from a strip under the map to a band over it (#265) put an opaque-enough, pointer-taking element across the full width of the map's bottom edge — roughly one text line, two when it wraps. Positioned elements paint above the static children, so any site marker whose centre falls in that band is unclickable and unhoverable: the pointer lands on the credits. The reach is not lost — markers stay in the tab order and the site table under the map is the table view of the same states (`docs/design/map-treatment.md` makes that relief rule explicit) — so this is a pointer-affordance gap rather than an unreachable site, which is why it is recorded rather than patched. It surfaced as a real red: the basemap-click case started clicking the band instead of the map, and the helper was generalised to route around it, which fixes the _test_ and leaves the _product_ behaviour exactly as described here. Wanted: a design decision, not a patch — accept and record it in the treatment (markers near the bottom edge are reached by list or keyboard), or inset the band from the map's edges so it covers a corner rather than a full-width strip, or shrink it to a single line that never wraps. Each changes what `map-treatment.md`'s Attribution section promises, so it belongs with whoever owns that section rather than in a layout chunk. **Update (#284 C12):** the third option has been taken as far as it goes — the band now drops each credit's leading prose below 37.25rem and holds one line from ~25rem up, where it used to wrap to two (and at 360px, to three). That shrinks the occluded strip to a single line across every width a reader is likely to be at, but it does not close this entry: a one-line band still spans the map's full width and still swallows the markers under it, which is the actual defect. The two remaining options are untouched, and the compact form is now part of what that section promises.
- Source: #265 review cycle 1 (layout)

## 2026-08-05 — Light-mode `--color-accent` is below AA for small text on its own surfaces

- Where: `--color-accent` in `packages/ui/src/tokens/tokens.css` (both blocks), and the accent-coloured link rules that remain — `.about-dialog a` in `apps/web/src/header/header.css` is the live one; the measurement is recorded in `tokens.css`'s own validation header
- What: light `--color-accent` #2a78d6 measures 4.30:1 on `--color-surface` and 3.92:1 on `--color-bg` — below WCAG AA's 4.5:1 for small text, which `--text-xs` link text certainly is. This predates #265 and is app-wide; the palette's approved record passes because the `dataviz` validator's checks are about categorical separation and the surfaces it was run against, not about small-text AA for the accent hue. The credits half of this is **resolved** and is not what remains: #265 decoupled both credit links from the accent (muted ink plus a permanent underline, `packages/ui/src/attribution/attribution.css` and `.map-attribution-tiles a`), which clears AA on the veil and would have been the right treatment on an opaque surface too. What is left is every other accent-on-surface link, the About dialog's source list being the one that ships today — so the dialog now renders its own composed Open-Meteo credit in the new treatment beside accent links in the old one, a visible mismatch that resolves when this does. The trap worth keeping for whoever picks it up: tuning a translucent surface upward cannot rescue an ink that already fails on the opaque version of that surface — the mix's ceiling is 4.30:1, so the ink has to change, not the background. Wanted: darken `--color-accent` in light mode until it clears 4.5:1 on both surfaces and re-run the palette validation, or adopt the underlined-muted treatment app-wide for small-text links, or record an explicit AA exception. Design-system decision with app-wide reach, which is why #265's layout chunk fixed only the links it had made illegible.
- Source: #265 review cycle 1 (layout)

## 2026-08-05 — The token gallery renders two colour tokens as chips with no colour

- Where: `apps/web/src/preview/TokensHarness.tsx` (the swatch list and its `[data-token='--name']` keyed rules in `apps/web/src/preview/preview.css`), for `--color-surface-veil` and `--color-focus-ring`; the test that does not notice is `TokensPreview.test.tsx`
- What: the gallery's whole job is to display the design system, and two colour tokens are displayed as an empty chip — `preview.css` has no `[data-token='--color-surface-veil']` or `[data-token='--color-focus-ring']` rule painting the swatch, so the name renders with a blank square beside it. `TokensPreview.test.tsx` checks that every token _name_ appears, which is why the gap is silent: the page is complete by the only measure anything takes of it. The veil makes it worse than cosmetic, because a veil is precisely a token whose point is what it looks like over something — a flat chip could not show it even if one existed, so the honest rendering is a swatch over a patterned or image backdrop. Wanted: paint the two missing swatches (the veil over a backdrop that reveals its translucency), and strengthen the harness test from "every name is listed" to "every listed colour token resolves to a non-empty computed background" — the second half is the ratchet that stops the next token joining them. Note the test half may need the browser lane: jsdom applies no stylesheet, so a computed-background assertion has nowhere to run under vitest.

  The next token has since arrived, and it is why the count here is still two rather than three: #284 D6 added `--color-shadow`, the gallery drew it as a third blank chip exactly as this entry predicted, and #284's review cycle 1 caught it by reading this entry rather than by any gate — a rule painting that swatch now sits in `preview.css` beside the others. So the paint half is one token nearer done and the ratchet half is untouched, which is the half that matters: nothing about _why_ a blank chip is silent has changed, and the token after this one will be found the same way, by somebody happening to look.

- Source: #265 review cycle 1 (layout); `--color-shadow`'s arrival painted, and the prediction recorded, at #284 review cycle 1

## 2026-08-05 — The contrast record's literals travel: two stylesheets restate tokens.css's measured ratios with no ledger

- Where: the owner is the validation record in `packages/ui/src/tokens/tokens.css` (the per-mode accent/muted/text ratios); restatement carriers are `packages/ui/src/attribution/attribution.css` (its header quotes `3.49:1`/`3.76:1`/`4.30:1`) and `apps/web/src/header/header.css` (the about-dialog rule quotes `4.30:1`/`4.89:1`)
- What: both carriers name `tokens.css` as the owner, which is half of rule 9; the other half is a ledger beside the record naming the copies, so a re-measurement (a palette change, a veil-mix change, the accent issue's resolution) finds every stale ratio at plan time instead of shipping prose that contradicts the record. `docs/design/map-treatment.md` shows the compliant form (defers the numbers to tokens.css entirely). Clusters with the other rule-9 ledger entries above; fix in the same cross-cutting pass, or strip the literals from the two carriers.
- Source: #265 review cycle 3 (layout)

## 2026-08-05 — The app's two native dialogs disagree about whether a modal names itself

- Where: `apps/web/src/add-site/AddSiteDialog.tsx` (the `<dialog className="add-site-dialog">` element) against `apps/web/src/header/AboutDialog.tsx` (which sets `aria-labelledby` from its own `useId`); the heading that would name the add-site one is `AddSiteForm`'s `.add-site-title`, whose id is generated by that component's `useId` and not exposed
- What: the add-site dialog carries no accessible name, so a screen reader announces it as an unnamed dialog; the About dialog beside it announces its heading. Two implementations of the same pattern now answer the same question differently, which is the part that makes this debt rather than a one-off gap — the next dialog copies whichever it lands on. The immediate reason is structural rather than an oversight: `AddSiteDialog` wraps a form it does not own the heading of, and the two available workarounds are both worse than the gap — an `aria-label` here would be a second copy of the words "Add a site" free to drift from the heading it restates (`structure.md` rule 7), and reaching into the form for its generated id is not a surface the form offers. The clean fix is `AddSiteForm` accepting a `titleId` (or exposing one), which makes the dialog's `aria-labelledby` point at the real heading and costs one prop; it was out of scope for #265 C6, which was scoped to wrap the form unchanged. Mitigated but not closed today: the form's mount effect focuses that heading, so the subject is the first thing announced on arrival. Worth settling as one convention for both dialogs rather than per component, since the a11y-linting gap logged above would not catch either.
- Source: #265 review cycle 1 (map controls)

## 2026-08-05 — `rotateMap`'s vertical stroke is hard-coded upward and can leave the viewport

- Where: `rotateMap` in `apps/web/e2e/map-regressions.spec.ts` (the `start.y - DRAG_DISTANCE_PX / 2` end-point), beside `dragMap`, which picks both directions from the viewport half
- What: the rotate gesture's horizontal direction adapts to which side of the viewport the start point sits in, but the vertical component always strokes upward; with the corner search currently returning top-left (both bottom corners sit under the credits band), the path ends above the map box and off the top of the default viewport. It passes today, and the pre-reset poll ("the camera never rotated") turns any regression into a loud failure rather than a false green — so this is robustness, not a defect. Mirroring the `x` direction choice on `y` removes the assumption. Take it next time the spec's helpers are open.
- Source: #265 review cycle 2 (map controls)

## 2026-08-05 — A selection change is one commit ahead of the state that answers for it: a wasted request, and one frame of the previous site's numbers under the new site's label

- Where: the `selectionReady={forecast.status === 'ready'}` expression in `Dashboard`'s render (`apps/web/src/dashboard/Dashboard.tsx`), the overlay query it gates and the `overlayState` call that labels the result in `FleetPanel` (`apps/web/src/dashboard/FleetPanel.tsx`), the run-reset at the head of `useFirstForecast`'s polling effect (`apps/web/src/data/use-first-forecast.ts`), and `useFleetQuery`'s own effect (`apps/web/src/data/use-fleet-query.ts`), which sets `loading` from an effect rather than during the render that changed the key
- What: `useFirstForecast` resets its state to `checking` inside the effect that starts a run, not during the render that changed `siteId`. So for one commit after a selection moves from a site whose forecast was ready, the hook still reports `ready` — about the _previous_ site. The dashboard passes that stale `true` down as `selectionReady`, and `FleetPanel`'s overlay query sees the new site's id in its key with the gate already open: it fires a request for the new site, and then fires a second one when the reset lands and re-opens the gate. One wasted per-site request per selection change from a ready state, the creation path included. It is invisible today because nothing renders a spinner for the overlay and the superseded answer is dropped by `useFleetQuery`'s own supersession rule — the cost is a metered request against the frugality budget in CLAUDE.md, not a wrong chart, which is why it is recorded rather than patched under a review. Papering over it in `FleetPanel` (comparing the site the ready state belongs to against the selected one) would put a second copy of "which site is this state about" in a component that should not have to ask; the real fix is in the hook, which should reconcile with the `siteId` prop **during render** — react.dev's own adjusting-state-on-prop-change pattern — so `checking` is what every consumer sees in the same commit the selection changes. That is a change to a hook three surfaces read, with its own suite to re-argue, and it belongs with whoever next opens `use-first-forecast.ts` rather than inside a chart chunk.

  **The same lag has a second, visible face in `useFleetQuery`, and it is the same fix.** That hook also resets to `loading` from its effect rather than during the render that changed `key`, so for one committed frame after the selection moves its state still holds the _previous_ site's forecasts while `FleetPanel` has already rebuilt the label from the _new_ `selectedSite`. `overlayState` composes the two, so that frame draws site A's numbers in the legend, the tooltip and the table column under site B's name. The comment on the overlay query — that keying on the site "drops the previous site's answer rather than letting it land on the chart under the next site's name" — is true from the effect onwards and not before it; the request really is superseded, but the render in between is not. It is one frame, immediately corrected, which is why it is recorded rather than blocking: a reader sees a flicker at worst. Recorded because a chart that is briefly, confidently wrong about whose numbers it is showing is a bad failure mode for an accuracy product, and because the same class of bug at a slower paint or under a React scheduling change stops being one frame. The local guard is available and deliberately not taken: comparing the site an answer belongs to against the selected one inside `FleetPanel` puts a second copy of "which site is this state about" in a component that already receives that fact twice — the same objection as above, and the same answer, which is that the hooks should reconcile with their inputs during render so no consumer can observe the gap.

- Source: #265 review cycle 1 (selection rework); label-lag face added cycle 2

## 2026-08-05 — The header search answers "No matching sites" while the fleet is still loading, or has failed to load

- Where: `SiteSearch` in `apps/web/src/header/SiteSearch.tsx` (its `sites` prop and the `NO_MATCHES_LABEL` arm of the popup), fed from `AppHeader` (`apps/web/src/header/AppHeader.tsx`) out of `Dashboard`'s `sites` (`apps/web/src/dashboard/Dashboard.tsx`), which is empty in the `loading` and `failed` arms of `FleetLoad`
- What: the search takes a plain array and has no way to tell "the fleet has no such site" from "the fleet is not here yet" or "the listing failed" — all three are an empty filter result, and all three are answered with a definitive negative. A reader who types before the listing resolves is told their site does not exist, and if the listing failed they are told it for the rest of the session while the panel below explains the real reason. This is the app's async-surface convention (`react.md`) being bypassed rather than a missing string: every other surface distinguishes pending, failed and empty, and this one collapses the three into the empty arm. Invisible on the demo source, whose listing resolves in the same tick — which is exactly why it is worth logging rather than trusting to be noticed: over HTTP it is the first thing a fast typist meets. Fix direction is a load state reaching the header rather than a bare array (the `FleetLoad` union is already the right shape and already lives one component up), plus a pending and a failed word owned by `dashboard/state-copy.ts` like every other one. It is bigger than a wording change — the prop's type, the dashboard's plumbing and the copy module all move together — which is why it is here and not in this diff.
- Source: #265 review cycle 1 (header search)

## 2026-08-05 — Re-selecting the site that is already selected is a complete no-op, and the search is where a reader cannot tell

- Where: the `selectSiteForReader` helper and the `selectedSiteId`/`selectionOrigin` state it sets in `Dashboard` (`apps/web/src/dashboard/Dashboard.tsx`), `SelectionCamera`'s effect dependencies (`apps/web/src/map/SelectionCamera.tsx`), and the mount-effect focus in `SitePopoverCard` (`apps/web/src/map/SitePopoverCard.tsx`), which is keyed on the card mounting rather than on a selection event
- What: selecting the already-selected site changes nothing, three times over: React bails out of a `useState` set to the identical value, `selectionOrigin` is already `'reader'`, so the camera effect's dependencies are unchanged and it does not re-run, and the card does not remount so it does not re-take focus. Harmless for the openers that existed before — pressing a marker you can see, or a row that is visibly current — because in all of those the reader can see the selection is already theirs. The search is the first opener where they cannot: the popup is over the map, the card may be off screen behind it, and the reader has no cue that the name they just typed is the site already selected. So the gesture reports success and does nothing at all: no camera move back to a site the reader has since panned away from, no focus move to the card, nothing. Fix direction is a selection _event_ rather than a selection _value_ — the camera and the focus both want "a reader asked for this site just now", which a monotonically increasing request token beside the id would give them, and which the id alone cannot. That touches the selection state, the camera and the card together, and is a design decision about what re-asking means rather than a patch, which is why it is logged.
- Source: #265 review cycle 1 (header search)

## 2026-08-05 — The search shows a capped number of matches and never says there were more

- Where: `MAX_VISIBLE_MATCHES` and the `slice` in `matchingSites` (`apps/web/src/header/SiteSearch.tsx`), against `Dashboard`'s `sites` ordering (`apps/web/src/dashboard/Dashboard.tsx`: the listing followed by anything created this session)
- What: the popup is capped, the cap is silent, and the cut is arbitrary from the reader's side. Typing a common substring — "rooftop" matches every site the demo fleet has — offers a handful with nothing to say how many more matched, so a reader who does not find their site concludes it is not there rather than that they need a narrower query. The order the cap cuts along is fleet order, which makes the omission systematic rather than random: the sites a reader created this session are appended last, so they are the first to fall off, and a reader searching for the site they just placed is the likeliest person to hit the cap. Two things want deciding together: a count line ("showing N of M", worded and owned under the `state-copy.ts` convention rather than spelled beside the JSX), and whether the ordering should put better matches first (a name that starts with the query before one that merely contains it) so the ones that survive the cut are the ones worth showing. Recorded rather than fixed because the copy has an owner it should go through and the ordering is a ranking decision, not a slice.
- Source: #265 review cycle 1 (header search)

## 2026-08-09 — The app holds three incompatible verdicts on `calc(var(--space-1) / 4)`, and no gate can see any of them

- Where: the two comments that contradict each other are both in `apps/web/src/charts/charts.css` — the block above `.forecast-chart-table thead th` ("a hairline is a hairline, not a quarter of a spacing step") and the block above `.forecast-chart-tooltip-panel` ("the quarter step is a _distance_ here"). The silent majority is the app's ordinary border idiom: `calc(var(--space-1) / 4)` as a `border-width`/`border-bottom-width`/`border-top-width` in `apps/web/src/app.css` (`.app-header`, `.app-failure`), `apps/web/src/header/header.css` (six rules), `.map-site-marker-tooltip` in `apps/web/src/map/map.css`, `.site-popover` in `apps/web/src/map/site-popover.css`, `apps/web/src/info/info.css`, `apps/web/src/add-site/add-site.css`, `apps/web/src/dashboard/panel-states.css` and `site-table.css`, and `.swatch-chip` plus the `.chart-table thead th` rule in `apps/web/src/preview/preview.css` — with the half-step variant on `.map-site-marker::before` and `.map-cluster-marker` in `map.css`, on `.map-marker` in `preview.css`, and on the focus ring's `outline-width` in `packages/ui/src/styles.css` (whose `outline-offset` takes the quarter step). The dissenters are `.forecast-chart-table thead th`'s plain `1px` and `.forecast-chart-tooltip-panel`'s `stroke-width: 1` beside it. The scale is `--space-1` in `packages/ui/src/tokens/tokens.css`; the reason no gate is involved is the length-half residual documented in `stylelint.config.mjs`'s `declaration-property-value-disallowed-list` note — `border-width`, `outline-offset`, `stroke-width` and shadow offsets are all off the allow-list.
- What: three positions on the same expression coexist, and the app cannot be right in all three. Roughly twenty declarations spend it as a border width with no comment at all; `charts.css`'s table-header rule refuses to, on the grounds that doing so "borrowed a token that means 'gap between things'" to express a thickness and "would change thickness if the spacing scale were ever retuned", and writes `1px` instead; `charts.css`'s tooltip rule, ten rules above it in the same file, endorses it as a shadow offset on the grounds that "the quarter step is a _distance_ here, which is what a spacing token means". The endorsement does not survive the objection it distinguishes itself from: a retune of `--space-1` moves the shadow's offset exactly as it would move a border's thickness, and the tooltip's lift is no more a "gap between things" than a hairline is — so the distinction separates two spendings that behave identically under the only change either comment worries about. The scale of the majority is what makes this debt rather than a two-line disagreement: the objection, taken seriously, indicts the app's default border idiom everywhere, and the one file that acted on it is now the outlier. Nothing mechanical is watching either way — the quarter-step calc, a bare `1px` and a token that does not exist yet are all equally green — so the verdicts can drift further apart indefinitely, and each new stylesheet picks whichever neighbour it copied. The resolution is a design-system decision rather than a rewording: either a hairline/offset category in the token set (which is what both comments are really asking for, and what `stylelint.config.mjs`'s residual note says closing the length half would require) or one written rule about when a spacing step may be divided, applied to every site above. Recorded rather than fixed because #284's polish diff owns comments and one token, not a token category. `charts.css`'s table-header comment already points at this log for "why the gate cannot see the difference" and until now had no entry to land on; this is that entry.
- Source: #284 PR group A, review cycle 2

## 2026-08-09 — D7 turned a pointer sweep from zero renders into thirty a second, and only the tooltip's four rows are memoised against them

- Where: `useChartHover` in `apps/web/src/charts/chart-hover-input.ts` (its `trackPointer`/`closeFrame` pair and the `HoverSelection` object they set) against everything `ForecastChart`'s returned `<figure>` builds on each of those commits (`apps/web/src/charts/ForecastChart.tsx`): the mark generators (`bandElements`, `boundElements`, `medianElements`, `overlayElements` and `actualsElements` in `forecast-chart-marks.tsx`, plus `gridElements`, `horizonElements` and `xAxisElements`, which #284 D15 moved out to `forecast-chart-axes.tsx` — `xAxisElements` because pass 3's two-tier axis (D9/D10) replaced the `xLabelElements` this entry was first written against, which is the second re-truing this one `Where:` line has needed and a reminder that a location list naming a dozen symbols ages symbol by symbol rather than all at once), `forecastChartLegend` (`forecast-chart-legend.tsx`), and `forecastChartTable` (`forecast-chart-table.tsx`), which emits one row per point at five columns — six with an overlay — over the windows `RANGE_OPTIONS` in `apps/web/src/dashboard/range-picker.tsx` offers (24, 48 and 168 hours; `DEFAULT_RANGE` in `FleetPanel.tsx` opens on 24). What _is_ memoised is `TooltipPanel` in `apps/web/src/charts/forecast-chart-hover.tsx` and the two `useMemo`s in `ForecastChart`. The claim that overstates that coverage is the "**The panel follows the pointer; the data snaps**" paragraph in `forecast-chart-hover.tsx`'s module docblock — "a frame that only moves the panel cannot re-render a single row".
- What: before D7 the hover state was a bare `useState<number | null>` in `ForecastChart` (still visible on `origin/main`), so a pointer sweeping inside one sample's span set the same index repeatedly and React's eager-state bailout made that **zero** re-renders. D7 replaced it with a `{ activeIndex, pointerX }` object set afresh on every frame, so the same sweep is now one commit per `POINTER_FRAME_MS` — by design, since a continuously tracking panel cannot be driven by a value that only changes at sample midpoints. The memoisation D7 added is real but narrow: it covers the tooltip's own rows (at most four) and nothing else in the figure, so each frame still rebuilds every mark generator, the legend, and a table of up to 168 rows × 6 cells — the largest structure on the surface — to move one `transform`. React reconciles those to no DOM change, which is why nothing is visibly wrong; the cost is element construction and reconciliation, bounded by the throttle, on a surface a reader hovers deliberately. Correct behaviour and a bounded cost, not a bug — logged because the docblock sentence above reads as covering the figure when it covers the panel, and because the fix is structural rather than local: either the hover state moves into a child wrapping only the SVG chrome that actually depends on it (so the legend and the table sit outside the re-rendering subtree entirely), or the marks, legend and table each gain a memo — which for the legend and the table means making them components first, since both are plain functions returning elements today and `memo` has nothing to attach to. Worth doing as one decision about where the hover boundary belongs, not three `useMemo`s bolted on.

  **Half-collides with [#293](https://github.com/TomBennett-Lloyd/cumulo/issues/293)** (`FleetPanel` re-aggregates the whole fleet on every render), and the half that does _not_ collide is the useful part: the hover state lives inside `ForecastChart`, and `FleetPanel` is its parent, so pointer frames stop at the `<ForecastChart>` boundary and never re-run `aggregateFleetForecast` or `joinFleetSeries`. The two are the same hot path and the same fix family — decide where memo boundaries go on the chart path once — but neither multiplies the other, and this entry is not evidence that #293 is worse than it was measured to be.

- Source: #284 PR group A, review cycle 3

## 2026-08-09 — Every `cumulo-series` volume figure in the repo prices two models, and only one of them has a producer

- Where: the `series` row and the "`series` row at all three fleet sizes" note in the `### Storage stack` cost section of `infra/README.md`, the matching per-cycle figures in the `### Forecast stack` section (its DynamoDB row and the section preamble's "on the order of 240 `cumulo-series` items"), the cost paragraph of section 2 in `infra/storage/tables.tf`, and the 2026-08-09 Amendments entry of `docs/adr/0002-storage-split.md` (its "~240 write units" burst figure carries no physics-only caveat, unlike the `tables.tf` copy); the producer is `locationForecasts` in `apps/forecast/src/location-forecasts.ts`, which calls `createPhysicsForecast` and nothing else
- What: every series volume in the repo derives from ADR 0002's `48 × 2 = 96` forecast items per site per cycle — 48 horizon hours across the `physics` and `ml` variants of `forecastModelSchema`. Only the physics variant has a producer: the ML correction layer is a schema enum member and a ticket, not code. So the deployed system writes about **half** the items every one of these figures prices, and the ≈ $1.48/month the storage cost table now carries for `series` is really ≈ $0.74 until the ML producer ships. The direction is conservative — the platform bills less than the document claims, and the figures become correct rather than wrong the day the second model lands — which is why it is recorded rather than corrected inside a billing-mode change. It is not free, though: the same doubling runs through the burst arithmetic (~240 write units per queue message rather than ~120) and the fleet-headroom rows, so a reader sizing anything against these numbers is sizing against the planned fleet and should be told which. The fix is a pass over the forecast and storage cost sections stating the physics-only figure and the both-models figure side by side, the way the weather row already prices three fleet sizes — it belongs with whoever next opens those cost tables, or with the ML producer ticket, which retires the gap rather than documenting it
- Source: PR for #258 review cycle 1

## 2026-08-09 — The infra README still describes #136's forecast consumer as unbuilt, in four places, while it is deployed and driving a bill

- Where: `infra/README.md` — the ingestion runbook's queue-drain expectations and its "whether to leave it up" teardown paragraph, the forecast runbook's introduction, and the section that reasons about who consumes the queue (four sites, all phrased as "until #136 lands" / "while #136 is being built" / "nothing is consuming")
- What: #136's event source mapping and forecast function are merged, applied, and were the writers in the throttle incident this PR responds to — the consumer is not pending, it is what drained the queue and metered the write units. The framing survives from when the queue genuinely had no consumer, and it now contradicts material in the same paragraph: the teardown note that says nothing consumes the queue until #136 lands also states, correctly, that an enabled schedule meters ≈ $1.48/month of `series` writes downstream of that consumer. No _number_ here is wrong — the arithmetic and the cost tables are current — so it is a reading hazard rather than an operational one: an operator deciding whether to leave the ingestion stack running is told the queue fills harmlessly, which stopped being true when the consumer shipped. Inherited rather than introduced by this diff, and cross-cutting: the honest fix is one pass over the file replacing pending-#136 framing with the deployed topology, which is a docs ticket rather than a clause in a billing-mode PR
- Source: PR for #258 review cycle 1

## 2026-08-09 — Every range press re-spends the whole forecast fan-out, on a forecast half that cannot answer differently

- Where: the two `useFleetQuery` keys in `FleetPanel` (`apps/web/src/dashboard/FleetPanel.tsx`) — the `'fleet-forecasts'` key and the `'fleet-actuals'` key, both of which carry `range` — against the control that moves `range` (`RangePicker` and its `RANGE_OPTIONS` in `apps/web/src/dashboard/range-picker.tsx`) and the gate that now offers that control to the deployed source (the `fleetLookback || fleetActuals` expression in the same component, #284 D5). What the forecast key re-fires is `fleetForecasts` on `apps/web/src/data/http-fleet-data-source.ts`, paced by `apps/web/src/data/request-pacing.ts`; its per-site cost is stated in `FleetPanel.tsx`'s own docblock, and the horizon that caps what a fan-out can return is ADR 0002's (`docs/adr/0002-storage-split.md`)
- What: `range` sits in both query keys, so a range press re-asks both reads. For the actuals half that is exactly the point — a wider window genuinely is more measured hours. For the forecast half it is close to pure waste: the fan-out serves hours ahead of the clock and is capped by the pipeline's horizon, so every option beyond that horizon returns the same rows and draws an identical plot, bought with one paced request per site in the fleet. D5 is what turned this from theoretical into the deployed path: before it, a source with `fleetActuals` and no `fleetLookback` — which is precisely what the live source is — had no picker at all and was pinned to the default window, so nothing could move `range`. Now the one control that panel offers spends the most expensive request the panel makes, on every press, to change something only the cheap request can answer. Not a correctness bug (the chart is right, and the refetch is idempotent) and not a regression in the fan-out's own pacing — it is query-key granularity being coarser than the question being asked. **[#296](https://github.com/TomBennett-Lloyd/cumulo/issues/296)'s batched fleet route, in flight, prices this down to one metered request per press**, which removes most of the cost but not the question: an identical forecast half is still re-fetched and re-summed on a press that could not change it. The fix direction is to key each read on what it actually varies with — the forecast on the horizon it can serve rather than on the reader's chosen window, the actuals on the window — which is a decision about what a range means per series, and belongs with #296's route rather than inside a polish diff. Cross-reference #296
- Source: #284 pass 3 wave B, review cycle 1

## 2026-08-09 — Since D3 the forecast chart is a keyboard tab stop in every state, including the ones with nothing plotted

- Where: the `tabIndex={0}` on the `<svg className="forecast-chart">` that `ForecastChart` returns (`apps/web/src/charts/ForecastChart.tsx`), together with the `readAtFocus`, `readAtKey` and `clearReadout` handlers bound beside it and the `role="img"` name it carries; the states that now reach it are the loading and failed arms of `stateContent`, the empty arm of `readyContent`, and `emptyFleetBody`, all in `apps/web/src/dashboard/fleet-panel-body.tsx` (#284 D3), whose figure-in-every-state contract is pinned by `apps/web/src/dashboard/FleetPanel.structure.test.tsx`
- What: the chart was made unconditionally focusable back when it was only ever rendered with data on it. D3 made the figure structural — it is on screen while the fleet is loading, after the fan-out failed, for a forecastless fleet and for a fleet with no sites — and the `tabIndex` came along unchanged, so a keyboard reader now tabs into an interactive element that announces an image and has nothing behind it. The behaviour is benign rather than broken: `readAtFocus` selects sample 0 of an empty series, `activePoint` is then `undefined`, and the live region renders empty, so nothing wrong is spoken and nothing throws. What it costs is a tab stop that promises an explorable plot and delivers silence, in exactly the states where the prose beside it has already said there is nothing to explore. It is logged rather than patched because the question is a pattern one: the hover target rect and the table twin's disclosure are unconditional in the same way and equally empty in those states, so "an affordance is offered when there is something behind it" is a rule the chart should hold in one place rather than in three. Gating `tabIndex` on whether any points exist is the obvious move and is deliberately not taken here — it makes focusability a function of the data, which needs a decision about what happens to a chart that gains its first point while focused, and about whether the tab stop should instead persist and describe its own emptiness. Belongs with whoever next opens the chart's interaction surface
- Source: #284 pass 3 wave B, review cycle 1

## 2026-08-09 — The chart's `Raw data` disclosure label is spelled in three places and owned by none of them

- Where: the `Raw data` literal in the `<summary className="forecast-chart-summary">` that `forecastChartTable` renders (`apps/web/src/charts/forecast-chart-table.tsx`), against `apps/web/src/charts/chart-copy.ts` — the declared owner of "the words a plot prints about its own frame", holding `TIME_COLUMN_HEADER` (and, until #284 D10 discharged the clock obligation through the time axis's own title, a second `CHART_CLOCK_LABEL` beside it), whose docblock scopes the data-naming headers out — and the restatement of the literal in the "**The twin is folded away, behind a `Raw data` disclosure**" bullet of `docs/design/chart-treatment.md`. The same question one component over is the fleet table's own inline summary in `apps/web/src/dashboard/SiteTable.tsx`
- What: `chart-copy.ts` exists to own chart chrome wording and draws its own boundary explicitly — the `Power (kW)` axis title and the P10/Median/P90/Actual headers stay in the components that render them because they _name the data_, while the words a plot prints about its own frame live in the module. (#284 D10 moved the time axis's title to the other side of that line, where it now consumes `TIME_COLUMN_HEADER`, on the ground that it names a clock rather than a quantity — which sharpens the boundary this entry says is ambiguous without settling the question it asks.) `Raw data` falls on the module's side of that line as drawn (it names a disclosure, not a plotted quantity) yet is spelled inline, and the treatment doc quotes the literal a third time, which is the shape `architecture.md` rule 9 is about. Nothing is currently wrong and the drift is bounded: `forecast-chart-details.test.tsx` asserts the summary's exact words, so a rename fails loudly in at least one place, and a stale doc line is a reading hazard rather than a defect. What makes it worth recording is that the boundary is genuinely ambiguous rather than merely unenforced — `chart-copy.ts`'s scope note was written before the twin had a disclosure to name — and the two honest resolutions point in opposite directions: move the literal into `chart-copy.ts` and let the doc name the constant (rule 9's ordinary shape), or amend that module's scope note to say disclosure labels belong to the component and let the doc keep the literal deliberately. Choosing is a copy-ownership decision across the chart surface rather than a rename, and it should settle `SiteTable.tsx`'s summary in the same pass
- Source: #284 pass 3 wave B, review cycle 1

## 2026-08-09 — One chart is named three times, in three independent spellings, and a screen reader hears two of them back to back

- Where: the `<h2 className="fleet-panel-title">` literal in `FleetPanel`'s header row (`apps/web/src/dashboard/FleetPanel.tsx`), against the two `ariaLabel` arms and the two `tableCaption` arms of `chartCopy` in `apps/web/src/dashboard/fleet-panel-copy.ts`. The window text each of those interpolates comes from `windowLabel` in the same module, which _is_ owned and shared; the product name wrapped around it is not
- What: the panel names the same chart in three places that cannot see each other, and the three have already drifted in case — the heading and both `ariaLabel` arms capitalise it, both `tableCaption` arms do not. #284 D3's heading rename created the third spelling: before it the name existed in the copy module's arms alone, and moving a title into the header row put a bare literal beside them without a constant to hang it on. Nothing is broken and no gate can see it, which is the shape this shares with the `Raw data` entry above — an unowned string in a component, beside a copy module that exists precisely to own that component's words. The reader-facing edge is the part worth recording rather than the duplication itself: a screen reader moving through the panel hears "Fleet forecast, heading level 2" and then, on the figure immediately below, an image named "Fleet forecast and simulated actuals, past 24 h and the forecast ahead" — the same name twice in a row, the second time longer, with nothing to say they are one thing rather than two. That is a question about what the chart's accessible name should say once the heading beside it already says the subject (a name that adds only the window and the series would read better and is shorter), not a question about deduplicating a literal, which is why it is logged rather than patched into a rename. **Resolves in the same copy-ownership pass as the `Raw data` entry above**: both are "a component spells a word its copy module should own", and both need the module's scope note amended or the literal moved before either is a mechanical change. Cross-reference that entry
- Source: #284 pass 3 wave B, review cycle 2

## 2026-08-09 — Does every equally-stacked overlay owe an occlusion case, or does one representative discharge them all?

- Where: the app's four stacking declarations and which of them anything measures — `.header-menu-popover` in `apps/web/src/header/header.css` (measured, by `panelIsOnTop` in `apps/web/e2e/header.spec.ts`), `.site-search-listbox` in the same file (**not** measured, though it is absolutely positioned under the same bar over the same maplibre canvas at the same value), `.info-tip-panel` in `apps/web/src/info/info.css` (**not** measured — that file's own comment says so outright), and `.site-popover-anchor` in `apps/web/src/map/site-popover.css` (measured, by the reclustering hit test in `apps/web/e2e/map-regressions.spec.ts`). The prose that now names the gap rather than papering over it is the census clause in `header.css`'s `.header-menu-popover` block and its two counterparts in `apps/web/e2e/header.spec.ts` and `apps/web/e2e/info-tips.spec.ts`
- What: **this updates the "Stylesheet facts with no observer" entry above rather than joining it as a second copy — read them together, and let triage merge them.** That entry already records the listbox's stacking as unobserved, so the gap itself is not new; what is new is the question, and the fact that the entry's own membership has gone stale. It names `.info-tip-panel` as the member that _gained_ an observer and stands as "the worked example of what the other two still lack" — but #284 D13 took the (i) off the bar and moved `panelIsOnTop` onto `.header-menu-popover` with it, so the observer went where the case went. The count is coincidentally still two of four; the membership is not. Today the measured pair is the menu popover and the site card's anchor, and the unmeasured pair is the search listbox and the info-tip panel. The question this raises, and which the older entry's per-declaration fix direction does not answer, is a coverage-policy one: three of the four are the same claim about the same canvas — absolutely positioned, same value, painted over maplibre — so either every equally-stacked overlay owes its own occlusion case (four cases, three of them near-identical, and a fifth owed the day anyone adds an overlay) or one representative discharges the class and the rest are covered by the shared declaration they inherit. `structure.md` rule 7's test applies to specs as well as to code: if the popover's case changed, would the listbox's be wrong until it changed the same way? If yes they are one intent and one representative is honest — provided the representative is _named_ as standing for the class rather than reading as its only member, which is exactly what the three census clauses now do in prose and nothing does mechanically. The listbox is the member worth promoting if a second case is ever written, because it hangs directly over the map and is the most visible to lose. Settle it alongside the older entry and alongside `info.css`'s own flag, as one decision about what the browser lane owes a shared stacking value, rather than by adding cases one overlay at a time
- Source: #284 pass 3 wave B, review cycle 3

## 2026-08-09 — The Node/pnpm setup block is copied into every workflow job that builds, and bumping one leaves the rest wrong

- Where: `.github/workflows/` — the `actions/checkout` + `pnpm/action-setup` + `actions/setup-node` (`node-version-file: .nvmrc`, `cache: pnpm`) + `pnpm install --frozen-lockfile` block, in the `checks`, `web-build` and `web-e2e` jobs of `ci.yml` and in the deploy job of each of `deploy-web.yml`, `deploy-api.yml`, `deploy-ingestion.yml` and `deploy-forecast.yml`. Eight jobs on the day this was logged: those seven, plus the interim `deploy-pages.yml` that #285 added and that its own teardown deletes, so the count reads seven again once that lands — count the sites when acting on this entry rather than trusting the number in it
- What: identical copies of the same four steps, at the same action majors, with the same intent — stand up the toolchain this repo builds with — which is structure.md rule 7's yes case exactly: change one and every other is wrong until it changes the same way. The changes that trigger it are the routine ones. An action major (`setup-node` or the pnpm one) has to land in every copy on the same day, or CI and the deploys stop building on the same toolchain; a change in how the Node version is sourced — dropping `.nvmrc` for a `packageManager` field, adding a second cache path — is the same all-sites edit. The failure mode is quiet in the worst way, because a stale copy still passes: a missed deploy workflow keeps building green on the old toolchain and ships an artefact CI never verified, which is precisely the property every one of these setup comments claims in the same words (`deploy-web.yml`'s is the durable copy: "What CI verified and what this workflow ships must be built by the same toolchain, or 'green on main' stops being a statement about the artefact") and which nothing enforces — the claim is a convention held by whoever remembers all the sites. The real fix is one definition they all call: a reusable workflow, or a composite action under `.github/actions/`. That touches every workflow in the repo and nothing #285 is about, which is why it is logged rather than taken here. It is also not new with this PR — the block was already at seven copies before #285 added its eighth; the entry is that count reaching the point where the copy-paste has become the convention rather than an accident
- Source: #285 PR 1, review cycle 1

## 2026-08-09 — The Pages workflow's bundle-gate comment paraphrases the gate, and gets both of its edges wrong

- Where: the comment on the "Assert bundle properties" step in `.github/workflows/deploy-pages.yml`, against `.claude/scripts/check-web-bundle.sh`'s header — its `entry budget` line and the "What a green run does NOT say" paragraph below it
- What: the comment calls the entry budget "a byte count over `dist/assets/`, independent of the base path", and both halves are wrong at the edge. The budget globs `dist/assets/index-*.js` and only that file — the script's header says so in a paragraph written to stop exactly this reading, and names what therefore passes untouched: weight landing beside the entry as its own asset, and weight that never reaches `dist/` at all. And the entry is not literally base-independent, because the `--base=/cumulo/` string is inlined into the entry chunk, so the byte count differs slightly between base paths (a handful of bytes, measured on this PR's review — far inside the budget's headroom, so no verdict changes either way, which is why this is a reading hazard rather than a gate bug). The fix is not a better paraphrase but architecture.md rule 9's: the gate owns what it measures, so the comment should keep the half that is this file's to say — why the step is here, that this is the copy a visitor downloads rather than a near-identical one — and point at `check-web-bundle.sh` for what the budget covers. Worth logging rather than fixing in place because the same paraphrase-instead-of-pointer habit is what rule 9 exists for, and the specific copy lives in a file whose whole argument is that it is temporary
- Source: #285 PR 1, review cycle 3 — demoted at the 3-cycle cap

## 2026-08-09 — The Pages teardown calls five hedges "one-line", and one of them is

- Where: teardown step 4 and the step-4 group of the "pointer sites" list, both in `.github/workflows/deploy-pages.yml`'s header, against the five hedges themselves: the `web_origins` description in `infra/api/variables.tf`, the `cors_configuration` comment in `infra/api/gateway.tf`, the `CUMULO_WEB_ORIGINS` comment in `infra/api/lambda.tf`, the `CUMULO_WEB_ORIGINS` paragraph in `apps/api/README.md`'s Configuration section, and the `web` stack paragraph in `infra/README.md`
- What: one instruction covering three shapes. Only `variables.tf`'s hedge is what step 4 describes — a blank-line-delimited paragraph that deletes cleanly. `gateway.tf` and `lambda.tf` are spliced into host sentences, so deleting the hedge lines alone leaves a dangling "— with one exception" in the first and "— as edits to this file" in the second: the executor has to restore the host sentence, and in `gateway.tf` also the wrap of the "**Tightening this…**" sentence the splice reflowed. `apps/api/README.md` and `infra/README.md` are trailing sentences inside wrapped paragraphs — no repair of meaning needed, but a mid-line edit rather than a line deletion. Each site was checked rather than taken from the finding, which put four sites in the repair class; two of those four need only the trailing sentence dropped. Nothing here is unrecoverable — a dangling em-dash is visible on sight, and `pnpm verify` never sees prose either way — but the list exists precisely to be worked from months later by someone who did not write it, and "one-line" understates the edit at four of five sites. Two fixes, either fine: say what each hedge is where the list names it, or word every future hedge as its own paragraph so the instruction becomes true
- Source: #285 PR 1, review cycle 3 — demoted at the 3-cycle cap

## 2026-08-09 — No gate asserts a workflow's credential properties, in either direction

- Where: `.claude/scripts/lint-workflows.sh` (the `lint:workflows` gate, reached through `pnpm lint` and so through `verify`), against the `configure-aws-credentials` step in `deploy-api.yml`, `deploy-forecast.yml`, `deploy-ingestion.yml`, `deploy-web.yml` and `oidc-smoke.yml`, and against the "NO AWS, AT ALL" claim in `.github/workflows/deploy-pages.yml`'s header
- What: the gate runs actionlint plus shellcheck and nothing else, so nothing in `verify` knows which workflows may authenticate to AWS. Both directions of that are convention. A workflow that claims it never touches AWS is re-checked only when a human remembers to grep for the credentials action — `deploy-pages.yml`'s header now says exactly that about itself, which is honest and is also the symptom. The converse matters more: nothing stops a new workflow adding the credentials action and assuming the deploy role, because the OIDC trust policy admits a ref (`refs/heads/main`) and not a named workflow, so the set of files permitted to hold AWS credentials at all is enforced nowhere and reviewed only by whoever notices the diff. The fix is a gate over `.github/workflows` with an allow-list of the files permitted to use the credentials action, failing on any other file that names it, wired into the `verify` script per CLAUDE.md's when-adding-a-gate rule so every caller inherits it — plus a decision on how the allow-list is kept honest, since an allow-list nobody prunes is the next version of this entry. Deliberately not a clause in `deploy-pages.yml`: that file is temporary and would take the argument with it. Same shape as the captured "No gate covers a constant restated in prose" entry now owned by #249 — a property asserted in prose with nothing mechanical behind it — so cluster the two at triage rather than filing them twice
- Source: #285 PR 1, review cycle 3 (SYSTEMIC)

## 2026-08-09 — The per-IP limiter's route coverage is asserted one hand-written case at a time

- Where: the `rateLimited` wrapper and its applications in the route table of `apps/api/src/main.ts` — the three writes (through `guardedWrite`), `GET /v1/sites/{siteId}/series`, `GET /v1/fleet/actuals` and `GET /v1/fleet/forecast` — against the limiter `describe` in `apps/api/src/main.test.ts`, whose only per-route positive controls are `limits the span-capped series read` and `limits the fleet forecast fan-out`, with `leaves the unlimited reads alone` (on `GET /v1/sites`) as the negative one. The two other places that could have covered it cannot: the route-list test earlier in `main.test.ts` compares method and path only, so a wrapper is invisible to it, and the handler suites exercise the handlers directly, below the wrapper
- What: `GET /v1/fleet/actuals` is the one limited read with no positive control of its own, so the suite is indifferent to whether that route is inside the limited set or outside it — and the gap runs both ways, since a route joining the set is asserted nowhere either. The set's real membership is whatever the route table happens to say on the day someone reads it, and the property ADR 0006's layer 1 exists to provide rests on an authoring convention rather than on anything mechanical. #296's PR did not create the asymmetry — it made it visible by adding the fleet-forecast case beside the series-read one. The coverage claim here was established by an exhaustive read of the limiter `describe` rather than an executed experiment (the demonstration would involve edits the harness's classifier refuses on sight, reasonably). The candidate mechanism is one exhaustive assertion binding a declared limited-route list to `main.routes`, failing on any disagreement in either direction — the shape `responses.ts` already uses for error codes, where `Record<ApiErrorCode, string>` makes an unhandled code a type error instead of a documentation gap. That would also discharge [#309](https://github.com/TomBennett-Lloyd/cumulo/issues/309), the prose half of the same problem, so the two belong together at triage. Logged rather than built: it is a decision about how the limited set is declared at all, which is more than a review cycle on a route-addition PR should settle. The same shape recurs one layer down, noticed in cycle 2 of the same PR: the four `*.read-deadline-reached` event names (`get-site-series.ts`, `get-site-forecast.ts`, `get-fleet-actuals.ts` and `get-fleet-forecast.ts`, all under `apps/api/src/forecast/`) are argued to be deliberately distinct in each one's own docblock — separating the routes in a log query is the entire reason they are not one name — and that distinctness is asserted by nothing, because each deadline test references its own exported constant. Collapsing all four onto one shared literal would therefore keep the suite green while destroying exactly the property the docblocks argue for, which is the same failure mode as the limiter's: a deliberate set whose membership only prose and authoring convention defend. Same candidate mechanism too — one declaration a gate compares against reality — so cluster it here at triage rather than filing it separately
- Source: #296 PR A, review cycles 1 and 2 (SYSTEMIC)

## 2026-08-09 — Three constants answer "how wide is this string at `--text-xs`", and the one no gate can see is 17% narrow

- Where: `HORIZON_LABEL_WIDTH` in `apps/web/src/charts/forecast-chart-axes.tsx` (spent as `labelWidth` by the horizon label's placement, and asserted against by `forecast-chart-axes.test.tsx`'s two horizon-label cases), against `TOOLTIP_CHAR_WIDTH` in `apps/web/src/charts/tooltip-geometry.ts` and the `AXIS_CHAR_WIDTH` in `apps/web/src/charts/chart-axis-ticks.ts` that re-exports it
- What: the app has one question — how wide is a string at `--text-xs` — and two independent answers to it. The per-character model is 6.3 units, trued in #284 D6 against a rendered measurement after 5.6 was caught clipping a real tooltip row, and it is one model under two names (`AXIS_CHAR_WIDTH` is `TOOLTIP_CHAR_WIDTH`). The horizon label answers the same question as a whole-string constant instead: 84 units for `forecast horizon`, which is 16 characters, so it prices them at 5.25 each — narrower than the 5.6 already demonstrated to clip, and 17% under the 101 the trued model gives. Both docblocks state the same asymmetry, that erring wide is harmless and erring narrow is the clipping the constant exists to prevent, so this is not two defensible calibrations but one of them being wrong by its own standard. Nothing catches it: the jsdom cases spend `HORIZON_LABEL_WIDTH` on both sides of their comparison, so a label 101 units wide passes an assertion written in 84s, and the browser lane's `escapedLabels` (`apps/web/e2e/chart-surfaces.spec.ts`) measures each text against the **canvas** rect rather than the plot, so an overrun past `plot.right` hides inside the 32-unit right margin `PLOT_RIGHT_MARGIN` leaves. Invisible today because those margins absorb it, and invisible tomorrow for the same reason right up until a longer chrome string or a tighter margin spends the slack. The fix is one owner for string width at `--text-xs` — the trued per-character model, applied to the horizon label as it already is to axis ticks and tooltip columns — rather than a third calibration of the same font
- Source: #284 pass 3 wave C, review cycle 1 (SYSTEMIC)

## 2026-08-09 — The collapsed search bar dismisses on any focus-out, and is right today only because nothing else in it can take focus

- Where: `AppHeader` in `apps/web/src/header/AppHeader.tsx` — the `onBlur` on the `.header-search-bar` wrapper, beside the `onKeyDown` on that same element; against the internals it silently depends on in `SiteSearch` (`apps/web/src/header/SiteSearch.tsx`: the options are `<li>`s pointed at by `aria-activedescendant` rather than focusable elements, and their `onMouseDown` calls `preventDefault`). Related on both sides: the "Neither popover has focus-out dismissal, and the second one has now copied the first" entry above owns the shared rule this wants, and the "No a11y linting…" entry carries this wrapper as its ninth member
- What: the bar closes on _any_ `focusout` from its subtree — no `relatedTarget` check, no `contains` test against the container. That is correct only because the bar currently holds exactly one focusable thing: the combobox input keeps the focus while a reader walks the matches (the highlight moves by `aria-activedescendant`, not by focus), and pressing a match cannot blur the field because the option's `mousedown` handler prevents the default. Both facts belong to `SiteSearch`'s internals, which this component neither owns nor can see. Put a second focusable in the bar — a clear button, a filter chip, a "show all matches" link — and the bar dismisses itself as focus moves _between its own children_, mid-interaction, which is the kind of failure that reads as a flicker rather than as a bug. The `null` `relatedTarget` case is already live: focus leaving the document entirely (the window going to the background, devtools opening) closes the bar, so a reader coming back finds their query gone. Nothing would notice either regression — every dismissal case in `AppHeader.test.tsx` drives the one-focusable arrangement, which is exactly the configuration the fix would change. Fix direction is not local: it is the same `focusout` rule the popover entry above asks for — close when `relatedTarget` is outside the container, stay open on `null` — now wanted in three components rather than two, which is the threshold that entry set for extracting it as one shared idiom instead of a third copy. This wrapper is also the third instance of the pattern the a11y ledger tracks: hand-written keyboard and focus handling on a non-interactive wrapper element, with review attention as its only gate. Cluster all three at triage rather than fixing this one in place
- Source: #284 pass 3 wave D, review cycle 1 (SYSTEMIC)

## 2026-08-09 — A site chosen through the collapsed search bar has no landing, because the selection removes the opener it just captured

- Where: `SitePopoverCard` in `apps/web/src/map/SitePopoverCard.tsx` — the mount effect's opener capture and its cleanup's not-chasing guard (`opener?.isConnected !== true`) — against the `onBlur` on the `.header-search-bar` wrapper in `apps/web/src/header/AppHeader.tsx`. The rule being amended is stated in `docs/standards/react.md` ("A surface that leaves owes a landing, and it owes it to whoever it took the focus from", including the sentence declining to chase an opener that has left the document) and echoed in `docs/design/dashboard-composition.md`. Cluster members: the "Neither popover has focus-out dismissal" entry and the "The collapsed search bar dismisses on any focus-out" entry above
- What: below `header.css`'s breakpoint the whole sequence rides one press. Enter in the bar selects a site; the card mounts with a reader origin, captures `document.activeElement` — the bar's input — as its opener, and focuses its own heading; that focus move fires `focusout` on the bar wrapper, which closes the bar and unmounts the input. When the card is later dismissed the cleanup finds `opener.isConnected` false, declines to chase it, and focus falls to `body`. The same reader above the breakpoint lands back on the header's field, which is what makes this D17's regression rather than a gap the card always had. The not-chasing rule is not wrong: it was written for openers that _happen_ to have gone (a submit button inside a dismissed dialog), and its fallback reasoning — "focus stays where the browser puts it, which is the same place a card with no landing at all would have left it" — holds while the departure is incidental. This is the first opener guaranteed to leave the document as a direct consequence of the selection that captured it, so that fallback stops being the unlucky case and becomes the ordinary outcome of the app's narrow-width search path. Fix direction is an amendment rather than a patch: when the captured opener is gone, land on something still connected and still meaningful — the search toggle, which is on screen at exactly the widths where the bar exists — decided where the origin is known rather than guessed at by the card, which cannot tell a detached search field from a detached anything else. That is the same question the two entries beside it ask (where does a surface hand focus when what opened it is no longer there), which is why it belongs to that cluster rather than to a chunk. Nothing observes it today: the landing cases in `Dashboard.focus.test.tsx` and `map/SitePopoverCard.test.tsx` drive marker and row openers, and the browser lane's phone case ends at the selection rather than at the card's dismissal
- Source: #284 pass 3 wave D, review cycle 3 (SYSTEMIC)
- Resolved by #284 D14 for the path reported: a reader-initiated selection now lands on the fleet panel's range picker instead of the card's heading, so the narrow-width search reader is left on a control that is connected at every width, and the card — never holding the focus — hands none back at all. Residual, and the reason this is a scope reduction rather than a clean close: a reader who afterwards tabs or presses _into_ the card and dismisses it from there still meets the same detached opener and still falls to `body`. That residual is the cluster's question rather than this entry's path, so triage should fold it into the two entries above rather than carry this one whole. The "Whether a keyboard reader can get from the landing back into the card" entry at the foot of this log is the same question asked at desktop width — that one is whether the card can be reached, this one is what the card does once it has been — so decide the three together

## 2026-08-09 — Three "there is no breakpoint" statements are scoped-true and read as the app's census

- Where: the #265 paragraph in `apps/web/src/dashboard/dashboard.css`'s file docblock, the same claim in `docs/design/dashboard-composition.md`'s composition section, and the comment in the "stacks the fleet chart under the map rather than beside it" case in `apps/web/e2e/composition.spec.ts` — against `stylelint.config.mjs`'s residual paragraph, which since #284 wave D cycle 2 advertises itself as the one place the app's breakpoints are counted
- What: each statement is true of the surface it describes — #265 made the narrow-screen arrangement the dashboard's only arrangement, so that layout really has none — and each is worded as though it were about the app, which has had two since #284 C12 and D17. Nothing behaves wrongly and no gate can see it; the cost is navigational and lands on the next reader rather than on this one. A grep for the census reaches these three before the paragraph that owns it, and a reader who trusts the first hit either "fixes" a file that was right or concludes the census owner is wrong. Fix is one clause each, scoping the claim to the dashboard's own layout and leaving the count where it now lives. Deliberately not folded into wave D's diff, which had already spent three cycles on breakpoint prose and would have been widening its own blast radius to do it
- Source: #284 pass 3 wave D, review cycle 3 — demoted at the 3-cycle cap

## 2026-08-09 — The header breakpoint's restatement ledger was one site short of the sweep that produced it

- Where: the file docblock in `apps/web/src/header/AppHeader.test.tsx` ("at a real 390px viewport") against the restatement ledger in the media-query block of `apps/web/src/header/header.css`
- What: 390px is a breakpoint-derived literal — it is the phone width chosen for sitting clear of 27.4375rem — so that docblock is a carrier the ledger written one cycle earlier does not name, while the `PHONE_VIEWPORT` doc in `e2e/header.spec.ts` carrying the same derivation is named. Either fix is a line: drop the number from the jsdom docblock, which is describing the other lane's viewport rather than asserting one, or add the ledger entry. The number is the smaller half of this entry. The larger half is the observation it is an instance of — **the ledger, like every enumeration this loop has produced, was one site short** — and the pattern is three for three on this branch: the a11y entry's member list gained its ninth member only when someone went looking, the breakpoint census was written as a count in three files before one owner was named, and this ledger was completed by the cycle after the one that wrote it. Each was assembled by a deliberate sweep and each still missed a member, which is the a11y entry's own failure mode one level up: an enumeration whose only gate is review attention decays exactly as fast as attention does. Worth a decision rather than a fourth careful sweep — a mechanical check that every literal derived from an owned value appears in that value's ledger is the shape of it, and it is the same want as the captured "No gate covers a constant restated in prose" entry now owned by #249, so cluster the two at triage
- Source: #284 pass 3 wave D, review cycle 3 — demoted at the 3-cycle cap

## 2026-08-09 — The credits band drops both prefixes at one width, so the weather prose leaves before the row forces it

- Where: the compact-row rule and the measurement block above it in `apps/web/src/map/map.css`, against the Attribution section of `docs/design/map-treatment.md` and CLAUDE.md's Open-Meteo constraint. Sits beside "The credits band occludes markers across the map's full width", which owns this band's layout
- What: one media query hides `.map-attribution-prefix` and `.cumulo-attribution-prefix` together, so the weather credit compacts at the width where the _pair_ stops fitting rather than where its own phrase does. Measured in review: from roughly 500px to 596px the row can still carry the full weather phrase once the tile prefix alone is dropped, so the weather prose currently goes about 96px earlier than the row forces it. That is inside the sanctioned reading — the owner's amendment attaches the compact form to the row as composed, and that row genuinely cannot hold both credits' full forms below 596px — but the margin is thinner than the prose around it implies, since "at widths where the row cannot hold it" reads as a statement about this credit's own necessity. The refinement is a staged drop: the tile prefix at the width the pair stops fitting, the weather prefix only at the width the weather phrase itself does. It costs a second query on this band and a second measurement, and it changes what `map-treatment.md`'s Attribution section promises, so it belongs with that section's owner rather than in a layout chunk — the same routing the band-occlusion entry above it takes, and a reason to decide the two together
- Source: #284 pass 3 wave D, review cycle 3 — demoted at the 3-cycle cap

## 2026-08-09 — Whether a keyboard reader can get from the landing back into the card is asserted by nobody, in either lane

- Where: the first case in `apps/web/e2e/keyboard-focus.spec.ts` ("hands a keyboard selection to the range picker, ring and all"), which used to end in an Escape-and-hand-back tail and now stops at the landing; against the `closeOnEscape` handler and its docblock in `apps/web/src/map/SitePopoverCard.tsx`, the "closes on Escape from inside the card" case in `apps/web/src/dashboard/Dashboard.focus.test.tsx`, and the Focus bullets in `docs/design/dashboard-composition.md`. Same gap, other end: the "A site chosen through the collapsed search bar has no landing" entry above, whose own last sentence records that the browser lane's phone case "ends at the selection rather than at the card's dismissal"
- What: #284 D14 moved the landing from the card's heading to the fleet panel's range picker, and rerouting the hand-back assertion to jsdom was right on its own terms — `document.activeElement` is all that case ever measured, and the jsdom lane is faster at it. What went unnoticed is that D14 **created a criterion neither lane had before**: the card and the landing are now in different halves of the page, so "can a keyboard reader get back into the card" stopped being a tautology and became a question about a real tab order. It is a hard one — the map precedes the reading column, so the route is _backwards_ past **six stops**: the (i) tip, the credits band's three links (`MapSurface` renders the band after the controls, and the Open-Meteo credit is in it by licence in every state, so none of the three is removable), and the map's two controls; then it crosses maplibre's marker overlay, whose DOM position relative to `.map-controls` is a browser fact no jsdom test can see. That enumeration was written as "the (i) tip and the map's two controls" until wave E cycle 3 counted it, and the three-link correction **raises the price of the second fix below**: shortening a two-stop route is a tweak, shortening a six-stop one that a licence obligation sits in the middle of is a layout decision. It also makes the case for the first fix stronger, since a route this long is exactly the kind that decays without an assertion on it. By `testing.md` rule 10 that is browser-lane work (real tab order, real key modality, real focusability), and the jsdom cases that replaced the deleted tail cannot stand in for it: they put the focus on Close directly, which asserts that Escape works _from_ the card while assuming the reader reached it. So Escape-from-inside is covered, the hand-back is covered, and the only step a reader actually has to perform is covered by nothing. Two candidate fixes and they are not equivalent: a browser case that Shift-Tabs from the landing until Close holds focus (a bounded loop like `tabToSiteRow`'s, which would also fail loudly if the overlay ever left the tab order) asserts the route as it is; making the route shorter — a keyboard affordance on the landing, or ordering the column so the card is ahead of it — changes the design and belongs with the D14 decision's owner. Both are worth pricing together with the narrow-width entry above, since that one is the same question asked at 390px
- Source: #284 pass 3 wave E, review cycle 1 (SYSTEMIC)

## 2026-08-09 — The D14 landing rule is re-argued at nearly twenty sites instead of pointed at, and the bill has now been paid three times

- Where: the argument's intended owner is the "Focus moves for a reader, and never for a link" docblock in `apps/web/src/map/SitePopoverCard.tsx`, and the standards owner is the "Where the focus lands" paragraph in `docs/standards/react.md`. The restatements, by anchor: the three prop hops — `pressedButtonRef` in `apps/web/src/dashboard/range-picker.tsx`, `rangePickerRef` in `apps/web/src/dashboard/FleetPanel.tsx` (and again on that panel's picker JSX), `selectionFocusRef` in `apps/web/src/dashboard/MapRegion.tsx`; four more inside the card itself (its `selectionFocusRef` prop doc, the focus effect's inline comment, the `closeOnEscape` docblock, the cleanup's "still has it to give" comment); `selectionFocusRef` and the `focusSiteRow` note in `apps/web/src/dashboard/Dashboard.tsx`; the docblock in `apps/web/src/dashboard/selection-origin.ts`; the first Focus bullet in `docs/design/dashboard-composition.md`; the file docblocks of `apps/web/src/dashboard/Dashboard.focus.test.tsx`, `apps/web/src/map/SitePopoverCard.test.tsx` (its focus describe) and `apps/web/e2e/keyboard-focus.spec.ts`; the `StubMapRegion` card comment in `apps/web/src/dashboard/dashboard-test-fixture.tsx`; and the contrast pair in `apps/web/src/add-site/AddSiteForm.tsx` and its test, which restate the rule by saying the card takes the other branch of it. Census reproducible with `command grep -rlE "D14" apps/web/src apps/web/e2e docs`, which printed **17 files** when this entry was last edited — 16 of them outside this log — of which roughly nineteen sites are arguments rather than pointers. The line count is deliberately not the headline: it was 39 at that moment (34 outside this log) and it moves whenever this entry is itself edited, which is a small illustration of the finding rather than a figure to trust. An earlier draft of this entry said "36 mentions" and did not reproduce a cycle later
- What: every one of those sites re-derives some part of one decision — why a control rather than the heading, what the fallback is and when it is reached, what the announcement costs — instead of naming the owner and stopping. That is `structure.md` rule 7's same-intent copy in prose form, and prose copies decay the way code copies do except that no compiler notices. **The bill is not hypothetical; it has been paid three times on this branch alone.** D14 itself had to rewrite the superseded heading claim at ten sites, and the enumeration of those sites was itself one short. Wave E review cycle 1 then raised five findings that were, every one of them, that same rewrite coming up short again — a Tab direction restated in three places and wrong in all three, an announcement mitigation restated in three places and wrong in all three, an Escape justification wrong in two. Cycle 2 found two more. The pattern is exact: each cycle fixed the sites it could see, and the next cycle found the ones it could not, because there is no single place where being wrong is cheap to notice. The fix is the obvious one — one owner carrying the argument, one-line pointers at every hop ("the landing rule; `map/SitePopoverCard.tsx` owns it") — but it must not be decided here, because **re-arguing at each hop is the prevailing house style for prop docs across `apps/web`**, not a lapse specific to this feature: the same shape is in `SiteSearch`'s `inputRef`, `MapRegionProps`, `FleetPanelProps` and `panel-states.tsx` alike. Changing it is a comment-convention decision with a large diff and a real trade-off on the other side (a prop doc that only points is a prop doc a reader must leave the file to understand), so it belongs to whoever owns that convention — plausibly the same decision as the captured "an enumeration whose only gate is review attention" entry above, since a rule stated once is an enumeration of one
- Source: #284 pass 3 wave E, review cycle 2 (SYSTEMIC)

## 2026-08-10 — review-loop's classification step fuses three owners into one paragraph, so a boundary marker can only be planted mid-step

- Where: step 5 of `.claude/skills/review-loop/SKILL.md` (the step opening "Classify the diff per `.claude/workflow.json`"), against the delegation boundary it now has to carry for rule 3 ("Stop before the merge — but you are not done") of `.claude/agents/task-orchestrator.md` and step 4 of `.claude/skills/run-issue/SKILL.md`
- What: that step is a single ~450-word paragraph doing three jobs at three different owners — classify the diff (a task-orchestrator's), execute the merge chain (the merge owner's), and run the human-review path (split between them) — with no seam between them. #336 had to plant the "the merge sequence from here on is the merge owner's" marker in front of the `Merge sequence under branch protection:` sentence, which is the earliest seam that exists, and it is already too late: an agent reading the step verbatim from its start meets "→ merge (squash, delete branch) and run `/retro`" in the classification sentence, several lines before the sentence telling it that neither is its job. The marker is therefore doing recovery work rather than boundary work, and the delegated pilot is the first reader for whom the difference bites — a flat top-level session owns all three jobs, so the fusion has never cost anything before. Fix direction is a split, not more prose: step 5 becomes classification / merge-execution / human-path sub-steps, and the ownership marker moves to the head of the merge-execution sub-step where a verbatim reader meets it before any merge verb. Deliberately not done in #336, whose whole diff-minimality argument is that restating a contract is how contracts diverge — restructuring the skill that owns the merge chain is a change to that contract's text and wants its own review
- Source: #336 review cycle 1 (SYSTEMIC)

## 2026-08-10 — The adopted orchestration spec carries two internal count and cross-reference errors, preserved on purpose as the reviewed text

- Where: the §9 opening sentence and the "Wedged nested agent invisible to the top-level" row of §10's failure-mode table in `docs/design/task-orchestrator.md`, against that document's own §9.1–§9.3 enumeration and step 7 of `.claude/skills/run-issue/SKILL.md`
- What: two self-references in the spec of record do not match the spec's own content. §9's opener announces "Four files: one new agent, one new top-level skill, one-line boundary notes in two existing skills, one CLAUDE.md bullet, one workflow.json block" — six items, three amended skills (execute, review-loop, retro), and seven files in §9's own enumeration — eight once the design doc itself is counted, nine in the landed diff with this ledger. §10's wedged-agent row points at "run-issue step 6" for wedge detection, which is step 7 in the extracted skill; step 6 is Release. Neither misleads an agent today, because the header's precedence sentence sends every operative question to the agent and skill files and these are commentary in the rationale document — which is exactly why they were left rather than quietly corrected: #336 committed the body as reviewed, and an implementer editing an owner-approved document's prose on its own initiative is the failure mode the verbatim rule exists to prevent. Fix direction: correct both in a docs-only pass if this document ever takes a substantive revision, in a diff whose subject is the correction — never silently folded into an unrelated change
- Source: #336 review cycle 1 (SYSTEMIC)
