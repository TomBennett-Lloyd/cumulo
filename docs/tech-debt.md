# Tech-debt log

A **buffer, not an archive**. `/review-loop` appends SYSTEMIC findings here instead of iterating on them; `/triage` periodically clusters entries, files root-cause GitHub issues, and **deletes** what it captured. A long-lived entry here means triage is overdue.

Entry format:

```
## YYYY-MM-DD — short title
- Where: file/module references
- What: the pattern or problem (symptom AND suspected root cause if known)
- Source: PR/issue #
```

Pointers must survive unrelated edits: cite **files and symbol or section names** (function names, headings, config keys, script names) — never bare line numbers, and never a copied code literal unless the literal itself is the finding. An entry that pins its claim to `file.sh`:42 sends its reader to whatever happens to sit at line 42 months later. Applies to entries dated 2026-07-31 onward; the back catalogue is retrofitted opportunistically, whenever an entry is edited for any other reason.

---

## Captured-entry redirects

Source comments across the repo cite entries by title ("recorded in `docs/tech-debt.md` (…)"). Triage deletes the entry but cannot edit those comments without turning a docs-only PR into a source-code one, so this table is where a dangling pointer lands. It is a **redirect, not an archive** — the content lives on the issue.

Maintenance: a row dies with its issue. Each capturing issue's implementation edits the very files whose comments cite it, so the citation and the row go together; whoever closes the issue deletes the row.

| Captured entry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Now owned by                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `check-supply-chain-policy.sh` reads YAML by line shape · Supply-chain gate asserts the manifest, but pnpm resolves policy from four places · `check-infra-mirrors.sh` reads Terraform by line shape                                                                                                                                                                                                                                                                                                                                                 | [#158](https://github.com/TomBennett-Lloyd/cumulo/issues/158)           |
| Only `@cumulo/shared` can express a type-level test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#159](https://github.com/TomBennett-Lloyd/cumulo/issues/159)           |
| Skill score compares two RMSEs computed over different sample sets · A metrics row cannot say which hours of its own period are missing                                                                                                                                                                                                                                                                                                                                                                                                              | [#160](https://github.com/TomBennett-Lloyd/cumulo/issues/160)           |
| No error boundary above the dashboard's async work · The map's placeholder shells restate `MapView`'s structure · Loading announcements are per-component conventions                                                                                                                                                                                                                                                                                                                                                                                | [#161](https://github.com/TomBennett-Lloyd/cumulo/issues/161)           |
| `invalid-response` now means two different failures · The shared data hook is still named after the surface that was deleted                                                                                                                                                                                                                                                                                                                                                                                                                         | [#162](https://github.com/TomBennett-Lloyd/cumulo/issues/162)           |
| Cycle rotation advances one location per hour, not one window                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#163](https://github.com/TomBennett-Lloyd/cumulo/issues/163)           |
| One ingestion alarm now stands for two unrelated operator questions · A forecast consumer pointed at the wrong environment is indistinguishable from an idle fleet · A user-site counter stuck above the real population                                                                                                                                                                                                                                                                                                                             | [#164](https://github.com/TomBennett-Lloyd/cumulo/issues/164)           |
| A hook that cannot read its own events disables edit-time lint silently                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [#102](https://github.com/TomBennett-Lloyd/cumulo/issues/102) (comment) |
| The port-inversion invariant in `@cumulo/hindcast` is convention, not a gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [#112](https://github.com/TomBennett-Lloyd/cumulo/issues/112) (comment) |
| No gate covers a constant restated in prose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [#249](https://github.com/TomBennett-Lloyd/cumulo/issues/249)           |
| CI never builds `apps/web`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#142](https://github.com/TomBennett-Lloyd/cumulo/issues/142) (comment) |
| `Retry-After` is unreadable cross-origin, so the client contract's backoff can never fire                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [#21](https://github.com/TomBennett-Lloyd/cumulo/issues/21) (comment)   |
| The bundle gate's "stale build" verdict only detects multi-entry staleness                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#111](https://github.com/TomBennett-Lloyd/cumulo/issues/111) (comment) |
| The conflict-retry budget is derived from a closed cohort, so exhausting it is read as a fault it need not be                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#155](https://github.com/TomBennett-Lloyd/cumulo/issues/155) (comment) |
| actionlint's action-input check is dead over most of this repo's `uses:` lines                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [#108](https://github.com/TomBennett-Lloyd/cumulo/issues/108) (comment) |
| The two batch paths refuse an unusable policy in different shapes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | [#100](https://github.com/TomBennett-Lloyd/cumulo/issues/100) (comment) |
| The split-capture migration silently narrowed the harness family's raw `$out` readers · reap's broken-git-link shape arm is shadowed by the identity arm                                                                                                                                                                                                                                                                                                                                                                                             | [#101](https://github.com/TomBennett-Lloyd/cumulo/issues/101) (comment) |
| The half-open UTC window is still declared five ways beside its named contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [#232](https://github.com/TomBennett-Lloyd/cumulo/issues/232)           |
| Browser-side fetches have no timeout policy, and the de-dup map inherits the hang · The ingestion cycle deadline is conditional on no mid-body stall, and no finite slack covers it                                                                                                                                                                                                                                                                                                                                                                  | [#233](https://github.com/TomBennett-Lloyd/cumulo/issues/233)           |
| The API deadline gates loops, not the straight-line prefix · `queryAllPages`'s pagination bound is optional, and four of five call sites walk unbounded                                                                                                                                                                                                                                                                                                                                                                                              | [#234](https://github.com/TomBennett-Lloyd/cumulo/issues/234)           |
| The token preview hand-rolls a chart the chrome convention cannot reach · A dashboard test pins the chart's chrome wording by whole-table `toEqual` · Five of the twelve consolidated strings sit outside every phrase-class sweep · The spoken chart readout carries neither the unit nor the clock                                                                                                                                                                                                                                                 | [#235](https://github.com/TomBennett-Lloyd/cumulo/issues/235)           |
| The hooks' "stdin was unreadable" report is dead code behind a hang                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#236](https://github.com/TomBennett-Lloyd/cumulo/issues/236)           |
| Neither popover has focus-out dismissal, and the second one has now copied the first · The collapsed search bar dismisses on any focus-out · A site chosen through the collapsed search bar has no landing · Whether a keyboard reader can get from the landing back into the card is asserted by nobody                                                                                                                                                                                                                                             | [#349](https://github.com/TomBennett-Lloyd/cumulo/issues/349)           |
| Stylesheet facts with no observer (four stacking values, the sheet's own import, the credits' veil, the credit-link treatment, the site card's anchoring) · Does every equally-stacked overlay owe an occlusion case                                                                                                                                                                                                                                                                                                                                 | [#350](https://github.com/TomBennett-Lloyd/cumulo/issues/350)           |
| No a11y linting, on the diff that lands the app's first seven interaction patterns · The app's two native dialogs disagree about whether a modal names itself                                                                                                                                                                                                                                                                                                                                                                                        | [#351](https://github.com/TomBennett-Lloyd/cumulo/issues/351)           |
| The chart's `Raw data` disclosure label is spelled in three places and owned by none of them · One chart is named three times, in three independent spellings                                                                                                                                                                                                                                                                                                                                                                                        | [#352](https://github.com/TomBennett-Lloyd/cumulo/issues/352)           |
| The selection-focus rule is re-argued at nearly twenty sites instead of pointed at — #328 removed the D14 landing and the sprawl simply re-argues its absence                                                                                                                                                                                                                                                                                                                                                                                        | [#353](https://github.com/TomBennett-Lloyd/cumulo/issues/353)           |
| A selection change is one commit ahead of the state that answers for it · Every range press re-spends the whole forecast read                                                                                                                                                                                                                                                                                                                                                                                                                        | [#354](https://github.com/TomBennett-Lloyd/cumulo/issues/354)           |
| The header search answers "No matching sites" while the fleet is still loading · Re-selecting the site that is already selected is a complete no-op · The search shows a capped number of matches and never says there were more                                                                                                                                                                                                                                                                                                                     | [#355](https://github.com/TomBennett-Lloyd/cumulo/issues/355)           |
| The credits band occludes markers across the map's full width · The credits band drops both prefixes at one width                                                                                                                                                                                                                                                                                                                                                                                                                                    | [#356](https://github.com/TomBennett-Lloyd/cumulo/issues/356)           |
| Three constants answer "how wide is this string at `--text-xs`", and the one no gate can see is 17% narrow                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#357](https://github.com/TomBennett-Lloyd/cumulo/issues/357)           |
| The Node/pnpm setup block is copied into every workflow job that builds · No gate asserts a workflow's credential properties, in either direction · The Pages workflow's bundle-gate comment paraphrases the gate · The Pages teardown calls five hedges "one-line"                                                                                                                                                                                                                                                                                  | [#358](https://github.com/TomBennett-Lloyd/cumulo/issues/358)           |
| review-loop's classification step fuses three owners into one paragraph · The adopted orchestration spec carries two internal count and cross-reference errors · A review-feedback entry carries a category the closed vocabulary does not declare                                                                                                                                                                                                                                                                                                   | [#359](https://github.com/TomBennett-Lloyd/cumulo/issues/359)           |
| A standards bullet is amended by paraphrase, in a comment three files from the rule it reinterprets                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#360](https://github.com/TomBennett-Lloyd/cumulo/issues/360)           |
| A second family of stated values is restated in prose with neither pointer nor ledger · Three owned values have restatement carriers and no rule-9 ledger · `ci.yml`'s hermetic-claim comment names the basemap provider outside the ledger · The contrast record's literals travel · Five stacks validate the environment alphabet nobody declared them against · The header breakpoint's restatement ledger was one site short · Three "there is no breakpoint" statements are scoped-true · The README's CI row is a census that stopped counting | [#249](https://github.com/TomBennett-Lloyd/cumulo/issues/249) (comment) |
| Every `cumulo-series` volume figure in the repo prices two models · The infra README still describes #136's forecast consumer as unbuilt                                                                                                                                                                                                                                                                                                                                                                                                             | [#289](https://github.com/TomBennett-Lloyd/cumulo/issues/289) (comment) |
| The per-IP limiter's route coverage is asserted one hand-written case at a time                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [#309](https://github.com/TomBennett-Lloyd/cumulo/issues/309) (comment) |
| Light-mode `--color-accent` is below AA for small text on its own surfaces                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | [#273](https://github.com/TomBennett-Lloyd/cumulo/issues/273) (comment) |
| The token gallery renders two colour tokens as chips with no colour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#297](https://github.com/TomBennett-Lloyd/cumulo/issues/297) (comment) |
| The app holds three incompatible verdicts on `calc(var(--space-1) / 4)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | [#103](https://github.com/TomBennett-Lloyd/cumulo/issues/103) (comment) |

---

## 2026-07-30 — `aws_budgets_budget.cost_types` left at AWS defaults

- Where: `infra/bootstrap/budget.tf` (`aws_budgets_budget.monthly_cost_ceiling`)
- What: no `cost_types` block, so the budget uses the AWS defaults, which subtract credits and refunds. On an account carrying promotional credits the meter can therefore run well past $100/month of gross usage while net cost stays under threshold and nothing alerts — the alarm reports what will be billed, not what is being consumed. That is a defensible reading of "cost ceiling" for a project whose ceiling is about the bank balance, and it is the current deliberate choice; it stops being defensible the moment credits land on the account, because the whole point of the ceiling is to catch runaway usage _before_ it is expensive. Revisit if credits appear (or before any AWS-credits programme is used for this project): either add `cost_types { include_credit = true, include_refund = false }`, or add a second usage-oriented budget beside the billed-cost one. Not a fix for this diff — it is a policy decision about what the number means, and it wants the account's credit state as an input.
- Source: #38 review cycle 1
- Triage note (2026-07-31): deliberately left in the buffer rather than filed. It is a parked decision with an external trigger (credits appearing on the account), not debt to clear — an open issue would be picked up by backlog burning and correctly do nothing. Convert it the moment the trigger fires.
- Triage note (2026-08-01): re-confirmed on the pass that emptied the other 29 entries. The trigger has not fired; this is the log's only survivor **by decision**, so its age is not evidence that triage is overdue.
- Triage note (2026-08-03): re-confirmed again on the pass that emptied the other 22 entries. Trigger still unfired.
- Triage note (2026-08-10): re-confirmed on the pass that emptied the other 41 entries. Trigger still unfired. It is no longer the log's only survivor by decision — this pass left seven others, each carrying its own dated note below saying why — so read the survivor set, not this entry alone, as the record of what triage considered and kept.

## 2026-08-02 — canon spawns an interpreter per path, and the sweep multiplies it

- Where: `canon` in `.claude/scripts/worktree-lib.sh`; its per-porcelain-line call sites in `reap-worktree.sh` and `sweep-worktrees.sh`
- What: the python→node migration kept canon's one-path-per-process shape, so reap is O(worktrees) node spawns and a sweep is O(N²); the lifecycle harness measured 27 s → 42 s on the same cases. No correctness impact and fine at this repo's worktree counts — recorded so the cost has a name. Fix direction if it ever bites: a `canon_many` reading a NUL-separated list in one spawn, or batching the porcelain parse's canon calls.
- Source: #102 review cycle 1
- Triage note (2026-08-03): left in the buffer, not filed. It is a measured cost with no correctness consequence and a named trigger ("if it ever bites" — worktree counts rising, or the lifecycle harness becoming slow enough to discourage running it); an open issue would be scheduled and would correctly find nothing worth doing. It clusters with nothing else in the log — every other script-harness entry this pass captured was about an assertion proving less than it claims, which this is not.
- Triage note (2026-08-10): considered and left again. The named trigger has not been met — worktree counts have not risen and the lifecycle harness is still run — so filing it would create an issue that correctly does nothing.

## 2026-08-03 — A provider payload repeating an hour is reported as ingestion's bug

- Where: `parseForecastResponse` in `apps/ingestion/src/open-meteo/response.ts` (its `hourly.time` loop and its `malformed` arm); the refusal it currently lands on is `requireUniqueKeys` in `packages/storage/src/write-preconditions.ts`, reached through `putForecastWeather` in `packages/storage/src/adapters/weather/weather-adapter.ts`
- What: `hourly.time` is mapped 1:1 into readings with no uniqueness check, so a payload that repeats an hour yields two readings for one location-hour and is caught a layer down, where the storage precondition refuses the write with "the caller must de-duplicate before writing". That message blames ingestion's code for the provider's payload and sends the reader to the wrong repository — the inverse of rule 1's blame tiebreak, which asks whose fault it is and wants the check owned by the `malformed` arm in `response.ts`, the arm that already names Open-Meteo as the party at fault and whose `ParsedForecast` failure value the cycle already knows how to report. Debt rather than blocker on two counts: the write is refused rather than corrupted, and the blast radius is correctly bounded to one location's cycle outcome. Only the blame is misrouted, and only on a payload we have never actually seen.
- Source: #100 review cycle 1
- Triage note (2026-08-10): considered and left. An unrelated singleton — nothing else in the log this pass was about blame routing on the ingestion parse path, so it clusters with no other entry and would file as an issue of one. Kept in the buffer until a second ingestion-blame entry gives it a root cause to share.

## 2026-08-02 — `drainWriteRequests`' policy seam outlived the caller that justified it

- Where: `drainWriteRequests` and `putSeriesItems` in `packages/storage/src/adapters/series/series-adapter.ts`
- What: with `deleteSiteSeries` retired (ADR 0007), `drainWriteRequests` has exactly one caller and its `policy` parameter only ever receives `this.batchPolicy` — the doc sentence that justified the seam ("how hard to push" is where a cleanup and an ingestion write legitimately disagree) was deleted with the caller that made it true, but the seam itself remains. Whether the adapter's two one-caller private write layers still earn their split is a `packages/storage` shape question: collapsing them touches the ingestion write path, which the retiring diff deliberately left alone. Decide next time that path is open — collapse into `putSeriesItems`, or restate a reason the parameter exists.
- Source: #167 review cycle 1
- Triage note (2026-08-10): considered and left. A singleton below the clustering threshold, and the entry's own condition ("decide next time that path is open") makes it a decision waiting on a diff rather than debt to schedule.

## 2026-08-05 — `response.ts` calls itself the only source of responses while two 204s are hand-built beside it

- Where: the module docblock at `apps/api/src/http/response.ts:6-7` ("the two ways this API ever produces one — a validated success body, or an `apiErrorSchema` failure"); the counter-examples are `apps/api/src/sites/delete-site.ts:199` and, as of this branch, the preflight answer in `apps/api/src/http/router.ts`
- What: the claim was already false before #263 — `delete-site.ts` hand-builds `{ statusCode: 204, headers: {} }` rather than going through any helper here — and the preflight 204 makes it two. So the file that owns the response vocabulary describes a vocabulary the service does not keep to, and each new 204 is another object literal whose "no body, no content-type" reasoning is re-derived at the call site while the reasoning for it already lives in this file's own `ApiResponse` docblock (lines 18-19). Wanted: a named `noContentResponse()` exported here, the two call sites routed through it, and the docblock trued to three ways. Deliberately not forced into #263's diff: the two 204s are the same shape for genuinely different reasons (a deleted resource versus a CORS mechanism answer), so they may legitimately diverge — a shared helper is only right if it stays a single fact, and `structure.md` rule 7 says that judgement wants its own look rather than a drive-by extraction under a fix-now.
- Source: #263 review cycle 1
- Triage note (2026-08-10): considered and left. It reads as a member of the restatement family that #249 and #360 captured, but it is not one — the false claim is a docblock's census of its own module, with no owned value and no ledger to join — so folding it into either would have widened that issue past its root cause. On its own it is a singleton below the clustering threshold.

## 2026-08-05 — `rotateMap`'s vertical stroke is hard-coded upward and can leave the viewport

- Where: `rotateMap` in `apps/web/e2e/map-regressions.spec.ts` (the `start.y - DRAG_DISTANCE_PX / 2` end-point), beside `dragMap`, which picks both directions from the viewport half
- What: the rotate gesture's horizontal direction adapts to which side of the viewport the start point sits in, but the vertical component always strokes upward; with the corner search currently returning top-left (both bottom corners sit under the credits band), the path ends above the map box and off the top of the default viewport. It passes today, and the pre-reset poll ("the camera never rotated") turns any regression into a loud failure rather than a false green — so this is robustness, not a defect. Mirroring the `x` direction choice on `y` removes the assumption. Take it next time the spec's helpers are open.
- Source: #265 review cycle 2 (map controls)
- Triage note (2026-08-10): considered and left. An unrelated singleton: this is a helper whose passing assertion rests on an assumption — a different root cause, and one whose own note already names the moment to take it ("next time the spec's helpers are open").

## 2026-08-09 — Since D3 the forecast chart is a keyboard tab stop in every state, including the ones with nothing plotted

- Where: the `tabIndex={0}` on the `<svg className="forecast-chart">` at the centre of the chart's figure — returned by `ForecastChartHoverBoundary` (`apps/web/src/charts/forecast-chart-hover-boundary.tsx`), which is also where the `readAtFocus`, `readAtKey` and `clearReadout` handlers bound to it now live, both having moved out of `ForecastChart` in #331 — together with the `role="img"` name it carries; the states that now reach it are the loading and failed arms of `stateContent`, the empty arm of `readyContent`, and `emptyFleetBody`, all in `apps/web/src/dashboard/fleet-panel-body.tsx` (#284 D3), whose figure-in-every-state contract is pinned by `apps/web/src/dashboard/FleetPanel.structure.test.tsx`
- What: the chart was made unconditionally focusable back when it was only ever rendered with data on it. D3 made the figure structural — it is on screen while the fleet is loading, after the fan-out failed, for a forecastless fleet and for a fleet with no sites — and the `tabIndex` came along unchanged, so a keyboard reader now tabs into an interactive element that announces an image and has nothing behind it. The behaviour is benign rather than broken: `readAtFocus` selects sample 0 of an empty series, `activePoint` is then `undefined`, and the live region renders empty, so nothing wrong is spoken and nothing throws. What it costs is a tab stop that promises an explorable plot and delivers silence, in exactly the states where the prose beside it has already said there is nothing to explore. It is logged rather than patched because the question is a pattern one: the hover target rect and the table twin's disclosure are unconditional in the same way and equally empty in those states, so "an affordance is offered when there is something behind it" is a rule the chart should hold in one place rather than in three. Gating `tabIndex` on whether any points exist is the obvious move and is deliberately not taken here — it makes focusability a function of the data, which needs a decision about what happens to a chart that gains its first point while focused, and about whether the tab stop should instead persist and describe its own emptiness. Belongs with whoever next opens the chart's interaction surface
- Source: #284 pass 3 wave B, review cycle 1
- Triage note (2026-08-10): considered and left. Nearest neighbours are #349 (dismissal and landing behaviour) and #351 (a11y linting), and it belongs to neither: this is a question about when an affordance should be offered at all, not about how an offered one behaves or about a rule a linter could carry. Below the clustering threshold on its own, and its "belongs with whoever next opens the chart's interaction surface" condition is the trigger to file it against.

## 2026-08-10 — `browser-smoke.md` obliges the agent to emit findings, and its report block has nowhere to put them

- Where: the `New chrome answers for itself`, `A copy-touching chunk asks the prior question` and `A measurement printed is not a measurement asserted` bullets under `## What counts as verification` in `.claude/agents/browser-smoke.md`, against the status block under that file's `## Report` heading
- What: three of that section's bullets each end by requiring the agent to report something that is not a measurement — an addition with no reader decision it serves "is a finding, reported alongside your measurements"; copy that survives the prior question "is worth reporting as having been asked it"; a criterion no durable assertion owns "is a finding, reported alongside the reading". The return contract they report into has three slots, `STATUS`, `Measured:` and `Server:`, and none of them is a findings slot. So an obligation the file states three times has no structural home: the agent either folds findings into `Measured:` (where a spawner reading for a measurement will skim past them) or drops them, and either way the difference between "no findings" and "findings not reported" is invisible to whoever dispatched the agent — the same distinguishability problem `docs/review-feedback.md`'s **approved-no-changes** category exists to solve one level up. Fix direction: add a `Findings:` line to the `## Report` status block, explicitly `none` when there are none, owned by whoever owns that contract; the three bullets then point at a slot instead of at the report in general.
- Source: this PR's review cycle 1 (batch retro + triage, 2026-08-10)

## 2026-08-10 — Rule 9 exempts ADRs, so an ADR restating another ADR's figure has no discipline at all

- Where: `docs/standards/architecture.md` rule 9's closing clause ("Historical claims survive in past tense, naming the decision they reason about; ADRs are immutable and exempt"), against the cross-ADR restatements it cannot reach: the `**Sites:**` bullet under `### 2. The cap: 40 user sites, and the arithmetic that picked the number` in `docs/adr/0006-demo-abuse-protection.md`, which restates ADR 0002's fleet-headroom row as "25 sustained dashboard loads/minute at 21 RCU"; and the "**The cost guard is not a capacity guard, and the gap should be visible**" paragraph under `## Consequences` in `docs/adr/0005-fleet-api-hosting.md`, which restates "ADR 0002 sized `cumulo-series` at 21 provisioned RCU" and "0002's 300-second burst reserve of 6,300 units". Two more in the same family, found by #334's review cycle 3 and listed here because an enumeration inside an entry about restatements nothing can find had better be a census: the "**Which routes are limited**" paragraph under `### 1. The four layers, and which one bites first` in the same 0006, which cites "`cumulo-series`'s 21 provisioned RCU"; and the "**Read-back erodes ADR 0002's `weather` read sizing**" bullet in `docs/adr/0004-ingestion-transport.md`, which says "0002 **allocates** `weather` 3 RCU" in the present tense, stale since #156.
- What: rule 9's whole premise is that a stated value has one owner and every other mention points at it carrying no literal, with a restatement ledger for the sites that must carry one. The ADR exemption is correct on its own terms — an immutable document cannot be edited to track a moving value, and cannot serve as a ledger owner because it cannot absorb the next move. But the exemption is stated as though ADRs only ever restate _code_ values, and the uncovered case is ADR→ADR: one ADR citing another's figure. Those restatements are immutable at both ends, so no ledger can reach them, no amendment in the _owning_ ADR finds them, and nothing fails when they go stale. Every example above is already stale, and 0006's `**Sites:**` bullet is doubly so: the 21 RCU stopped existing with #258 (every table is on-demand), and the per-dashboard-load figure that bullet derives from halves with #264, which is what #334 exists to record. The others are stale on the #258 count alone — 0005's cost-guard arithmetic is a maximum-width single-site series read at ~30 read units, so #264's second fleet fan-out does not touch it, and 0004's is a `weather` allocation #156 retired. #334 could amend 0002 and move ownership to `infra/storage/tables.tf`, and all four sites would still assert the old numbers with no mechanism aware of it. Candidate conventions, none obviously right: a derived-figure amendment in the _citing_ ADR whenever the cited one is amended (cheap, but scales with the citation graph and needs the owning ADR's amendment to list its citers); an "as sized at the time" past-tense hedge required of every cross-ADR figure citation (free, but weakens genuinely load-bearing arithmetic like 0005's throttle-gap analysis, which needs the number to be true to make its point); or a rule that cross-ADR citations name the section rather than the value, which is rule 9's own prose-points-at-owner discipline applied one level up. Needs a decision rather than an edit, which is why it is here and not in #334's diff.
- Note: the family is live on two fronts — [#200](https://github.com/TomBennett-Lloyd/cumulo/issues/200) is in flight amending 0005 and 0006 for _different_ figures, so whichever convention is chosen should be checked against that work before it lands, and the two tickets should not settle the question independently.
- Source: [#334](https://github.com/TomBennett-Lloyd/cumulo/issues/334) review cycle 1

## 2026-08-10 — The ADR amendment procedure and its own guardrail collide, and practice has silently picked a winner

- Where: the `## Amendments` section of `docs/adr/README.md` — its procedure bullet ("true the value up inline where it appears, **and** append a dated entry…") against its closing guardrail ("an amendment never touches reasoning. If you find yourself rewording _why_, that is a supersession in disguise"). The evidence is the `## Amendments` section of `docs/adr/0002-storage-split.md` — all four of its entries, 2026-08-01 (#122), 2026-08-03 (#156), 2026-08-09 (#258) and 2026-08-10 (#334), read against the body edits their commits did or did not make — and the capacity table under `### Capacity mode: hybrid — provisioned where the load is batch-shaped, on-demand where it is request-shaped`, which still reads `provisioned | 14 | 21` for `series`.
- What: the procedure assumes a stated value sits somewhere an editor can reach without touching an argument, and sometimes it does not. The practice across ADR 0002's four amendments is a **hybrid that nothing writes down** — and the hybrid, not either pure rule, is what the README is missing. #122 (`6920bf3`) and #156 (`607f316`) both trued up inline: #156 edited the `cumulo-weather` row of the capacity table and the "Total against the free tier" line beneath it; #122 edited Consequence 5 and, notably, a clause _inside_ the "Provisioned capacity can throttle" reasoning paragraph — changing the pinned retry count without rewording why the retry budget is pinned at all, which is the guardrail honoured rather than breached. #258 and #334 skipped the inline step, and #258 hedged its omission in prose ("now describe no deployed resource"). The distinguishing feature is not whether the figure sits near an argument but whether a self-contained carrier can hold it: a table cell, a parenthetical, a Consequence line can each be trued up in isolation, whereas a number that _is_ a premise — the ≈ 27 the "read side fits too" argument reasons from, the 21 RCU option E's rejection turns on — cannot be moved without moving the inference built on it. So the real rule is "true up every self-contained carrier; describe in the entry what only a premise carries", and because it is unwritten each amendment re-derives it: #334's entry spends several sentences justifying its omissions and enumerating all seven affected sites. The cost is not just repetition — a reader comparing the README's letter against the ADR's practice sees a document apparently disobeying its own procedure, with the inline edits that would show otherwise visible only in git. Fix direction: the README states the hybrid, with the carrier-vs-premise test as the thing that decides which half applies. This is a decision about the amendment procedure itself, which is why it is logged rather than fixed in a ticket that was only ever about one figure.
- Source: [#334](https://github.com/TomBennett-Lloyd/cumulo/issues/334) review cycle 2

## 2026-08-10 — The wall-clock admission invariant has three prose carriers and no rule-9 ledger beside its owner

- Where: the owner is the module docblock in `apps/api/src/request-budget.ts` — the paragraph stating that every admitted unit is bounded by one `STORAGE_COMMAND_WORST_MS` of wall clock, together with the per-route ungated-prefix list beneath it. Its carriers are the 504 bullet in `apps/api/README.md`'s error-contract section, the `commonFailures` docblock in `apps/api/src/openapi/responses.ts`, and the timeout header comment above the `aws_lambda_function.api` block in `infra/api/lambda.tf`. The precedent for what is missing is `infra/storage/tables.tf`, whose table comments carry an explicit "Restatement ledger: this file's header".
- What: `docs/standards/architecture.md` rule 9 asks for a short restatement ledger comment beside an owned value naming every site that restates it. This invariant has none, and #321 paid for the omission twice in one branch: changing the invariant's denomination from commands to wall-clock units left all three carriers asserting the retired unit, and each was found by a different mechanism rather than by consulting a list — the README by an implementer's family sweep, and `responses.ts` and `lambda.tf` only in review cycle 1, after two chunk-level sweeps had reported the family clean. The root cause is that the carriers **paraphrase** rather than quote, so a sweep keyed to the owner's distinctive phrasing does not find them; only a sweep shaped around the _claim_ did. A ledger would make the set enumerable instead of rediscoverable, which is the difference between a plan-time check and a review-time catch. Two things sharpen the entry: the `lambda.tf` carrier now also compresses a definition of "unit" for a Terraform reader with no `STORAGE_COMMAND_WORST_MS` in scope — correct for that reader, and a widening of the surface any ledger must enumerate — and the ADR 0007 copy of the superseded wording is deliberately **not** a fourth carrier, since rule 9 exempts ADRs as immutable and the owner now cites ADR 0007 in the past tense. Belongs with the restatement-compliance family already captured on #249 rather than as a fix for this diff.
- Note: this is the **compliance** half of a gap #334's review found the **coverage** half of, and the two entries above are the other two views of it — read the three as one conversation. The distinction that matters: this entry is an owned value that _could_ carry a rule-9 ledger and simply does not, so it is fixable under the rule as written; "Rule 9 exempts ADRs" is a class the rule structurally cannot reach, so it needs the rule changed. The seam between them is visible inside this very entry — ADR 0007's verbatim copy of the superseded invariant is correctly untouched _because_ of the exemption, which is exactly the immutable-at-both-ends case that entry is about. Whatever convention settles the ADR half should be checked against this one before it lands, since a ledger that cannot name its ADR carriers is the same problem twice.
- Source: #321 review cycle 2

## 2026-08-10 — Rule 9 exempts ADRs from the restatement ledger, and the Amendments convention made that exemption false

- Where: rule 9 in `docs/standards/architecture.md` (its closing clause, "ADRs are immutable and exempt"), against the `## Amendments` section of `docs/adr/README.md`; the pair that demonstrates it is the worst-case bound owned by `docs/adr/0005-fleet-api-hosting.md` (its cost table's **Worst case, sustained** row, restated in the Consequences paragraph on worst-case spend) and the same bound restated under `## Cost` → `### API stack` in `infra/README.md`
- What: rule 9 requires every stated value to have one owner and requires any site that must carry the literal to be listed in a restatement ledger beside that owner — then exempts ADRs, on the premise that an ADR never changes. The Amendments convention (added to `docs/adr/README.md` on 2026-08-01, after #122) retired that premise: an ADR-owned value can now legitimately move while the decision stands. The exemption did not move with it, so an ADR is now the one kind of owner that can change without anything pointing at its copies. #200 is the demonstration — ADR 0005's bound was amended and its external copies were found by a reviewer's grep sweep rather than by following a ledger, which is exactly the serial-discovery-in-review failure rule 9's own text cites #156 to prevent. Concretely the amended bound now has two owners that disagree, and `structure.md` rule 7's test ("if one copy changed, would the other be wrong until it changed the same way?") answers yes for the pair. **Not a duplicate of the truing work**: correcting the drifted copies is [#376](https://github.com/TomBennett-Lloyd/cumulo/issues/376), and completing the bound's terms is [#375](https://github.com/TomBennett-Lloyd/cumulo/issues/375); both fix instances, and neither touches the rule that let the instances happen, so this entry outlives both. Two resolution directions, and picking between them is the decision: rule 9's exemption narrows to reasoning and decisions only (leaving amendable stated values inside the ledger discipline), or ADR-owned values that anything outside the ADR restates get an explicit restatement ledger beside the owner — for which the ADR's own `## Amendments` section, already obliged to name "the code location that now owns the value", is the natural home. Repo-level and not #200-shaped: it is a standards-document gap that any future amendment re-triggers.
- Source: #200 review cycle 1

## 2026-08-10 — The Amendments convention has no category for a value that was wrong when written

- Where: the `## Amendments` section of `docs/adr/README.md` — its scoping sentence ("facts … that code legitimately moves later") and the clause that gates the procedure ("When only a stated value has moved and the decision stands"); the entries that set the precedent against it are the 2026-08-10 amendments in the `## Amendments` sections of `docs/adr/0005-fleet-api-hosting.md` (the us-east-1 → eu-west-1 re-base) and `docs/adr/0004-ingestion-transport.md` (the "no resource bills for existing" absolute made precise)
- What: the convention admits exactly one kind of amendment — a value that was right when written and has since moved — and neither of #200's two changes is that kind. The rates were the **wrong Region when written** (us-east-1 list prices in a platform that deploys to eu-west-1; nothing moved, the original basis was wrong), and the "no resource bills for existing" absolute was **imprecise when written**, as #179/#188's audit showed; the amendment entries are candid about it, one of them saying in as many words that the absolute was shown imprecise rather than wrong. So correcting an original error is a third category, sitting between the movement the convention permits and the retcon supersession exists to prevent, and the document names neither the category nor its guardrail. The consequence is the shape rule 9 exists to stop: the governing rule for these amendments now lives only as precedent inside two ADRs' histories, where the next author will not look. **Deliberately deferred, not overlooked** — #200 planned exactly this sentence as chunk C4 and held it back, because the owner's waiver covered amendment-shaped edits to ADRs and not a change to the convention governing amendments; it therefore needs an explicit owner decision before anyone writes it. What it wants when that decision lands: one sentence in `docs/adr/README.md`'s `## Amendments` section naming correction-of-an-original-error as amendment-eligible, carrying the same guardrail the section already states — an amendment never touches reasoning, so if the reasoning was what was wrong, that is a supersession and wants a new ADR.
- Source: #200 review cycle 1

## 2026-08-10 — Amending an ADR turns every verbatim quotation of it into a misquotation, and nothing points from an amendment to its quoters

- Where: the `## Amendments` section of `docs/adr/README.md` — its procedure bullet, which requires an amendment to name "old value → new value, the driving issue/PR, and the code location that now owns the value" and never asks who quotes the old text. The amended text in this PR is `docs/adr/0004-ingestion-transport.md`'s Consequences headline ("**The platform's total standing cost is now $0.**" … "no resource in Cumulo bills for existing", now carrying "outside an always-free allowance"). Its quoters: `docs/adr/0005-fleet-api-hosting.md`'s `## Context` (the "The frame is ADR 0004's, deliberately" paragraph — "That document's Consequences open:" followed by the headline verbatim), `docs/adr/0006-demo-abuse-protection.md`'s `### B. AWS WAF rate-based rules — rejected, and not on price first` ("ADR 0004 got the platform's standing cost to exactly $0 and wrote …"), and — the one that is still **unpatched** — `docs/adr/0007-series-deletion-is-ttl-only.md`'s `### A. A sweeper — scheduled, or DynamoDB-Streams-driven off the site delete`, in its **It needs its own alarm** bullet, which reads `ADR 0004's headline is that "no resource in Cumulo bills for existing", and this would end that`. The same mechanism runs the other way between the budget-alarm header comment in `infra/api/alarms.tf` ("under ADR 0005's ≈ $36 worst case it would never fire at all") and the paragraph quoting it under `### 5. The backstops: alarm notifications and the budget` in `docs/adr/0006-demo-abuse-protection.md` ("since `infra/api/alarms.tf` already made the opposite one").
- What: an ADR amendment changes text that other documents have copied verbatim and attributed by name, so the moment it lands, every such copy is a quotation of words its stated source no longer contains. Nothing in the convention or the repo points from an amendment to its quoters, so whether a copy is caught is a property of how it was phrased. ADR 0005's Context asserts in the **present tense** that ADR 0004's Consequences "open" with the headline — false the instant the headline was amended, and patched in this PR with a parenthetical, so treat it as the worked **example**, not as outstanding work. ADR 0006's WAF rejection quotes the same headline and survives untouched only because it is in the **past tense** ("wrote") — a historically accurate claim about what the document said when it said it. The difference between the two is luck of phrasing, not discipline — and ADR 0007's copy proves it, because it is **both** present-tense and unpatched, which makes it the entry's most important example rather than a footnote. Its option A rejection says, in the present tense, that ADR 0004's headline **is** the pre-amendment wording, quoting the amended span exactly; nothing in this PR touches it, because ADR 0007 is merged and immutable, so correcting it is an amendment on 0007 and outside this PR's footprint. So of the three quoters, the one that was caught is the one the amending PR happened to have open, and the live misquotation is the one nobody was editing — which is the failure this entry describes, observed rather than hypothesised. **The same bullet carries a second casualty of the same amendment**, recorded here because it has the same root cause and wants the same fix: it argues that an eleventh CloudWatch alarm "would be the first one in the platform" that bills for existing, and ADR 0004's amendment now says the opposite in as many words — alarms already bill for existing, at $0 only because the always-free ten absorb them. That claim is reasoning rather than a quotation, so it is a second _kind_ of collateral an amendment can inflict on a document that points at it, and equally invisible from the amending side. The `infra/api/alarms.tf` case shows the direction that is easier still to miss: an **immutable ADR has frozen the text of a mutable non-ADR file**, so editing a Terraform comment falsifies an ADR's quotation of it, and the constraint is invisible from the Terraform side. Only that one instance is recorded anywhere, as a carve-out note on [#376](https://github.com/TomBennett-Lloyd/cumulo/issues/376); the other two are recorded nowhere but here. **How this differs from the two 2026-08-10 entries above it**, because triage will otherwise merge them: the rule-9 entry is about a stated **value** drifting from copies with no ledger, and both resolutions it names (narrowing rule 9's ADR exemption, or a ledger for ADR-owned values) would find none of the instances here — these are quoted **prose** becoming a false attribution, with no number in them at all (the WAF paragraph quotes a sentence, not a figure). The wrong-when-written entry is about which **categories of change** an amendment may make. This entry is about what an amendment **breaks elsewhere** once made, whatever its category. **Cycle 2's reviewer's own assessment, recorded so triage decides it deliberately rather than rediscovering it**: this reads as a sibling of the rule-9 entry and may well fold into it, on the grounds that "an amendable owner with no pointer to its copies" could be stated once to cover both values and quotations — the reason to record it separately is that the fold is a judgement about scope, and a merge that silently drops the quoted-prose half would leave the WAF and alarms.tf cases uncaptured. Three resolution directions, none of them picked here: (a) an amendment entry is required to name its known quoters — cheap, and it relies on the amender knowing who quotes them, which is the same knowledge whose absence caused this; (b) a mechanical check that greps for quoted spans of amended ADR text, the durable version, and probably a `check-*.sh` beside `check-adr-index.sh` in that family; (c) a convention that ADR-to-ADR references cite section and date rather than quoting verbatim, which prevents the class outright at a real cost in expressiveness — ADR 0005's Context quotes the headline precisely because the sentence is the argument.
- Source: #200 review cycle 2

## 2026-08-10 — A value true-up moved the numerals and left behind the prose characterisations derived from them

- Where: in `docs/adr/0005-fleet-api-hosting.md`, the "**Roughly a third of the ~$100/month ceiling**" sentence immediately below the worst-case table under `### The number that decides it`, and the closing REST-premium sentence of `### C. Lambda + API Gateway REST API` (the one carrying the "(us-east-1 basis on both sides; see Amendments)" caveat). In `docs/adr/0006-demo-abuse-protection.md`, the "**Roughly half the ~$100 ceiling**" sentence opening `### The worst-case bill, computed`, the same characterisation restated as "still roughly half" inside that document's 2026-08-10 `## Amendments` entry, and the budget-backstop paragraph under `### 5. The backstops: alarm notifications and the budget` ("only after roughly four weeks at the ceiling").
- What: one root cause, four instances — **a stated value was trued up and the prose characterisations derived from it were not re-checked**, so each characterisation now describes the old operand. 0005's bound moved ≈ $36 → ≈ $39, so "roughly a third of the ~$100/month ceiling" describes 39% where it described 36%. 0006's moved ≈ $54 → ≈ $59, so "roughly half" describes 59% where it described 54%, twice — once in Consequences and once inside the amendment entry that made the move. 0006's §5 backstop paragraph derives a duration rather than a fraction: $50 of a $59/month run rate is reached in 50/59 of a month ≈ 25.4 days ≈ 3.6 weeks, where at $54 it was 27.8 days, so "only after roughly four weeks at the ceiling" is now the loosest of the four. The fourth is a caveat's scope rather than a rounding: 0005's REST-premium sentence holds three figures and its "(us-east-1 basis on both sides; see Amendments)" caveat covers two of them, but the third — the bounded worst case being pushed "from roughly a third of the ceiling to essentially all of it" — is the amended eu-west-1 ≈ $39, not a us-east-1 figure; the conclusion is basis-independent (36 + 65 ≈ 101 on the old basis, 39 + 62 ≈ 101 on the new), so the caveat is imprecise rather than wrong. **Why all four were demotable and none is a correctness bug**: not one inverts or even weakens the conclusion it supports — a third is still a third, half is still half, a backstop that arrives in 3.6 weeks is still the late backstop §5 argues it is — and every reader gets the exact figure from the table sitting directly above or beside the sentence, so no reader is misled about a number, only about a rounding of one. The general lesson is the reusable part, and it is the thing to carry into the next amendment: **a value true-up must re-check the prose characterisations derived from the value, not only the numerals that restate it** — fractions of the ceiling, "roughly half", durations at a run rate, and the scope lines of basis caveats are all derived quantities that no grep for the old literal will ever surface, because the old literal is not in them.
- Source: #200 review cycle 3 — **demoted at the 3-cycle cap**. All four were pure-quality FIX-NOWs outstanding when the cap was reached, logged rather than fixed per `.claude/skills/review-loop/SKILL.md` ("remaining pure-quality FIX-NOWs get demoted to `docs/tech-debt.md` with a note that they were demoted at cap"). Recorded explicitly as cap-demotions because that is the signal `/retro` is asked to read: the quality bar and the 3-cycle cap were in tension on this PR. Note also that three of the four sites sit in `docs/adr/0006-demo-abuse-protection.md`, which #200's footprint does not include — so even without the cap they were not this PR's to fix, and clearing them means an amendment on 0006.

## 2026-08-10 — The header lane still reads layout non-atomically, one lane after the fleet panel stopped

- Where: `searchGeometry` in `apps/web/e2e/header.spec.ts` (its `Promise.all` over three `boundingBox()` calls, and the centring assertion its `leftGap`/`rightGap` feed), against the settled `layoutBoxOf` reader the fleet-panel cases in `apps/web/e2e/composition.spec.ts` use; the platform-dependence is named in `apps/web/src/header/header.css`, in the docblock over the `27.4375rem` fold media query (its "where a platform's fonts run wider than the measurement" paragraph, on the wordmark being set in the system stack)
- What: three bounding boxes read in sequence from a page that can still relayout, presented as one geometry. `Promise.all` does not make them atomic — it starts three round trips that resolve independently — so a font swap or any late reflow landing between the brand read and the search read moves `leftGap` without moving `rightGap`, and the 2px centring assertion fails on a page that was never off-centre. The wordmark is set in the system stack and its width is therefore platform-dependent, which is exactly the condition that makes a swap plausible rather than theoretical. This is the racy shape `layoutBoxOf` was written to replace and that in-flight [#367](https://github.com/TomBennett-Lloyd/cumulo/issues/367) is settling; batch #327's Amendment 2 routed the fleet-panel case to the settled helper, but nothing routed the header lane, so the two lanes now read layout two different ways. The fix is cross-cutting rather than local — adopt the settled reader across every geometry read in the header lane in one pass, once #367 lands and there is a single reader to adopt.
- Source: #327 design batch, review cycle 1

## 2026-08-10 — The close-affordance gate's reach is narrower than it looks, in two ways it does not say

- Where: `isGoverned` in `apps/web/src/close-affordance-contract.test.ts`, against the sibling idiom it is copied from — `isGoverned` in `apps/web/src/dashboard/state-copy-contract.test.ts`
- What: two gaps, neither a defect and neither written down until now. First, the copy narrows to `path.endsWith('.tsx')` where the original matches `/\.tsx?$/`, so a `.ts` module is outside the sweep here and inside it there; the narrowing is probably right (a close affordance is JSX) but it is undocumented, and a reader comparing the two idioms cannot tell a decision from a transcription slip. Second, and shared by both files: a text sweep sees the word only where the word is written, so `<button aria-label="…">{CLOSE_LABEL}</button>` with the string living in a `*-copy.ts` module evades both patterns of both gates — the exact-line one and the inline one. That is inherent to sweeping sources as text rather than rendering them, and the component tests named in the file's own docblock (`map/SitePopoverCard.test.tsx`, `header/AboutDialog.test.tsx`) are what actually see a rendered affordance. Recorded rather than fixed because the gate is worth what it costs as it stands; the point is that its reach should be legible from the file instead of living in a reviewer's head.
- Source: #327 design batch, review cycle 1

## 2026-08-10 — The pre-#274 unpolled-box shape survives at seven sites across the browser lane

- Where: `apps/web/e2e/composition.spec.ts` (the canvas read inside `swaps the loading placeholder for a laid-out WebGL canvas`, which could simply call `layoutBoxOf` in the same file); `apps/web/e2e/header.spec.ts` (`panFleetOutOfView`; the popover read inside `hangs the menu over the map rather than under it`; the brand/bar pair inside `folds the field behind an icon, opens it focused, and hands it back when the width returns`, under the `the search on a bar too narrow to hold it` describe); `apps/web/e2e/info-tips.spec.ts` (the panel read inside `opens the fleet chart’s description from the keyboard, and closes it back onto the button`); `apps/web/e2e/map-regressions.spec.ts` (`basemapPoint`); `apps/web/e2e/chart-surfaces.spec.ts` (`expectChartLaidOut`)
- What: seven sites still carry the shape `layoutBoxOf` was written to retire — a single unpolled `boundingBox()` (a pair of them at the header brand/bar site), throwing on `null`. What makes each of them a race is not that visibility is weak about layout: Playwright counts an element visible only when it has a non-empty bounding box and is not `visibility: hidden`, so an element that has not been laid out **fails** `toBeVisible` rather than passing it. The window is between the gate and the read, and it opens two ways. Where the read does follow a `toBeVisible()` on the element it then measures — the canvas, the header popover, the info-tip panel, and the bar half of the header brand/bar pair — the element that satisfied the gate can be swapped or re-created before the box is asked for, which is what `layoutBoxOf`'s own docblock in `composition.spec.ts` records: `.map-canvas` is worn by both the pending shell and the live map, so the gate can be answered by the placeholder and the read can land in the instant the placeholder has gone and maplibre's container has not yet been laid out. The rest were never gated on what they measure at all — `expectChartLaidOut` asserts the `figure` visible and then reads `figure.locator(PLOT_SVG)`, and a visible ancestor says nothing about a descendant's layout, while `panFleetOutOfView`, `basemapPoint` and the brand half of that same header pair read with no visibility assertion in front of them. Either way the read is a race against layout rather than a measurement of it: exactly the failure #274 documented and #367 re-proved, latent at every one of them. Under this lane's `retries: 0` (`apps/web/e2e/playwright.config.ts`) each firing is a red CI run and a manual re-run, and the throw names a defect the app does not have. The fix is mechanical — route each through the polling helper — but it is seven call sites across five specs, none of them #367's subject, and the helper currently lives in `composition.spec.ts` rather than anywhere the other specs can import it, so the change is really "give the lane a shared helper module, then move the sites onto it". Note that `chart-surfaces.spec.ts` was off-limits to #367 because it is PR #366's landing surface.
- Source: #367 review cycle 1

## 2026-08-10 — The deterministic regression case inherits the lane's flakiest step

- Where: the file-level `test.beforeEach` in `apps/web/e2e/composition.spec.ts` (`routeBasemap` + `page.goto('/')`), against the `layoutBoxOf returns the box its poll observed rather than re-reading it` case at the foot of the same file
- What: the new regression case takes no `page` fixture — its whole value is that it drives `layoutBoxOf` from a hand-built `BoxSource` and is therefore deterministic — but Playwright runs the file-level hook for every test in the file regardless, so the one case that needs no browser state still pays for a basemap route and a full page boot before it runs. The fix is a `test.describe` around the composition cases that owns the hook, leaving the helper's case outside it; that is a whole-file re-indent for a small gain, which is why it is logged rather than taken. The failure mode is benign in the meantime: a boot failure reds every case in the file and attributes itself to the hook, so nobody is misled about what broke — the cost is a deterministic case that can be failed by something it does not depend on.
- Source: #367 review cycle 1

## 2026-08-10 — Two settling box helpers in one lane, and rule 7's question is unanswered between them

- Where: `layoutBoxOf` in `apps/web/e2e/composition.spec.ts` and `settledPlotBox` in `apps/web/e2e/chart-surfaces.spec.ts` (the latter on PR #366's branch, not yet in `main`)
- What: `settledPlotBox` solves an adjacent problem — it waits for a box to _settle_, two matching readings, because its callers aim pointer events at the box — where `layoutBoxOf` deliberately does not settle, because its callers only need any box (its docblock says so). They may therefore be incidental duplication rather than one fact written twice, and `structure.md` rule 7's decision procedure ("if one copy changed, would the other be wrong until it changed the same way?") is the thing to answer explicitly once both are in the same tree, rather than either merging them on sight or letting a second near-copy accrete unremarked. Blocked until #366 merges: `chart-surfaces.spec.ts` is that PR's landing surface and touching it earlier guarantees a conflict.
- Source: #367 review cycle 1

## 2026-08-10 — Two lane comments credit a box-size assertion with catching what visibility already excludes

- Where: the comment above the canvas box-size assertion in the `swaps the loading placeholder for a laid-out WebGL canvas` case (`apps/web/e2e/composition.spec.ts`), and the comment above the corresponding assertion in `expectChartLaidOut` (`apps/web/e2e/chart-surfaces.spec.ts`)
- What: both say, in substance, "Visibility alone would pass on a canvas/plot collapsed to nothing". It would not. Playwright counts an element visible only when it has a non-empty bounding box and is not `visibility: hidden`, so a zero-sized element is **invisible** to it and the `toBeVisible()` above each comment has already excluded the defect the comment credits the box assertion with catching. What that costs differs by site, and both readings are worth having: in `composition.spec.ts` the gate and the measurement are the same canvas, so the `width > 0` / `height > 0` pair is vacuous against the stated defect — the only thing left for it to catch is a canvas swapped or re-created between the gate and the read, which is the #274/#367 race and not what the comment claims. In `expectChartLaidOut` the same pair is _not_ vacuous, but only by accident of a different element: the gate is on the `figure` and the box is read on `figure.locator(PLOT_SVG)`, and a visible ancestor says nothing about a descendant's layout — so the assertion earns its place while the reason written above it is still wrong about Playwright. The record exists because the identical misconception was written into this log's own "The pre-#274 unpolled-box shape survives at seven sites across the browser lane" entry and corrected in #367 review cycle 2: it should be corrected once across the sites that hold it rather than rediscovered file by file. Sweep basis, so the next reader knows how far this goes: the lane's _other_ "visibility is weak" comments are about **occlusion** — `toBeVisible` is a box plus a computed style rather than a hit test — which genuinely is outside Playwright's check, so the credit-band trial-click comments elsewhere in `composition.spec.ts`, `header.spec.ts`'s popover-stacking paragraph and `info-tips.spec.ts`'s tip-panel paragraph are accurate as written. Two nearby readings are not clean and belong with this rather than with those: `info-tips.spec.ts` closes its accurate occlusion paragraph with "a zero-height one is what a stacking or layout failure would leave", which is the same error in one clause, and the `height > 0` it justifies sits behind a `toBeVisible()` on that same panel; the header popover site asserts the same size pair behind a same-element gate but justifies it as a precondition for the `elementFromPoint` hit test that follows rather than as something visibility misses, which makes it redundant rather than untrue. The file-level docblock in `info-tips.spec.ts` uses the same "collapsed to nothing" wording about **jsdom**, where it is correct and unrelated. Not fixed here: `chart-surfaces.spec.ts` is off-limits until PR #366 lands, and correcting one of a pair is how the next reader concludes the other was checked.
- Source: #367 review cycle 2

## 2026-08-10 — The lane's `projects` docblock prices a second engine against a count nobody kept

- Where: the `projects` docblock in `apps/web/e2e/playwright.config.ts`, against the lane-charter docblock at the head of the same file
- What: the paragraph justifying one browser project says a second engine "would roughly double that cost to re-assert the same three facts". The lane is six spec files and thirty declared cases now, and #367 added one of them — `layoutBoxOf returns the box its poll observed rather than re-reading it`, at the foot of `composition.spec.ts` — which asserts no fact about the app at all: it proves the lane's own measuring instrument. So the arithmetic behind "the same three facts" no longer describes what a second engine would re-run, and the stated reason for staying on one project rests on a number that stopped being maintained. Pre-existing and untouched by #367's diff. It is logged rather than fixed because it belongs to the same lane-scope claim family as the charter docblock at the head of this very file, which #367 _did_ amend — that one now carves out the measuring-instrument case, so the file holds two statements of what this lane asserts and only one of them has been trued. Whoever trues one should true the other. Fix direction: either count, or replace the count with a claim about kind, which is what the paragraph actually means and is what stops the sentence ageing again on the next case added.
- Source: #367 review cycle 2

## 2026-08-10 — The poll's retry path is now testable and nothing tests it

- Where: the `layoutBoxOf returns the box its poll observed rather than re-reading it` case at the foot of `apps/web/e2e/composition.spec.ts`, against the `expect.poll` inside `layoutBoxOf` in the same file
- What: #367 introduced `BoxSource` as a seam so `layoutBoxOf` can be driven from a hand-built source, and used it for exactly one case — a source whose box is present on the first read and gone afterwards, which pins #367's own defect (the discarded-and-re-read sample). Nothing exercises the other half of the helper, the poll's **retry** path, and the gap is demonstrable rather than theoretical: a mutant that deletes the poll altogether — a single `await source.boundingBox()` followed by the existing guard — survives the entire lane deterministically. The regression case still passes, because its stub answers on read one and never needs a retry; and the browser-driven cases pass either way, because the race they would catch is nondeterministic by definition and does not reproduce on a developer machine (#367's own evidence: 30/30 green locally on the tree that failed CI three consecutive times). So the behaviour `layoutBoxOf` exists for — waiting out an element that has no box _yet_, which is what #274 added the poll to fix — is currently unpinned by any deterministic assertion, and a future refactor could delete it with a green lane. The fix is small and uses machinery this ticket already built: one more `BoxSource` whose box appears on a later read, asserting the helper retries and returns the settling sample. It is logged rather than taken because #367 was scoped to a flake on the critical path for PR #366, and because it belongs with the shared-helper follow-up already recorded above — whoever answers `structure.md` rule 7 for `layoutBoxOf` and `settledPlotBox` will be writing this lane's box-helper cases anyway, and should cover both paths then.
- Source: #367 review cycle 3

## 2026-08-10 — The live region's place in the figure is a written contract that nothing asserts, and #331 moved it across a component boundary

- Where: the `<p className="forecast-chart-readout" aria-live="polite">` returned by `ForecastChartHoverBoundary` (`apps/web/src/charts/forecast-chart-hover-boundary.tsx`), against the legend and table twin that follow it — `forecastChartLegend` and `forecastChartTable`, both still composed by `ForecastChart` (`apps/web/src/charts/ForecastChart.tsx`). The contract it has to satisfy is the "one visually hidden `aria-live="polite"` region in the figure, between the plot and the legend" clause in `docs/design/chart-treatment.md`, which `docs/standards/react.md`'s live-region budget reasons about in the same terms. The tests that touch the region reach it by bare selector and never ask where it sits: the readout cases in `apps/web/src/charts/forecast-chart-readout.test.tsx`, `ForecastChart.test.tsx` and `forecast-chart-union-domain.test.tsx` all `querySelector('.forecast-chart-readout')`; the only document-order assertion in the panel's structure suite (`apps/web/src/dashboard/FleetPanel.structure.test.tsx`) is over the heading row, not the figure's children
- What: the ordering plot → readout → legend → table is stated in the design doc as a requirement and held in the code by reading alone. Nothing goes red if it breaks, and this branch is exactly the kind of change that could break it: the `<p>` now lives in a different component from the legend and table it must sit between, so its position is the product of two files agreeing rather than one file's JSX. Emitting it after the legend, or moving it inside the `<svg>` after `{children}`, leaves all 135 chart tests green — verified by reading, not by mutating, since the point is that no assertion exists to fail. The screen-reader consequence of getting it wrong is real but quiet: a live region announced after the legend reads the sample's numbers to a reader who has already been told what the series are, in the wrong order relative to the plot it belongs to, and no sighted review catches it because the region is visually hidden. This is a pre-existing gap rather than a regression — the ordering is correct on this branch — but the new seam makes it cheaper to trip and there is no longer a single component whose JSX a reviewer can read to confirm it. Fix direction: one child-order assertion on `.forecast-chart-figure` (the figure's children are svg → readout → legend → table), placed where the fleet panel's figure-in-every-state contract already lives, so the ratchet covers every state rather than only the ready one. Cross-cutting rather than chart-local, which is why it is logged rather than folded into #331
- Source: #331 review cycle 1 (SYSTEMIC)
